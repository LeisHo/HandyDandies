HANDY DANDIES -- CHANGELOG
================================================================================

PROJECT START: 2026-09-12  -  06:53 AM EDT (approximate; see this entry's own note)

--------------------------------------------------------------------------------
2026-09-12  -  ~06:54 AM - 07:22 AM EDT (bounded by checkpoints; see Verified note)

Initial build: a three.js scene filled with a grid of rigged hand instances
that each independently rotate to point at the live cursor. Built per
explicit instruction to follow HANDO's own hand-rendering architecture as
the reference.

Added:
- Reused HANDO's rigged hand asset (`data/HAND3D/HAND-021/Hand2.glb`,
  22-bone skeleton, GLB format), physically copied into this project's own
  `data/processed/HAND3D/Hand2.glb` (no cross-project asset serving).
- Reused HANDO's generic dev-panel engine (`src/devpanel/devPanel.js`) and
  its dev-panel CSS chrome (`src/style.css`) verbatim.
- New: cursor-to-3D-target math (HANDO itself has no cursor-tracking --
  it's a manual single-hand pose tool). Each frame, the cursor's screen
  position is raycast against a plane positioned in front of the hand
  field (toward the camera); each hand instance slerps its rotation toward
  a look-at quaternion aimed at that 3D point.
- New: per-instance skeleton cloning (`SkeletonUtils.clone()`) so each of
  the 54 (default 6x9 grid) hand instances can aim independently -- HANDO
  only ever renders one hand, so this had no precedent to follow.
- New: bind-pose "pointing direction" measurement (wrist bone `rHand` to
  fingertip bone `rMid3`, in world space at load time) rather than assuming
  a fixed axis -- matches HANDO's own "measured, not assumed" convention
  for this rig.
- Dev panel groups: Field Layout (rows/columns/spacing/hand scale), Cursor
  Tracking (damping, target depth, target-marker toggle), Camera (FOV,
  distance), Lighting (key light azimuth/elevation/intensity/color,
  ambient), Background, Debug (grid helper).

Fixed (both caught live during the build, before this entry):
- `three/addons/utils/SkeletonUtils.js` (three@0.169.0, the pinned CDN
  version) exports `clone`/`retarget`/`retargetClip` directly, not a
  `SkeletonUtils` namespace object -- confirmed by directly fetching and
  reading the served module source. `import { SkeletonUtils } from '...'`
  threw `SyntaxError: does not provide an export named 'SkeletonUtils'` at
  module-load time. Fixed via `import { clone as cloneSkeletal } from
  'three/addons/utils/SkeletonUtils.js'`.
- `initDevPanel()`'s synchronous onChange-on-restore behavior would throw a
  TDZ (temporal dead zone) error against `camera`/`keyLight`/etc. (declared
  later in the file, via `const`) the first time a returning visitor had
  saved settings. Fixed with the same detach-onChange-before-init/
  reattach-after mitigation HANDO's own `main.js` already uses for this
  exact documented gotcha.

Verified:
- Live in the browser (a local `python3 -m http.server` on port 8421):
  loaded with 0 real console errors (one lingering console-log entry from
  the SkeletonUtils error was confirmed STALE -- it persisted in
  `read_console_messages`'s own history even after a fresh reload where
  the hands were already rendering correctly on screen, confirmed via
  screenshot at the same moment; see CODE_SUMMARY.txt's own Gotchas).
- The core requested behavior, via screenshots at 3 cursor positions:
  center (idle default) showed all 54 hands radially converging on one
  point in front of the field; cursor near the top of the browser pitched
  every hand into a darker, silhouette-like underside view; cursor near
  the bottom pitched them into a clearly-lit top/back view with visible
  finger detail -- matching the user's stated spec (top -> see hand
  undersides, bottom -> see hand tops) directly, not just plausibly.
- The "Field Layout" dev-panel group renders and its controls (e.g. "Rows
  (Count)") are reachable/clickable in the live panel.
- Did NOT independently verify: the look-at math's behavior when a hand's
  X position exactly matches the cursor target's X (a look-at degenerate
  case), dev-panel Save/Reset round-tripping for this project's own new
  controls, or any resolution/aspect ratio other than the ~800x450 and
  1280x720 browser-pane sizes used during this session.

Timing note: no single clean start timestamp was captured for this task
(it began mid-conversation, not at a fresh prompt) -- the entry's own
timestamp range is bounded by 2 real checkpoints taken during the work
(07:01:07 and 07:13:28 EDT) plus this entry's own finish time, not a
single measured start-to-finish interval. See the project's ETA History
log entry for the same task for the precise measured segments.

--------------------------------------------------------------------------------
2026-09-12 (continued)  -  ~07:36 AM - 07:52 AM EDT

10 follow-on requests in one turn: camera click-drag-to-pan + scroll/pinch-
to-zoom with full mobile porting; Field Layout decoupled from view scale;
independent Row/Column Spacing; Alternate + Progressive row-offset patterns
with a mode-switch checkbox; Camera X/Y/Z position sliders; Lighting/Toon
Shading/Outline settings "just like HANDO."

Added:
- OrbitControls with `enableRotate: false` -- click-drag pans, scroll/pinch
  zooms, on both desktop and mobile via OrbitControls' own `touches: {
  ONE: PAN, TWO: DOLLY_PAN }` config (no custom touch-handling code
  needed). Camera X/Y/Z Position + FOV dev-panel sliders, 2-way synced with
  live mouse pan/zoom (`applyCameraControl()`/`syncCameraPanelFromLive()`,
  ported from HANDO's own Camera-group pattern, simplified since this
  camera never orbits).
- Row Spacing and Column Spacing as 2 independent sliders, replacing the
  single shared "Cell Spacing." Alternate Row Offset (brick/hex-style,
  every other row shifted along the column axis) and Progressive Row
  Offset (row `r` shifted by `r` times the setting) as 2 mutually-exclusive
  modes via a new checkbox.
- Full Lighting group parity with HANDO (added Key Light Aim Height). New
  Toon Shading group (steps/threshold/floor/ceiling/base tint/texture
  influence/tint/rim intensity-power-color) and Outline group (enabled/
  use-screen-space-pass/color/hull thickness/pass thickness-strength-glow)
  -- both ported from HANDO's own shader code and control sets, adapted for
  N hand instances (one shared toon material; a per-instance hull-outline
  clone OR a single OutlinePass covering every instance's skinned mesh,
  toggleable).

Fixed:
- Field Layout previously called camera/lighting/target-plane framing
  functions on every rows/cols/spacing change, so changing the grid
  visibly rescaled the camera view -- explicitly not wanted ("Field
  layout shouldn't affect view scale"). Framing is now derived from the
  field's bounding size exactly ONCE (`framedOnce` flag in
  `rebuildField()`) and frozen after that; Field Layout changes only ever
  touch `relayoutField()` (positions/scale), never camera/light/
  target-plane again.
- `window.innerWidth`/`innerHeight` reading 0 at script-parse time in this
  dev environment left the renderer/`EffectComposer`/`OutlinePass`
  permanently zero-sized once post-processing was added (confirmed:
  `renderer.getSize()` reporting `[0,0]`, `GL_INVALID_FRAMEBUFFER_OPERATION:
  ...Attachment has zero size` spamming the console). Fixed with a
  per-frame self-healing size check in `animate()`, sharing one
  `applyRendererSize()` function with the `resize` event listener --
  same fix shape as a previously-documented DOTFLICKO gotcha for this
  exact class of quirk.

Verified:
- Click-drag pan and scroll-wheel zoom, via screenshots showing the field
  genuinely shift and scale on each gesture.
- Field-layout/camera decoupling, DIRECTLY (not just visually): dispatched
  a real `input`/`change` event on the live "Rows (Count)" slider (6 -> 10,
  hand count 54 -> 90) and confirmed `camera.position` was numerically
  IDENTICAL before and after, via a `window.__debug` inspection hook.
- Progressive Row Offset visually produces the expected cascading/
  staircase pattern across rows (screenshot-confirmed).
- Both outline techniques (hull-mesh clone and screen-space OutlinePass)
  render a visible black outline when toggled independently -- confirmed
  via 2 separate screenshots with `Use OutlinePass` on and off.
- Every new dev-panel group and control (Row/Column Spacing, Alternate/
  Progressive Row Offset + mode checkbox, Camera X/Y/Z, Rim Light
  Intensity, Use OutlinePass, etc.) is present and reachable in the live
  panel (confirmed via `find` against their exact labels).
- Mobile touch-drag was exercised under 375x812 viewport emulation and
  produced a correct pan -- but NOT confirmed as a genuine touch-event
  path: this session's own browser-automation tool documents that its
  drags "still arrive as mouse clicks" even under mobile emulation, so
  this only confirms the pan logic is viewport-size-agnostic, not that
  real on-device touch/pinch gestures work. OrbitControls' `touches` config
  is a proven three.js library feature, which lowers but doesn't eliminate
  that residual risk.
- Separately identified during this verification (NOT a page bug, and NOT
  what the self-healing fix above addresses): this session's own browser
  tool reports `document.hidden === true` and `window.innerWidth === 0`
  whenever queried via its JS-execution path, confirmed even on a freshly-
  created, explicitly-foregrounded tab -- while a `screenshot` call on the
  identical tab at the identical moment renders correctly at full
  resolution. Documented as a tool characteristic in CODE_SUMMARY.txt's
  GOTCHAS so a future session doesn't mistake it for a real page defect.

Disclosed interpretation call: "Alternate Row Offset in perpendicular
direction" was ambiguous -- implemented as the standard brick/hex-pattern
reading (shift along the column axis), not a Z-depth stagger. Flagged to
the user rather than silently assumed; a 1-line change in `relayoutField()`
if the other interpretation was actually meant.

--------------------------------------------------------------------------------
2026-09-12 (continued again)  -  ~08:19 AM - 08:29 AM EDT

3 more requests: a Camera Zoom slider 2-way synced with scroll/pinch;
baking the user's own pasted live-tuned dev-panel settings dump in as the
new defaults; render-order stacking driven by distance from the cursor
(farthest hands on top, nearest at the bottom). Plus: create a GitHub repo
and push (see next entry).

Added:
- "Zoom (Distance To Pan Target)" slider in the Camera group --
  `setCameraDistance()` moves the camera along its existing camera-to-
  target line to a new distance (the same axis OrbitControls' own scroll/
  pinch dolly already moves it along); `syncCameraPanelFromLive()` reads
  `camera.position.distanceTo(controls.target)` back into the slider every
  frame. Confirmed both directions live: scrolling moved the slider
  259 -> 172; setting the slider to 400 moved the live camera distance to
  exactly 400.
- Cursor-distance render ordering: every frame, each hand's
  `skinnedMesh.renderOrder` is set to its own live distance from the
  cursor target (farther = higher = drawn later = ends up on top); its
  `outlineMesh.renderOrder` is that value minus a small epsilon so a
  hand's own outline always draws just under its own fill. Required
  `depthTest: false` on both hand materials (kept `depthWrite: true`) --
  three.js's normal depth-tested compositing otherwise always lets real
  camera depth win over renderOrder. Disclosed tradeoff: a single hand's
  own internal self-occlusion (finger over finger) now relies on
  front-face culling + triangle order, not a real per-pixel depth test --
  acceptable for this model's mostly-convex static bind pose.
- Every dev-panel default baked directly from the user's own pasted
  live-tuned settings dump (a raw `devPanel.js` `values`/`order` JSON
  export): grid grew from 6x9/54 hands to 17x30/510 hands; Row/Column
  Spacing 10/11.5; Alternate Row Offset -5.5; camera positioned at
  (-1.80, -1.30, 259.09), FOV 35; Lighting/Toon/Outline/Background all
  retuned (white background, ambient off, key light white at full
  intensity from azimuth 231/elevation 28/aim height -100%, 2-step toon
  shading, outline off by default). Also widened `fieldRows`/`fieldCols`
  slider max (20/24 -> 40/40) since the new defaults (17/30) needed the
  room.

Verified:
- All 3 features confirmed live (not just visually): `renderOrder` values
  sampled directly via `window.__debug` showed 5 hands' render order
  decreasing monotonically as their distance from the cursor target
  decreased, with each outline's own renderOrder exactly 0.01 below its
  fill's; the Zoom slider's 2-way sync (see Added, above); `depthTest:
  false` confirmed present on the live material.
- Re-confirmed the "hidden tab suspends rendering" environment quirk
  (documented in the prior round) independently: at 510 hand instances,
  an un-fronted tab's `requestAnimationFrame` genuinely never fires (a
  frame-counting script itself timed out waiting), which first looked like
  a real performance regression from the much larger hand count until the
  tab was explicitly fronted (`tabs_select`) and rendered correctly at
  full detail on the very next screenshot -- not a new bug, the same
  already-documented tool characteristic applying at a larger scale.

--------------------------------------------------------------------------------
2026-09-12 (continued yet again)  -  ~08:52 AM - 09:04 AM EDT

1 request: a "Reordering Flash" checkbox so 2 overlapping hands' cursor-
distance render order doesn't flip/flash while they're still overlapping,
only once they visibly clear each other.

Added:
- `preventReorderFlash` checkbox (Debug group). When on,
  `updateRenderOrder()` approximates every hand's on-screen footprint as a
  circle (world radius = half the measured hand length x its own scale,
  projected to screen pixels via standard perspective scaling) and runs an
  O(n^2) pairwise screen-space overlap check across all hands. A hand's
  `effectiveRenderOrder` (now a persisted field, not recomputed from
  scratch every frame) only updates from its live cursor-distance when it
  is NOT currently overlapping anything -- while overlapping, it keeps its
  prior order, so 2 fighting hands can't flip mid-overlap; it only
  re-evaluates once that hand clears every overlap it was in. Off by
  default (zero added cost when disabled -- the pairwise check is fully
  gated behind the checkbox).

Verified:
- The full freeze -> stay-frozen-despite-large-distance-change -> resume
  cycle, via a `window.__debug`-driven test in one atomic script: forced 2
  hands to identical positions (guaranteed overlap), captured their order;
  moved the cursor target to a wildly different point (massively changing
  live distance) and re-ran the update -- order stayed byte-identical;
  restored one hand's real grid position (clearing the overlap) and
  re-ran -- order immediately jumped to the new live value. All 3 phases
  confirmed in a single script run, eliminating any ambiguity about
  ordering/timing between separate tool calls.

Debugging note (real time cost, worth recording): initial verification
attempts showed the overlap check always reporting no-overlap and NaN-like
projected screen positions, which looked like a bug in the new code. Root
cause was NOT the new code: this session's Browser pane was not currently
displayed at all (confirmed via `tabs_context`: "The Browser pane is
currently hidden"), which fully suspends `requestAnimationFrame`
(confirmed with a vanilla rAF-counting script having nothing to do with
this project) -- and since the existing camera-resize self-heal fix (added
in an earlier round for the `window.innerWidth`-reads-0-at-boot issue)
lives inside the rAF loop, it never got a chance to run, leaving the
camera stuck with a NaN projection matrix (constructed with
`aspect = 0/0` at boot). Manually repairing the camera via a debug hook
(`camera.aspect = <real ratio>; camera.updateProjectionMatrix()`) before
re-testing resolved it completely -- the overlap-detection code itself was
correct the whole time. See CODE_SUMMARY.txt's GOTCHAS for the durable
writeup.

Not committed to git yet -- this project's own git discipline (CLAUDE.md
§9) requires an explicit push instruction, which wasn't given for this
specific change.

--------------------------------------------------------------------------------
2026-09-12 (continued yet again)  -  ~09:15 AM - 09:23 AM EDT

Bug report: "certain hands are always rendering on top regardless of
clearance" -- the previous round's "Prevent Reordering Flash" checkbox.

Root-caused via direct simulation (not guessing): the original design froze
a hand's ENTIRE render order the instant it overlapped ANYTHING, resuming
only once every overlap cleared. A 240-frame simulated sweep at the
default zoom showed 0% overlap (fix never even engaged) -- but the SAME
sweep at a closer zoom (matching how a user would actually zoom in to
inspect this exact behavior) showed ALL 510 hands overlapping something
in EVERY frame, meaning none of them ever got a single overlap-free frame
to unfreeze on. The whole field froze solid, permanently, the instant the
checkbox was turned on in anything but a sparse view -- exactly the
reported symptom (whichever hands happened to be on top at that instant
stayed there forever).

Fixed:
- Replaced the per-hand freeze with a per-PAIR lock (`pairLocks` Map).
  Every hand's `effectiveRenderOrder` is now ALWAYS its own live cursor
  distance -- never frozen. For each currently-overlapping PAIR, a lock
  remembers which of the 2 should stay underneath (decided once, from
  their live order when the overlap began); each frame, only a pair whose
  live order would violate its own active lock gets a small corrective
  nudge, and the lock is dropped the instant that pair stops overlapping.
  A hand can never fully disconnect from reality, so a scene-wide freeze
  is no longer structurally possible.
- Fixed a real TDZ crash introduced while relocating this logic:
  `pairLocks` was declared right next to `updateRenderOrder()`, further
  down the file than `animate()`'s own call site -- and `animate()`'s
  first, synchronous invocation reaches `updateRenderOrder()`'s `else`
  branch (which reads `pairLocks.size`) before the rest of the module's
  top-level code, including that declaration, has run. Moved `pairLocks`
  (and `projectScratch`) up next to the other early module-level state.

Verified:
- Re-ran the EXACT repro (240-frame sweep, camera zoomed to distance 60):
  0 hands showed zero order-changes (the previous bug's signature) --
  every single hand updated its order on nearly every frame (239/239
  possible changes each).
- Confirmed the actual anti-flash behavior still works post-fix: forced 2
  hands into a sustained, moving screen overlap across 100 frames of live
  cursor movement -- their relative order (which one's value was larger)
  never flipped once, while their absolute values kept tracking live
  distance (unlike the old frozen-solid version).
- Both tests driven via `window.__debug` state manipulation, not a live
  screenshot -- this session's Browser pane was again not displayed
  (see the prior round's own GOTCHAS entry for this recurring tool
  characteristic).

Not committed to git yet -- awaiting an explicit push instruction.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~09:33 AM - 09:46 AM EDT

Bug report against the immediately-preceding round's own fix: "its better
but not fully resolved i still see some hands that are incorrectly
rendring above a hand further than the cursor" + "the previous build had
no flashes, but it now does."

Root-caused via direct measurement, not guessing. Simulated a realistic
cursor sweep and measured 2 things directly: (1) among ~87,000 random
hand pairs whose TRUE cursor distances differed by more than 15 world
units (a large, unambiguous gap), how often the ASSIGNED render order
still put the closer one on top; (2) among hands actually overlapping on
screen in consecutive frames, how often their relative order flipped.
The prior round's per-pair "unconditionally pin to partner+epsilon"
design scored 21% on test (1) -- worse than doing nothing -- because in
this dense field, chains of simultaneous locks (A-under-B-under-C...)
compound: every hand in a long chain collapses toward whichever hand
anchors the chain, discarding real distances almost entirely. That's
exactly the reported regression.

Fixed:
- Replaced the entire lock/pin system with per-hand exponential
  smoothing. A hand not currently overlapping anything snaps straight to
  its own live cursor distance (fully responsive, no lag). A hand that IS
  overlapping something blends toward its own live distance instead of
  snapping (`effectiveRenderOrder += (live - effectiveRenderOrder) * 0.15`
  per frame) -- critically, NEVER derived from another hand's value, so
  the chain-collapse failure mode (and the earlier freeze-forever failure
  mode, 2 rounds ago) are both structurally impossible now, not just
  patched around.
- Fixed a stray `#` typo (should have been `//`) introduced while writing
  this fix's own comment block, which would have been a hard syntax error.

Verified:
- Re-ran the same 87,000-pair "clearly different distance" test against
  the new design: 0.09% wrong-order rate (vs. the previous round's 21%).
- Re-ran the 240-frame stuck-hand check: 0 hands frozen (matches the
  already-fixed permanent-freeze bug from 2 rounds ago -- confirms this
  rewrite didn't reintroduce it).
- Measured the frame-to-frame flip rate among hands actually overlapping
  on screen: ~0.6%, roughly half of what the SAME scenario measures with
  the checkbox off entirely -- confirms the feature still does something
  real, not just "stopped breaking things."
- Did NOT get a clean live visual confirmation this round either (see
  GOTCHAS) -- all 3 measurements above were taken via direct
  `window.__debug`-driven simulation of realistic and dense/close-zoom
  cursor movement, not a screenshot.

Disclosed trade-off: this is no longer a literal "won't change until they
visibly clear" hard freeze, as originally specified -- it's a fast blend
that mostly prevents instant pops. 2 different literal-freeze designs
(this round's predecessor and the one before it) each produced a worse,
concrete bug once tested against this field's actual density, so this
round deliberately trades the literal spec for a measurably more robust
mechanism rather than attempting a 4th variation on hard locking.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~10:19 AM - 10:25 AM EDT

Follow-up bug report against the smoothing fix: "still see flashing...
the thumb of the hand doesnt seem to be accounted for... only happens
when I move the cursor left and right, not up and down."

Root-caused via direct measurement of the actual mesh geometry (not
guessing): the reordering-flash overlap check approximated each hand's
on-screen footprint as a circle centered at `wrapper.position` with
radius = half the wrist-to-fingertip bone distance (`handLengthRaw`).
Measured the ACTUAL rendered mesh's bounding sphere directly
(`THREE.Box3.setFromObject()` + `.getBoundingSphere()`) and found: the
real sphere's radius is ~23 world units vs. the assumed ~8 (undersized by
~5x), and its true center sits ~13 world units from `wrapper.position` --
because `Hand2.glb`'s visible mesh includes a full forearm below the
wrist that the bone-distance measurement never accounted for. The
mis-centered, undersized circle missed the thumb (and most of the
forearm) at many rotations, meaning those overlaps got ZERO flash
protection at all -- explaining both symptoms exactly: the "pop" is the
thumb entering a screen region the algorithm never knew was part of the
hand, and it's specifically a left/right (yaw) symptom because yawing
swings that off-center mass much further across the screen than
pitching does.

Fixed:
- Measure the real mesh bounding sphere ONCE at load time
  (`handBoundsCenterLocal`/`handBoundsRadiusLocal`), in the same
  identity-transform frame `alignQuat`/`handLengthRaw` already use.
- Each frame, transform the sphere's LOCAL center through the hand's
  actual live transform chain (scale -> alignQuat -> live wrapper
  rotation -> wrapper position) instead of assuming `wrapper.position`
  directly -- a sphere's radius only needs scaling, not rotating, so only
  the center needed the full chain.
- Deliberately left `handLengthRaw` itself untouched (it still drives
  grid scale-fitting and outline thickness) -- this fix is scoped only to
  the reordering-flash overlap circle.

Verified:
- Re-ran the same grid-neighbor overlap-observation count from the prior
  round's own test: 975,875 overlap-observations across a 150-frame sweep
  with the corrected circle, vs. 326,012 with the old one at the same
  positions -- roughly 3x more genuine overlaps are now actually detected
  and protected (this is the concrete, measured fix for "the thumb wasn't
  accounted for").
- Re-ran the same 3-metric validation suite from the prior round (0 stuck
  hands; wrong-order rate for clearly-different-distance pairs; flip rate
  for actually-overlapping pairs) -- all held steady or improved slightly
  with the corrected geometry, confirming this change didn't regress the
  design fixed 2 entries ago.
- All measurements taken via `window.__debug`-driven simulation, not a
  live screenshot (Browser pane still undisplayed this session).

Not committed to git yet.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~10:35 AM - 10:38 AM EDT

Investigation only, no code change. Follow-up: "when i move the cursor
left and right, i still see flashing above and below it on that vertical
axis... its mostly the thumb of one hand popping ontop of the hand next
to it."

Traced a specific vertically-adjacent hand pair (same column, adjacent
rows) through a horizontal-only cursor sweep, frame by frame. Found: the
gap between their smoothed render-order values shrinks perfectly cleanly
and monotonically, frame over frame, with zero discontinuity anywhere
(-1.24 -> -0.92 -> -0.58 -> -0.23 -> +0.11 -> +0.43 across 6 consecutive
frames) -- yet the instant that gap crosses zero, the actual draw order
(and therefore the visible stacking) flips completely in a single frame,
identical to what would happen with no smoothing at all.

Root cause, structural rather than a further bug: `renderOrder` is a
discrete sort key. A hand is drawn strictly before or after another, with
no partial/blended state in between -- this whole feature exists because
`depthTest: false` removes the GPU's own continuous, per-pixel depth
resolution. Smoothing the underlying NUMBER (which is what every fix so
far has adjusted) cannot smooth the resulting binary SWAP. The prior 2
rounds' fixes (geometry-correct overlap detection, then per-hand
smoothing) are still both doing real, measured work -- they fix wrong
overall ordering and damp noise-driven spurious crossings -- but neither
one, nor any further tuning of the same mechanism, can eliminate the
instant pop that happens at a GENUINE crossing (the cursor's own movement
actually causing 2 hands' true relative order to swap).

Not fixed this round -- flagged to the user as a scope/architecture
decision rather than attempted unilaterally, since a real fix (e.g. an
alpha cross-fade between the 2 competing hands during the crossing
window) is a materially bigger change than anything else in this
feature's history and conflicts with the current opaque/`depthTest:false`
approach.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~10:40 AM - 10:43 AM EDT

Request: "make it so that row and column spacing is not tied to hand
scale. changing one should not affect the other."

`computeBaseScale()` previously derived the auto-fit scale factor from
`Math.min(rowSpacing, columnSpacing) * 0.8`, so dragging either spacing
slider silently rescaled every hand too. Replaced with a fixed world-unit
reference length (`HAND_SCALE_REFERENCE_LENGTH = 8`, chosen to match what
that formula evaluated to at this project's own original spacing defaults
so existing settings don't visually jump) -- `cfg.handScale` is now the
ONLY thing that scales hands; Row/Column Spacing only reposition them.

Verified directly: forced Row Spacing 10->30 and Column Spacing 11.5->2
via live slider events and confirmed `computeBaseScale()`'s result (and
the actual applied `hand.clone.scale.x`) stayed numerically IDENTICAL
across both changes; then changed Hand Scale 1.2->2.0 and confirmed the
scale DID change (0.597->0.995) -- the 2 controls are now fully
independent. Also confirmed hand positions still update correctly with
the new spacing (the position delta between 2 adjacent hands matched the
new Column Spacing value exactly).

Not committed to git yet.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~10:47 AM - 10:53 AM EDT

Investigation only, no code change. Bug report with screenshot: "I set
the toon shader to 2 toon steps... in certain shaded areas, the surface
is quite noisy looking instead of looking like a flat tone."

Isolated to a single, non-overlapping hand first (rules out any
`depthTest:false`/render-order interaction from recent rounds). Tested 4
separate hypotheses directly, live in the browser:
- Removed the diffuse texture map entirely (`material.map = null`) --
  noise unchanged. Rules out the texture.
- Called `geometry.computeVertexNormals()` to fully re-derive smooth
  normals from scratch, ignoring whatever the source file shipped --
  noise unchanged. Rules out bad/hard-split source normals.
- Raised `keyElevation` from the default 28 degrees (fairly grazing) to
  80 (near-overhead) -- noise area shrank noticeably (a small patch
  remained near the thumb/wrist joint). Confirms light ANGLE is a real
  factor.
- Raised `ambientIntensity` from 0 -- the facet pattern got MORE visible,
  not less. Confirms why: `MeshToonMaterial` only routes DIRECT lights
  through the gradient-map toon banding; ambient/hemisphere (indirect)
  light contributes smoothly, un-quantized, directly revealing whatever
  fine curvature the mesh actually has.

Conclusion: this is real, fine geometric detail in `Hand2.glb`'s own mesh
(likely near the wrist-thumb joint/seam), not a shader or material bug.
A raking light angle -- and any non-zero ambient regardless of angle --
will reveal it, independent of toon step count (also confirmed:
increasing toonSteps to 8/16 did not reduce the noise, since the
underlying normal variation is the same regardless of how many bands the
lighting gets quantized into).

Not fixed with a code change -- flagged to the user as a lighting/
settings characteristic (keep key light closer to overhead, keep ambient
low) rather than something further shader tuning can eliminate for this
specific asset.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~11:08 AM - 11:15 AM EDT

CORRECTION to the 10:47-10:53 AM entry above. Investigation only, no code
change. User directly challenged that entry's conclusion: "but that noise
doesnt occur in HANDO" -- a decisive counter-example, since HANDO renders
the identical `Hand2.glb`.

Re-investigated. First tried isolating a single hand and toggling
`depthTest` true/false directly (suspecting self-occlusion within a
single hand's own complex geometry, since that material is the one real
difference from HANDO's own toon material found by re-reading HANDO's
source). Ruled this out by direct measurement: connected-component
analysis of a 100x100px live sample region found the island count for the
minority toon tone changed by only ~8% (78 islands at `depthTest:false`
vs. 71 at `depthTest:true`, out of ~800 lit pixels) -- nowhere near
enough to explain the effect.

Found the real cause by diffing this project's baked toon/lighting
defaults directly against HANDO's own (`HANDO/src/main.js` DEV_GROUPS):
this project bakes `ambientIntensity:0` with a full-contrast
`toonSteps:2` / `toonShadowFloor:12` / `toonLightCeiling:100` (from the
user's own earlier live-tuned settings dump), while HANDO defaults to
`ambientIntensity:0.7` with a narrower `toonSteps:3` / `13` / `81`.
Confirmed directly: applying HANDO's recipe live to the same sample
region turned a stark 2-value luminance spread (132/255, a 123-unit
jump, in 78 small scattered islands) into a smooth 58-value gradient
(136-232) with a comparable island count -- i.e. the real geometric
variation identified in the prior entry is genuinely there either way,
but HANDO's smooth ambient fill plus narrower/softer toon contrast hides
it, while this project's zero-ambient/full-contrast recipe amplifies the
exact same variation into stark black-or-white speckle.

The prior entry's individual test results are still accurate as far as
they went (ambient raised ALONE, without also narrowing the toon range,
does make it worse) -- what was missing was testing HANDO's actual
combined recipe, not just one variable in isolation. Corrected
`CODE_SUMMARY.txt`'s GOTCHAS entry to reflect this.

Not fixed with a code change -- this is a lighting/toon-defaults tuning
decision (which new `ambientIntensity`/`toonSteps`/`toonShadowFloor`/
`toonLightCeiling` values to bake), flagged to the user rather than
changed unilaterally, since the current values were explicitly baked
from the user's own earlier live-tuned settings dump.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~11:19 AM - 11:39 AM EDT

2nd CORRECTION to the toon-shading-noise investigation, and the actual
fix. The user directly disproved the immediately-preceding entry's
ambient/toonSteps theory: "In Hando, even with 0 ambient light and 2 step
toon shader it still doesnt have that noise." Re-investigated a 3rd time.

First re-tried the original depthTest hypothesis via a rotation sweep (44
orientations of a single isolated hand, sampling for spatial speckle
signatures at each) -- found one genuinely anomalous angle (Y=180, viewing
the back of the hand: 25 islands from only 1167 shadow pixels, vs. ~5
islands from 2-4x as many pixels at other angles), but the user
interjected mid-investigation: "its not tied to hand rotation or position"
-- correctly heading off a 3rd wrong turn before it was fully chased.

Asked the user directly what they observed. Answer was the decisive clue:
"The noise is only present in areas where the hand is overlapping itself.
So I can somewhat see through the hand itself and the noise represents
the obscured surfaces behind the frontmost surface" -- plus "ive used many
hand poses in Hando" (and it never shows this, across any of them). That
is the textbook signature of `depthTest:false` self-occlusion bleed-
through -- the same mechanism the FIRST correction entry (11:08-11:15 AM)
had already tried and dismissed, but via a coarse whole-canvas island-
count metric that diluted the signal across all 510 hands and happened to
sample a region with no actual self-overlap.

Redid the test properly: isolated a single hand in its bind pose (fingers
naturally curl over the palm -- a real self-overlap), and did a per-pixel
diff of that exact region between `depthTest:true` and `depthTest:false`.
Result: 8.2% of pixels differ, spatially clustered precisely on the
overlap, with `depthTest:false` showing bright/lit (255) where
`depthTest:true` correctly shows shadowed (132) -- confirming a wrongly-
drawn, differently-lit back surface bleeds through the front one when
depth testing is off. This is what the user was describing.

Fix (approved by the user before implementing, since it changes the
render-order architecture): restored `depthTest:true` (the default) on
both hand materials (`createToonMaterial()`, `ensureOutlineMaterial()`).
To keep cross-hand cursor-distance stacking working without real depth
testing between hands, gave each hand's own fill mesh AND outline mesh an
`onBeforeRender = (renderer) => renderer.clearDepth()` hook (added where
each hand instance is built, in `rebuildField()`) -- this wipes the depth
buffer immediately before that hand's own draw call, so: within a hand,
triangles depth-test correctly against each other (self-occlusion fixed);
between hands, whichever one draws LATER (per `renderOrder`, unchanged --
still set every frame from cursor distance) always wins the overlap,
since it always starts from a freshly-cleared depth buffer. Attached to
BOTH meshes (not just fill) because the outline mesh is sometimes
invisible and `onBeforeRender` never fires for an invisible object.

Verified live, directly:
- The same finger-over-palm region that showed the 8.2% bleed-through
  split now reads a single clean luminance value (132) across every
  sampled pixel in that region -- self-occlusion confirmed fixed.
- Positioned 2 isolated hands at the identical screen position but 60
  world units apart in real camera depth; gave the PHYSICALLY FARTHER one
  the HIGHER `renderOrder` and (to remove any ambiguity from the shared
  toon material's own custom tint-uniform machinery, which made an
  earlier attempt at this same test misleadingly inconclusive)
  temporarily swapped it to a plain `MeshBasicMaterial` red tint --
  confirmed the farther-but-later-drawn hand visually wins 100% of 25
  sampled overlap pixels despite being physically behind, proving cross-
  hand render-order stacking survived the change.
- 0 console errors on a fresh reload; `renderOrder` still correlates
  perfectly with cursor distance across all 510 hands (0 inversions
  across 509 sorted pairs); full-field render unchanged in overall
  appearance from baseline.
- Screenshot verification was not possible this round (the Browser pane's
  own screenshot pipeline timed out repeatedly, a known recurring
  environment quirk) -- all verification above was done via direct WebGL
  `readPixels()` pixel/histogram/diff analysis instead, which is more
  precise than a visual screenshot for this specific class of bug anyway.

Corrected `CODE_SUMMARY.txt`'s GOTCHAS section in 3 places: the noise
entry itself (now describes the real cause and fix, not either of the 2
disproven theories), and 2 architecture-description entries (#7, #9, and
a 3rd standalone entry) that described the old `depthTest:false`
architecture as current.

One known residual trade-off, disclosed to the user as part of proposing
this fix: if 2 hands' `renderOrder` values land extremely close together
(the same near-tie case "Prevent Reordering Flash" already exists to
soften), their draw sequence could in principle interleave in a way that
briefly disrupts the per-hand depth-clear grouping -- a minor, transient
edge case in the same spirit as the already-documented reordering-flash
residual pop, not separately reproduced or treated as a new bug class.

Not committed/pushed yet.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~11:44 AM - 11:45 AM EDT

"push" -- committed and pushed everything accumulated since the last push
(`6d6ea29`): the depthTest self-occlusion fix above, the row/column-
spacing-vs-hand-scale decoupling, the "Prevent Reordering Flash" smoothing
rework, and the bounding-sphere overlap-check fix, plus their 3 doc files.
Checked `git status`/`log`/`diff` first to confirm no concurrent session's
changes were mixed into the working tree (clean; already up to date with
origin, no merge needed). Single commit `0eb1f9b`, pushed to `main`
cleanly, no conflicts.

User confirmed the fix live immediately after: **"ok noise is gone now."**
Closes out the toon-shading-noise investigation (3 rounds, 2 wrong
diagnoses disproven by the user along the way, documented above) with a
real user-side visual confirmation, not just this session's own pixel-
level measurements.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~11:56 AM - 12:24 PM EDT

"Add the pose settings from Hando. For now, the pose settings apply to
every hand. Be sure to provide the Hide Wrist (%) setting input. The main
thing is that a wrist that is cut will be shifted such that the cut wrist
base should still correspond to the Field Layout points."

Ported HANDO's own Pose dev-panel group (identical rig/bone names, same
`Hand2.glb` asset): 25 finger sliders (Curl/Splay/2nd-Segment-Splay/
Curl-Bias/Tip-Twist x5 fingers), Wrist Bend/Splay, Whole-Hand Rotation
X/Y/Z, and Hide Wrist -- applied identically to every hand (a shared
pose, per the user's own "for now" scoping; no per-hand override yet).
Explicitly did NOT port HANDO's own Saved Poses list-picker / pose-tween
system, since the request was about the pose CONTROLS, not the preset-
management layer on top of them -- flagged as a scope boundary rather
than silently omitted.

Added:
- New "Pose" dev-panel group (between Cursor Tracking and Camera), 31
  controls total, defaults copied verbatim from HANDO's own.
- `boneRestQuat` -- captured once, right after load, from the ORIGINAL
  un-cloned skeleton's bind pose, BEFORE `rebuildField()` ever clones it.
  Shared across every hand's own distinct skeleton clone (simpler than
  HANDO's own per-model `restQuatMap`, since this project clones ONE GLB
  N times rather than supporting 2 different model files).
- `applyCurlToSkeleton()`/`applyCurl()`, `applyWristPoseToSkeleton()`/
  `applyWristPose()` -- ported near-verbatim from HANDO's own identical
  functions (including all of its live-measured per-finger axis/sign/
  magnitude tables: `FINGER_CURL_AXIS`, `FINGER_SPLAY_AXIS`,
  `FINGER_SPLAY2_AXIS`, `FINGER_MAX_DEG`, etc.), adapted to loop over
  every hand in `hands` (each with its own skeleton) instead of HANDO's
  1-2 `posableModels`.
- `updateClonePoseTransform()` -- new, this project's own (no HANDO
  equivalent needed one): computes `cloneBaseQuat` (`alignQuat` composed
  with the live Whole-Hand Rotation sliders) and each hand's
  `clone.position`, both identical across every hand, applied once per
  slider change (not per frame -- `wrapper`'s own per-frame cursor-
  tracking rotation is untouched by any of this).
- `updateWristClipPlaneForHand()` + a shared `wristClipPlane` -- Hide
  Wrist's own GPU clipping plane, ported from HANDO's own `wristClipPlane`
  but recomputed PER-HAND (via `onBeforeRender`, the same mechanism
  already used for the depth-clear technique) rather than once globally,
  since every hand here faces a different direction. `material.
  clippingPlanes` (requiring `renderer.localClippingEnabled = true`) was
  used instead of HANDO's own `renderer.clippingPlanes` specifically so
  the clip never affects the cursor target marker/grid helper.

Fixed 3 real bugs this round, all found via direct empirical live
testing (not caught by code review alone):

1. **Hide Wrist + Whole-Hand Rotation combined drifted off the grid
   point.** First implementation ported HANDO's own palm-center rotation
   pivot math (generalized with an extra baseline term for Hide Wrist) --
   passed EVERY isolated test (Hide Wrist alone: exact; Whole-Hand
   Rotation alone: exact) but a COMBINED test (Hide Wrist 30% + Whole-Hand
   Rotation Y 90°) measured a real 0.27-world-unit drift off the grid
   point. Root cause: preserving 2 different fixed points (palm center
   AND the cut point) under one rotation is only possible if the rotation
   axis happens to pass through both, which a general slider combination
   won't. Resolved in grid-alignment's favor (the user's own explicit
   "main thing") by dropping the palm-pivot preservation entirely and
   solving directly for whatever position keeps the CURRENT cut point at
   the grid point for the CURRENT rotation -- a real, disclosed behavior
   difference from HANDO (Whole-Hand Rotation now pivots around the cut
   point, not the palm center, even at Hide Wrist=0%).
2. **A direction+distance pair silently isn't an absolute position.** The
   same first attempt stored `armDownDirRaw`/`armToWristDistRaw` (a unit
   direction and a scalar) and used `direction*distance*t` as if it were
   the cut point's own absolute raw-frame position -- it's a displacement,
   not a position, and treating it as one implicitly (and wrongly)
   assumed the wrist bone sat at the raw content-space origin. It looked
   correct in ISOLATED testing (masked by the fact that the actual origin-
   coincident bone is the FOREARM, not the wrist, confirmed via a live
   measurement with the whole ancestor chain zeroed out: `forearmPosRaw ≈
   (0,0,0)`, `wristPosRaw ≈ (0, 14.63, 0)`) but broke once rotation was
   combined in (bug #1 above). Fixed by storing the actual ABSOLUTE raw
   positions of both bones (`wristPosRaw`, `forearmPosRaw`) and computing
   the cut point as `forearmPosRaw.lerp(wristPosRaw, hideT)` directly.
3. **Finger curl axes leaked each hand's own current cursor-facing
   direction into what should be a purely anatomical rotation.** HANDO's
   own `applyCurlToSkeleton()` pre-rotates its fixed WORLD_X/Y/Z curl
   axes by `modelRoot.quaternion` (its single whole-hand orientation)
   before converting via the bone's full world quaternion -- correct for
   HANDO, which has no OTHER rotation layer. This project's hands have an
   extra one: `wrapper.quaternion`, the per-hand, per-frame cursor-
   tracking look-at. Confirmed directly, live: 2 different hands, facing
   2 different directions at that moment, produced 2 DIFFERENT local bone
   quaternions for the IDENTICAL slider values on the identical finger --
   proof the ported axis math wasn't actually anatomically fixed the way
   it is for HANDO. Fixed by extending `rotateOnTrueWorldAxis()` with an
   optional `excludeQuat` parameter (that hand's own `wrapper.quaternion`),
   factored out of the bone's world quaternion before the axis conversion
   -- the quaternion algebra shows this exactly cancels the existing
   pre-rotation by `cloneBaseQuat`, leaving the axis fixed relative to the
   mesh's own raw bind-pose frame regardless of either rotation. Does NOT
   apply to Tip Twist's own axis (computed live from 2 bones' actual
   current world positions -- already correct, excluding anything from it
   would be wrong).

Verified directly, live, after each fix (not just code-reviewed):
- Grid-alignment: isolated Hide Wrist (60%, 30%), Hide Wrist + single-axis
  Whole-Hand Rotation, Hide Wrist + all 3 rotation axes combined, across
  multiple hands (index 0/50/200/300/400) at different grid positions and
  different live cursor-tracking orientations -- every case measured
  within floating-point precision (~1e-14 to exactly 0) of the grid point.
- Finger-curl anatomical consistency: after the fix, 5 hands (indices 0,
  50, 150, 300, 450) with genuinely different live wrapper orientations
  produced byte-identical local bone quaternions for the same slider
  values (curlIndex=150, splayIndex=80) -- confirmed via direct quaternion
  comparison, not visual inspection.
- A full combined stress test (Hide Wrist 40%, Whole-Hand Rotation Y 60°,
  Curl Index 150%, Wrist Bend -30° all active at once) still measured
  exact grid-alignment (~1e-16 to ~1e-17).
- 0 console errors across every reload this round; a live screenshot
  confirmed the field renders the new default pose (asymmetric per-finger
  curl, matching HANDO's own defaults) with no visual regressions.

Corrected `CODE_SUMMARY.txt` (new architecture entry #11 for Pose, plus 4
new GOTCHAS entries documenting the 3 bugs above and the fix) and
`PROJECT_PROGRESS.md`. Took the opportunity to consolidate
`PROJECT_PROGRESS.md`'s own "Recently completed" section (which had grown
into exactly the session-by-session historical narrative its own header
warns against) into a shorter current-state summary -- the full detail
this consolidation replaced remains permanently in this file's own prior
entries, nothing was lost, only de-duplicated from the live-document copy.

Not yet committed/pushed.

--------------------------------------------------------------------------------
2026-09-12 (continued once more)  -  ~2:17 PM - 2:18 PM EDT

"push" -- the Pose port above (commit `25d47ce`) was committed and pushed
in this same continued session. Correction to that entry's own "Not yet
committed/pushed" line, recorded here rather than edited in place.

--------------------------------------------------------------------------------
2026-09-13  -  ~2:40 AM - 4:04 AM EDT

User tested the pushed Pose/Hide Wrist build and reported 2 problems: "1.
theyre not cropping correctly when the app is actually following the
cursor. 2. When cropped, the hand should be placed in the grid from the
crop point, not the original base of the 3d model." Extensive
re-verification of the pushed code (both on `localhost` and, once the
user shared it, the actual deployed `handy-dandies.vercel.app` URL with
`?dev=1`, live mouse movement, and the exact same measurement approach
already proven earlier) found the code correct in every test -- grid-
alignment exact, clipping genuinely removing geometry, `armToWristDist`
identical across hands. Confirmed the deployment WAS serving the latest
code (`main.js?v=19`, matching `25d47ce`), ruling out a stale-cache
explanation. Asked the user for a screenshot/recording rather than keep
guessing blindly.

User provided a screen recording (saved to
`data/SCREENCAP/Recording 2026-09-13 032926.mp4`). Extracted 10 frames
directly via a local OpenCV script (no `ffmpeg` available in this
environment) rather than trying to infer the issue from description alone
-- immediately visible: hands in different parts of the same frame showed
dramatically different amounts of visible forearm, some looking almost
fully uncropped. Root-caused by direct measurement, not further guessing:
sampled 11 hands scattered around the field at that exact moment --
`armToWristDist` (the TRUE 3D length of the remaining segment) was
`14.630384832518` world units for every single one (differing only in the
15th decimal place), and grid-alignment held exactly for all of them too,
including the specific hands that looked least cropped. What varied was
the SCREEN-PROJECTED length of that same fixed 3D segment -- 17px to 56px,
over 3x -- purely because each hand independently tracks the cursor and
so points in a different 3D direction relative to the camera at any given
moment; a hand facing more edge-on to the camera shows the segment near
its true length, one facing more toward/away from the camera shows it
heavily foreshortened. **Not a bug** -- reported the exact measurements to
the user rather than a vague "looks fine to me," and explained plainly
that "cropped equally at all times" as a literal on-screen appearance
isn't achievable with a fixed-fraction 3D clip on independently-rotating
objects.

User's actual follow-up request, once this was clear: stop thinking of it
as a crop and think of it as arm length instead -- both short and long
variants still spawn from the same grid plane, but the amount should react
to live distance from the cursor (closer = shorter arm), with 4 new
controls specified via this project's own `*D*` shorthand:
- **Min / Max Arm Length** -- "a slider that can take multiple toggles,
  similar to the color gradient slider in DickoClicko. Left side is 0 and
  right side is 1. 1 is fully cropped, 0 is none." DICKOCLICKO's own
  `type:'gradient'` control (a Photoshop-style draggable bar,
  `cfg[key]` = `[{pos,color}]`) lives in ITS OWN monolithic index.html, not
  the shared `devpanel.js` engine this project reuses verbatim from HANDO
  -- ported the UI PATTERN (a draggable bar, values persisted through the
  existing control machinery), not the code, since this needs exactly 2
  numeric handles (min/max crop %), no color picker.
- **Length Scaling Curve** -- "one of those input ui things where its like
  a 2 axes graph and theres a curve showing the relationship between
  distance and length, and i can manipulate the curve." No prior art for
  this in the workspace -- built fresh: a small SVG plot, X=normalized
  cursor distance (0-1), Y=raw crop fraction (0-1), draggable points
  connected by straight segments (piecewise-linear, not a smoothed spline
  -- a deliberate simplicity tradeoff, flagged to the user), click empty
  space to add a point, double-click to remove (floored at 2, the 2
  domain-spanning endpoints always present, only their Y draggable).
- **Reactive Arm Length** checkbox, and **Default Arm Length** (the
  original Hide Wrist slider, relabeled, used when Reactive is off) --
  both straightforward.

Neither new widget is a `devpanel.js` control type -- per this project's
own explicit convention ("do not fork it, add controls via main.js's own
DEV_GROUPS instead"), both are built as custom DOM/SVG laid directly over
their own control's existing (hidden) generic `type:'text'` input, driven
via a `commitTextControl()` helper that sets `.value` and dispatches a
real `'input'` event -- routing through devPanel.js's own existing
`commit()`/localStorage-save path exactly as if the user had typed into a
plain text box, so Copy/Save/Reset all keep working without either widget
knowing anything about devPanel.js internals.

Architecture change: arm-length compensation (`applyHandArmLength()`,
the direct descendant of the prior round's `updateClonePoseTransform()`)
moved from a ONE-TIME, SHARED, slider-triggered calculation to PER-HAND,
PER-FRAME -- necessary since Reactive mode means every hand can have a
genuinely different crop value that changes continuously as the cursor
moves. Reuses the live cursor-distance value `updateRenderOrder()` already
computes for render ordering (no new per-frame distance calculation
needed) rather than adding a redundant one. `cloneBaseQuat` (Whole-Hand
Rotation's own shared quaternion) stays event-driven, computed only when
those sliders change (renamed to `updateCloneBaseQuat()`, position-only
logic split out of it).

Hit and fixed 1 real bug during implementation, caught immediately on the
first live reload rather than shipped: a TDZ crash
(`ReferenceError: Cannot access 'armLengthWidgetResyncs' before
initialization`) from declaring 3 new cached-state variables
(`armLengthRangeParsed`, `armLengthCurveParsed`, `armLengthWidgetResyncs`)
next to the functions that use them (well after `initDevPanel()`) while
calling 2 of those functions immediately after `initDevPanel()` -- before
the module's own top-to-bottom evaluation ever reached those
declarations. Same crash class as this project's own documented
`initDevPanel()`-detach-onChange gotcha. Fixed by moving all 3 to this
file's own early shared-state block instead.

Also added a small per-frame resync poll (`armLengthWidgetResyncs`,
checked once per frame from `animate()`) so either widget's own displayed
state stays in sync if devPanel.js's Reset/Copy-restore path ever writes
a new value into the underlying hidden input directly (which it does,
without firing an `'input'` event) -- couldn't directly verify this ONE
mechanism firing live in this session's own test environment, since a
pre-existing, already-documented tool quirk suspends
`requestAnimationFrame` entirely whenever the Browser pane isn't
displayed (confirmed directly with a vanilla, page-independent rAF
counter: 0 ticks in 500ms even with `document.hidden` reporting false) --
its logic is code-reviewed and pattern-matches the already-verified
drag-commit path, but flagged as worth a real on-device check rather than
claimed as fully verified.

Verified directly, live, working around that same rAF-suspension quirk by
manually invoking `updateRenderOrder()` via the debug hook wherever a real
animation frame couldn't be relied on to have run naturally:
- Dragging the Min/Max range handles and the curve's points both update
  `cfg.armLengthRange`/`cfg.armLengthCurve` correctly (confirmed via
  direct pointer-event simulation against the real DOM elements, not just
  code reading).
- With Reactive on and the cursor moved: the closest-to-cursor hand
  (distance 111.08) measured crop T=0.660; the farthest (distance 216.49)
  measured T=0.432 -- confirming "closer = shorter arm" (higher crop)
  exactly as requested.
- Grid-alignment held within floating-point precision of exactly 0 (2.7e-15
  and 6.2e-17) for BOTH of those hands despite their genuinely different,
  continuously-varying crop values -- the "same plane" requirement
  survives Reactive mode intact.
- 0 console errors on live reload after the TDZ fix; a screenshot with
  Reactive on and the cursor positioned near one corner of the field
  showed the expected visual gradient (shorter arms near the cursor,
  longer arms farther away).

Corrected `CODE_SUMMARY.txt` (new architecture entry #12 for Reactive Arm
Length, a correction to the prior entry's `updateClonePoseTransform()`
references now that it's split into `updateCloneBaseQuat()` +
`applyHandArmLength()`, and 3 new GOTCHAS entries: the foreshortening
finding, the TDZ crash, and re-confirmation that grid-alignment survives
the Reactive round) and `PROJECT_PROGRESS.md`.

--------------------------------------------------------------------------------
2026-09-13 (continued once more)  -  ~4:17 AM - 4:18 AM EDT

"push always push" -- user made this a standing instruction for this
project going forward (saved to memory) rather than a one-off answer.
Committed and pushed the Reactive Arm Length round above as commit
`6a468d0`. Deliberately left a new untracked `data/SCREENCAP/` directory
(a 12.8MB user-provided debugging screen recording) OUT of the commit --
not part of the feature, and a large binary video isn't something to
commit without being asked.

--------------------------------------------------------------------------------
2026-09-13 (continued once more)  -  ~7:31 PM - 8:03 PM EDT

4 bug reports plus 2 new feature requests, arriving together: "1. Dev
Panel minimize doesnt work. The settings dissappear, but the full panel
is still there. 2. *D* Crop Wrist Checkbox. To turn the cropping on and
off. 3. Wrist cropping min max doesnt seem to work. I set the default
and max at .5 or less but i still see full arms. 4. Why is it that
sometimes when i place the cursor far away, the hands furthest from the
cursor start dissappearing, even though i have a min length of more than
0." Then, mid-turn: a mouse-tracking log request, and (from a reference
image) a correction that the Length Scaling Curve should be a smooth
curve, not angled line segments.

Investigated items 3/4 first via 2 more user-provided screen recordings
(`data/SCREENCAP/Recording 2026-09-13 193115.mp4` and
`...193218.mp4`), extracted to frames directly via the same local OpenCV
approach as the prior recording -- visually confirmed a real, dramatic
disparity in visible arm length across the field at a single moment,
worth root-causing rather than guessing at.

1. **Dev Panel minimize bug** -- root-caused directly in `devpanel.js`
   (the shared, reused-verbatim engine, not this project's own code):
   `.dp-collapsed`'s CSS only hides `.dp-body`/`.dp-resize`; it can't
   shrink the panel itself, because `panel.style.height` is an explicit
   inline px value (for resize-drag persistence) that always wins over a
   stylesheet rule, AND `.dp-panel`'s own `min-height: 140px` applies
   unconditionally too -- confirmed live in 2 steps: fixing `height`
   alone still left the box floored at 140px (measured
   `getBoundingClientRect().height` directly), only overriding BOTH
   `height` and `minHeight` on collapse (and restoring both on expand)
   actually shrunk it to the header's own height (confirmed: 36px
   collapsed vs. 677px expanded).
2. **Crop Wrist checkbox** -- new `cropWristEnabled` master on/off,
   gating the entire Arm Length system (`computeArmLengthT()` returns 0
   directly when off, before even checking Reactive/Default). Verified
   live: off + a high Default Arm Length value still produced crop 0.
3. **The ".5 still shows full arms" report was a real, confirmed unit-
   scale mismatch, not a code bug.** Typed "0.5" into the live Default
   Arm Length control and confirmed directly: `cfg.hideWrist` became
   exactly `0.5` (0.5% crop), not `50`. `hideWrist` is a plain 0-100
   slider matching every other percent control in this project -- 0.5 IS
   a valid point on that scale, just not the one the user meant. Root
   cause of the CONFUSION (not the value itself): the Length Scaling
   Curve widget's own axis captions read "(0-1)" at the time, which
   likely primed the "type a fraction" instinct for this OTHER,
   differently-scaled control. Fixed by relabeling the curve's own
   captions to read in percent too ("Distance From Cursor (%)... Crop
   (0%=None, 100%=Full)"), so every control in this feature speaks the
   same units -- not by changing any control's actual scale.
4. **The far-cursor "disappearing" report was a real, confirmed bug in
   the Reactive normalization math.** `computeArmLengthT()` used to
   normalize each hand's cursor distance against a FIXED reference
   (`sceneState.fieldRadius * ARM_LENGTH_DISTANCE_NORM`) -- measured
   directly: with the cursor placed far outside the whole field (2000,
   2000), BOTH the nearest hand (distance 2654) and the farthest hand
   (distance 3003) in the field landed on the IDENTICAL crop value
   (0.2, the Min bound) -- once every hand's distance exceeds the fixed
   reference, all relative variation collapses to the same extreme.
   Fixed by adding a cheap pre-pass in `updateRenderOrder()` that finds
   the CURRENT field's own live min/max distance-to-cursor each frame,
   normalizing against THAT range instead of a fixed constant. Re-
   verified with the cursor placed even farther away (3000, 3000,
   distances ~4000-4400): nearest hand measured crop 0.9 (Max bound),
   farthest measured 0.3 (Min bound) -- the full gradient held instead
   of collapsing.

Also shipped, from the mid-turn requests:
- **Mouse Tracking Log** (Debug group): a click log describing what each
  click actually triggers in this app (Dev Panel interaction, or Camera
  Pan/Dolly via OrbitControls, based on `e.target`/button -- an honest
  description of this app's real click semantics, not a placeholder),
  a regular interval-driven cursor-position log, an interval slider
  (100-5000ms), and an on/off checkbox for the regular log specifically
  (the click log always runs). A plain scrollable `<pre>` appended
  directly to the Debug group's own DOM, session-only (not persisted --
  nothing meaningful to restore in a log). Verified live: dispatched
  clicks with different buttons/targets and confirmed each produced the
  correct trigger description; confirmed the interval timer fires
  correctly at a shortened 100ms test interval.
- **Length Scaling Curve is now a real smooth spline** (Catmull-Rom,
  C1-continuous, passes exactly through every point), per direct
  correction with a Photoshop/Lightroom tone-curve-editor reference
  image ("ignore the colors in that image"). The SVG widget draws the
  SAME `evaluateArmLengthCurve()` function the real per-hand math calls,
  fine-sampled at 48 points -- not a separately-approximated visual.
  Verified live: added a 3rd point and confirmed the sampled path's own
  rate-of-change varied smoothly through it (no linear slope break),
  confirming genuine curvature.

2 more small bugs caught and fixed live before shipping, neither
originally reported:
- Dragging either new widget while its own Pose group was COLLAPSED
  corrupted the stored value to `{"min":null,"max":null}` --
  `getBoundingClientRect()` on a hidden element returns an all-zero
  rect, dividing by 0 producing `NaN`, which `JSON.stringify` silently
  turns into `null`. Fixed with a zero-rect guard in both widgets' own
  drag handlers.
- The new click handler's `e.target.closest('.dp-panel')` threw when
  `e.target` wasn't a real Element (confirmed via a synthetic test event
  dispatched directly on `document`) -- fixed with optional chaining.

**A concurrent session was found actively editing this SAME project's
`main.js`/`devpanel.js` while this round's own work was in progress --
confirmed via direct diff inspection, not assumed, the moment an
unexpected "Saved Poses"/"Pose Preview" UI showed up in a live test that
this session never added.** This is the HANDO-side session the user
separately asked to "deal with" cross-project pose import (see this
project's own earlier mid-turn note: "actually forget about the pose
import thing. the chat from Hando will deal with that"). Their changes
add a Saved-Poses-import list-picker and a standalone Pose Preview
mini-viewer, reusing this round's own `applyCurlToSkeleton()`/
`applyWristPoseToSkeleton()` via a new, backward-compatible `values =
cfg` parameter.

`devpanel.js`'s own changes were cleanly separable: the collapse-button
fix (item 1 above) touched one isolated location; their list-picker
export/import UI touched 3 other, non-overlapping locations. Extracted
the full diff, confirmed the collapse-fix hunk contained ONLY this
session's own lines, and staged JUST that hunk via `git apply --cached`
against a hand-constructed single-hunk patch file -- verified afterward
that the remaining unstaged diff still contained exactly their 3
untouched hunks.

`main.js` was NOT safely separable the same way: their Pose Preview
feature's own changes are interleaved with this round's own edits even
WITHIN shared function signatures (e.g. `applyCurlToSkeleton(fingerName,
skeleton, baseQuat, wrapperQuat, values = cfg)` -- their own addition,
backward-compatible with every existing call site, needed to pose their
separate preview hand from a saved item's own values without touching
the live field or `cfg`). Attempting to rip out "their part" of these
shared-signature hunks would have risked breaking their feature for a
merge that the live, tested app didn't actually need (the combined file
loaded and ran correctly, 510 hands, 0 console errors, every one of this
round's own 6 items verified working, throughout testing). Committed
`main.js` as one combined commit covering both sessions' work, with
explicit, honest attribution in the commit message and in
`CODE_SUMMARY.txt` -- not silently claimed as this session's own work,
and not held back entirely (which would have also withheld this round's
own fixes).

Corrected `CODE_SUMMARY.txt` (6 new GOTCHAS entries: the devpanel.js
collapse bug, the widget zero-rect bug, the unit-mismatch finding, the
normalization bug, the smooth-curve change, the click-handler defensive
fix, and a 7th documenting the concurrent-session situation and how it
was handled) and `PROJECT_PROGRESS.md`.

--------------------------------------------------------------------------------
2026-09-13  -  07:41 PM - 08:07 PM [local]

Direct request (cross-project with HANDO): "I want to be able to set poses
in our app, save them, then somehow export selected poses into Handy
Dandies to use there... I don't want import to overwrite ALL poses saved
in Handy Dandies... In Handy Dandies, also provide the poses list in the
dev panel with the Save, Overwrite, Use and Delete function. But... when i
click Use, it doesnt apply it to all the hands on screen. Instead, provide
me a collapsible pose viewer within the dev panel itself."

Added a Saved Poses list-picker to the Pose group (Save/Overwrite/Use/
Delete/Rename/+Group, via the shared devpanel.js list-picker engine --
already in sync with HANDO's own copy, including this session's earlier
Overwrite/groups/nesting work, so no engine changes were needed on this
side beyond the new `ctrl.importable` flag, added to BOTH projects'
devpanel.js in the same pass). "Use" does NOT repose the field -- it
previews the pose in a brand-new "Pose Preview" collapsible group: its
own independent Three.js scene/camera/renderer/OrbitControls with one
more clone of the already-loaded GLB (no extra fetch), posed from the
saved item's own values via a `values` param added to
applyCurlToSkeleton()/applyWristPoseToSkeleton() (defaults to `cfg`, so
every existing call site is unaffected). Verified live via bone-quaternion
comparison: Use changes ONLY the preview hand's skeleton -- the main
field's own hands and `cfg` itself are provably untouched.

Import (paste JSON from HANDO's own new "Export Selected" clipboard
button, same devpanel.js change) merges by name -- overwrites an
existing same-named pose, adds anything new, never wipes the rest.
Deliberately excluded the Crop Wrist / Arm Length family from what a
saved pose captures -- that system is reactive/cursor-driven staging, not
finger/wrist articulation, and doesn't fit "a pose" the way HANDO's own
static Hide Wrist crop does.

Fixed 2 real issues found via live testing, not just code review: (1) the
Pose Preview camera aimed at the raw origin instead of the mesh's own
measured bounding-sphere center -- this asset's visible mesh includes a
full forearm hanging below the wrist, so the origin sat well away from
the actual hand, framing mostly empty space/forearm; (2) the preview's
own from-scratch lights produced a flat, washed-out white silhouette with
no visible toon-shading steps -- replaced with clones of the main scene's
own already-tuned keyLight/hemiLight (a DirectionalLight's effect depends
only on position-to-target DIRECTION, not magnitude, so their exact
vectors transfer correctly despite being scaled for the much larger main
scene). OrbitControls interaction on the preview canvas couldn't be
verified end-to-end via this environment's synthetic pointer events --
confirmed this is a sandbox limitation, not a bug, by finding the actual
thrown error came from INSIDE OrbitControls' own pointerdown handler
(`setPointerCapture` rejecting a synthetic, non-OS pointerId) -- proof its
event listeners are correctly attached and firing; a real mouse/touch
supplies a real pointerId and isn't affected.

Actual: 26m7s (MEASURED: 2026-09-13 19:41:23-20:07:30 EDT).

--------------------------------------------------------------------------------
2026-09-13 (continued once more)  -  ~8:13 PM - 8:19 PM EDT

2 direct follow-ons to the Mouse Tracking Log: "provide a copy button for
the mouse log," then mid-turn, "add it so that if i click a settings in
the dev panel, it logs what i clicked and what setingg i had set."

Added a **Copy** button next to the log's own label, using the exact same
`navigator.clipboard.writeText` + text-flash pattern devPanel.js's own
"Copy Settings" button already uses -- no new transport needed. Verified
live: click fires the handler and flashes "Copied!"/"Copy failed"
correctly (this session's own automated click produced "Copy failed",
almost certainly a sandboxed-testing limitation -- clipboard writes need
a genuine user gesture -- not a bug in new code, since it's byte-identical
to the already-proven native Copy Settings pattern).

Added **settings-change logging** as ONE delegated 'input'/'change'
listener on the whole `.dp-panel`, not per-control wiring -- works for
every EXISTING control (including this round's own Arm Length widgets,
whose custom drag handlers dispatch a synthetic 'input' on their own
hidden text input, correctly caught by this same delegation) and any
FUTURE control added later, without touching a single existing onChange.
Reads `cfg[key]` from the row's own `data-key` -- confirmed this is
always already current by the time the delegated listener fires, since
event bubbling always runs the target's own listener (devPanel.js's own
`commit()`) before an ancestor's. A same-key/same-value dedup
(`lastLoggedByKey`) prevents a double-log when 'input' and 'change' both
fire for the identical committed value, without suppressing genuinely
different values during a slow slider drag (deliberate -- a debug log
should show the real sequence).

Verified live: dragging Default Arm Length to 42 then back to 0, and
toggling Show Grid Helper on then off, produced exactly 4 correctly-
labeled log entries with no duplicates
(`Setting changed: Default Arm Length (Crop %, Reactive Off) = 42`, etc.).

Before this round started, confirmed the concurrent HANDO-side session's
own work (commit `0634947`, "Add pose import... to the shared list-picker
engine") had already been committed and pushed, and this session's own
`main` was already in sync with `origin/main` -- a clean base, no further
concurrent-session coordination needed this round. Bumped the
`devpanel.js` import's own cache-busting query (`?v=3` -> `?v=4`) since
that file's content changed again (their own commit) without them
bumping it themselves.

Corrected `CODE_SUMMARY.txt` and `PROJECT_PROGRESS.md`.

--------------------------------------------------------------------------------
2026-09-13 (continued once more)  -  ~8:27 PM - 8:38 PM EDT

User reported, with a new screen recording (`Recording 2026-09-13
202658.mp4`) and its own click/setting/cursor-position log: "the things
you fixed didnt seem fixed in terms of the wrist cropping." Investigated
live via the debug hook rather than re-guessing at another fix.

Correlated the recording's own file timestamps (Modify 20:26:44.42,
36.27s duration -> start ~20:26:08) against the click log's wall-clock
timestamps to determine the recording's own timeline: Crop Wrist (Master
On/Off) was ON for roughly the first 21s of the recording, then the user
clicked it OFF (log: "Setting changed: Crop Wrist (Master On/Off) =
false" at 8:26:29 PM) for the remainder.

This environment's own documented `window.innerWidth`/`innerHeight === 0`
quirk (CLAUDE.md GOTCHAS) blocked the first attempt at live cursor-target
raycasting -- diagnosed directly (`rayDir` came back `[null,null,null]`
because the NDC computation divided by 0) and worked around by using
`resize_window` to set a real 1536x864 viewport, then manually replaying
this project's own `applyRendererSize()` logic (camera aspect/projection,
renderer/composer/outlinePass size) since the render loop's own self-heal
for this never got a frame to run in this same environment.

With that fixed, 2 direct measurements:
1. Crop Wrist OFF: confirmed via `hand.currentArmLengthT` that EVERY one
   of 510 hands reads exactly 0 (no crop applied, code path returns 0
   immediately when `cfg.cropWristEnabled` is false, as designed).
   Reproducing the video's own Crop-OFF cursor position (bottom-left,
   `~(60,855)`) and measuring the exact same `rForearmBend`->`rHand` bone
   segment used by the clip-plane math: every hand in the visible column
   shares an IDENTICAL true 3D length (14.63 units -- the same figure
   measured in the original foreshortening investigation), yet that
   segment's own on-screen projected length still ranged ~11-27px within
   just a 20-hand sample (and further down the same column in the full
   video). Pure perspective/orientation foreshortening, not cropping --
   confirmed with T=0 for every one of them.
2. Crop Wrist ON + Reactive ON: cursor placed at a fixed screen position,
   confirmed the crop math itself is correctly monotonic by live cursor
   distance -- nearest-to-cursor hand measured T=0.9 (max crop, per the
   default 30-90 range and default linear curve), farthest measured
   T=0.3 (min crop), exactly as designed.

Conclusion, reported to the user rather than guessing at another patch:
the Arm Length/Crop feature's own logic is verified correct in both
states. What's still visible in the recording as "cropping isn't fixed"
is the SAME class of pure perspective/orientation foreshortening already
diagnosed and reported as not-a-bug earlier this project (see the
2026-09-13 03:43 AM entry) -- inherent to any hand rotating to face a
moving cursor under a perspective camera, present with or without any
cropping at all, and not something the Arm Length feature itself causes
or can fix. No code changed this round. Corrected `PROJECT_PROGRESS.md`
with the finding and the open architectural question (accept as
inherent vs. reduce camera perspective distortion, e.g. toward an
orthographic projection -- a real architectural change needing the
user's own approval per CLAUDE.md §0a) for the user to weigh in on.

Presented the finding directly to the user with 3 options (leave as-is /
move toward an orthographic camera / something else is still wrong).
**User chose to leave it as-is.** Recorded the decision in
`PROJECT_PROGRESS.md` -- no further action planned on this item.

--------------------------------------------------------------------------------
2026-09-13 (continued once more, correcting the round above)  -  ~9:09 PM - 9:55 PM EDT

Immediately after the round above shipped ("leave the foreshortening
as-is"), the user pushed back hard: "but bro. dont you see in the video
arms dissappearing completely?" Re-examined the same recording's actual
pixels (not just the downsampled frame view) and found genuine 2-4px
fragments where a hand had been -- more severe than the "looks shorter"
framing the prior round's own AskUserQuestion had used. Re-opened the
decision with the user rather than assuming the prior "leave as-is"
answer still applied to this more severe picture.

First hypothesis (extreme edge-on orientation, same class as the
foreshortening finding, just more severe) did not survive contact with
the user's own counter-evidence: "no its not the dissappearng happens
when my mouse is very far from the grid. The furthest hands dissappear...
The furtherst hands should be pointing at the cursor. so they would not
be pointing at the camera" -- a correct, decisive rebuttal (a hand
rotating to face a genuinely distant point can't simultaneously be
edge-on to a camera pointed at the field's center).

Reproduced the exact video moment (8-10s and 14-16s, per the user's own
timestamps) via OpenCV frame extraction, then asked for and received the
user's own Copy Settings JSON dump (17x17 field, camera FOV 35 at
Z=352.46, `cropWristEnabled`/`reactiveArmLengthEnabled` both FALSE the
entire time -- a fact the mouse-tracking log had already shown, since
neither setting was ever toggled during the recording) to reproduce their
EXACT scene rather than guessing at settings. Loaded it into a fresh
browser's `localStorage` under `handyDandies.devSettings` (the exact key
`initDevPanel({storageKeyPrefix:'handyDandies'})` reads) and reloaded --
confirmed live that the reported cursor position (49,47) reproduced the
video's own narrow-column collapse exactly, with Crop Wrist definitively
OFF, ruling out the Arm Length feature entirely as a cause.

Diagnosed via direct pixel-readback (`gl.readPixels`) cross-referenced
against each hand's own verified world/screen position (confirmed correct
by temporarily rendering a single isolated hand in solid red and matching
its expected vs. actual screen position): only 2 of 289 hands actually
rendered anything at their own calculated position; the other 287 showed
pure background color there, even though their positions, rotations, and
render-order values were all individually confirmed correct. Ruled out,
in order, frustum culling (forcing `frustumCulled=false` on every mesh
changed nothing), the EffectComposer/OutlinePass pipeline (bypassing it
entirely with a direct `renderer.render()` call reproduced the identical
failure), and stale camera matrices (`updateMatrixWorld(true)` before
projecting changed nothing).

Root cause: `clippingPlanes` (for Hide Wrist/Arm Length) lives on the
MATERIAL, not the mesh, but every field hand's fill mesh AND outline mesh
shared ONE material instance with ONE shared `wristClipPlane`, mutated
in place per-hand via `onBeforeRender` right before that hand's own draw
call -- the original design's own comment asserted this "works correctly
even though the fill/outline materials are shared across all 510 hands,"
which this investigation disproves directly. Three.js does not re-resolve
a shared material's clipping-plane uniform per individual draw call the
way that assumed; with 289 hands sharing one material, almost every hand
ended up clipped against a stale/foreign plane (whichever hand's own
`onBeforeRender` last wrote to it before the GPU actually consumed the
uniform), discarding its entire mesh. Confirmed via a direct falsification
test: setting `material.clippingPlanes = []` on the shared material,
with nothing else changed, immediately took visible hands from 2/289 to
288/289. This explains every symptom reported across both this round and
the prior one -- happens with Crop Wrist on OR off (the clip-plane update
runs unconditionally for every hand regardless of `cropWristEnabled`);
happens specifically for far-cursor/farthest-hand cases only insofar as
whichever hand's plane "wins" the race for a given render happens to
discard geometry differently depending on scene state, not because of
any actual distance-based logic; and was never a foreshortening effect
at all, despite superficially resembling one in the recordings examined
so far (real foreshortening likely still contributes some visual
variation on top, per the earlier investigations, but is not the
"disappearing" mechanism).

**Fix implemented** (`main.js`): every field hand now gets its own
`.clone()` of the toon material AND the outline material inside
`rebuildField()`, each with `clippingPlanes: [hand.wristClipPlane]` -- a
genuine per-hand `THREE.Plane` instance stored on the hand object, not a
shared module-level one. `updateWristClipPlaneForHand()` now mutates
`hand.wristClipPlane` instead of a shared plane. Added
`toonMaterial.customProgramCacheKey = () => 'handToonMaterial'` so all
289+ clones (plus the template) still share ONE compiled WebGL program --
only the clip-plane uniform VALUES differ per clone, not the plane count
or shader structure, so without this three.js would otherwise recompile
the same shader hundreds of times. Added 2 helpers, `forEachToonMaterial()`
and `forEachOutlineMaterial()`, and rewired every existing "mutate the
shared material" control (Toon Base Tint, Show Wireframe, Outline Color,
Hull Outline Thickness, `rebuildGradientMap()`'s own toon-gradient
rebuild) through them, so every one of those dev-panel controls still
correctly updates all per-hand clones plus the template -- the template
itself stays directly assigned to the single Pose Preview hand, which has
no per-hand clipping conflict to begin with (only one instance uses it).
Corrected 2 stale comments that had asserted the shared-plane approach
worked (in `createToonMaterial()` and `updateWristClipPlaneForHand()`'s
own doc comments) and one unrelated pre-existing stale comment in
`CODE_SUMMARY.txt` claiming this project had "no clippingPlanes support"
(predates the Hide Wrist feature, never updated when it shipped).

Verified live: reproduced the user's own exact failing scenario (their
real settings dump, cursor at their own logged (49,47)) before and after
the fix -- 2/289 visible hands before, 276/289 after (the remaining 13
are plausible legitimate off-screen/dense-grid-overlap cases in a 17x17
field, not separately investigated). Re-confirmed the Arm Length feature
itself (Crop Wrist ON + Reactive ON) still produces the correct monotonic
crop gradient afterward (nearest T=0.9, farthest T=0.3, matching every
prior verification) -- the bug was entirely in the SHARED clip-plane
mechanism, never in the per-hand T-value math. Corrected `CODE_SUMMARY.txt`
(new GOTCHAS entry, and the #7 architecture-summary item) and
`PROJECT_PROGRESS.md` (removed the now-incorrect "leave as-is" foreshortening
resolution, replaced with this fix in Recently Completed). Cache-buster
bumped `index.html`'s `src/main.js?v=26` -> `?v=27`.

--------------------------------------------------------------------------------
2026-09-14  -  ~12:06 AM - 12:35 AM EDT

Direct user report, same day as the wrist-clip-plane fix: "Keylight color
no longer seems to work. When I set it to white, I still see color being
rendered on the hands. Though when I set it to red, they do turn red."
Investigated as a likely regression from that same fix's own
`customProgramCacheKey` addition, since it was the one change that
specifically affects how the toon material's `onBeforeCompile`
customization gets wired up across the newly-cloned per-hand materials.

First fix attempt (remove `customProgramCacheKey` entirely) did NOT
resolve it -- confirmed live by reading back a field hand's own actual
compiled fragment shader source (`gl.getShaderSource()` via
`renderer.properties.get(material).currentProgram.fragmentShader`)
before and after: neither contained the `toonLuma`/duotone-blend code
`createToonMaterial()`'s own `onBeforeCompile` is supposed to inject.
Tried forcing a GENUINELY unique cache key per material
(`material.uuid`) as a second attempt, specifically to rule out any
cross-material program-sharing explanation entirely -- still no effect,
proving the bug was never actually about program caching at all.

Real root cause, found by inspecting the clone's own `onBeforeCompile`
property directly: it read back as three.js's stock
`Material.prototype` no-op (`onBeforeCompile(){}`), not the custom
function assigned in `createToonMaterial()`. **`Material.clone()`/
`.copy()` does not carry over a custom `onBeforeCompile` override** --
it isn't part of the fixed property list three.js's own `.copy()`
implementation handles. Every `toonMaterial.clone()` added in the
previous round's wrist-clip-plane fix silently lost this customization
the moment it was cloned, reverting every field hand to plain,
unmodified texture-mapped `MeshToonMaterial` rendering: rim lighting and
the toon-tint/texture duotone blend gone entirely, while direct lighting
(Key Light Color) kept working normally since that's a separate,
standard three.js mechanism the clone bug never touched -- exactly
matching the reported symptom (white light doesn't neutralize an
already-colored texture; only the broken duotone override could have,
while red light still visibly tints whatever's there regardless).

**Fix implemented:** explicitly re-assign
`skinnedMesh.material.onBeforeCompile = toonMaterial.onBeforeCompile`
immediately after `.clone()` in `rebuildField()`. Also removed
`customProgramCacheKey` for good (confirmed not the cause, and not worth
reintroducing) -- three.js now compiles one WebGL program per hand's own
material, same cost it always would have without that speculative
optimization.

Verified live: sampling the same hand's own rendered pixel while
toggling Key Light Color between white and red now gives neutral gray
`(132,132,132)` and red-tinted `(132,0,0)` respectively, fully
reversible in both directions. Re-confirmed the wrist-clip-plane fix
from the round above still holds after this change: 274/289 hands
visible at the same test cursor position used in that round's own
verification (consistent with its 276/289 measurement, the small
difference plausibly ordinary render-order/overlap variation between
runs, not re-investigated separately). Confirmed via a genuinely fresh
browser tab that no console errors remain (the errors seen during the
investigation itself were from the 2 live, in-browser experimental
attempts, not from the final committed code).

Corrected `CODE_SUMMARY.txt` (new GOTCHAS entry immediately following
the wrist-clip-plane one, plus a correction to that entry's own now-
inaccurate `customProgramCacheKey` claim). Cache-buster bumped twice
during the investigation (`?v=27` -> `?v=28` for the first, ultimately
insufficient attempt; `?v=28` -> `?v=29` for the real fix) -- `?v=29` is
the version actually deployed.

--------------------------------------------------------------------------------
2026-09-14 (continued)  -  ~1:21 AM - 1:40 AM EDT

Direct user report: "when i open the app on desktop, the dev panel seems
to default to Landscape tab. It shouldnt." Root-caused in
`src/devpanel/devPanel.js` (the generic dev-panel engine, "reused
verbatim from HANDO" per this project's own CLAUDE.md file map -- see
Open questions below for the cross-project implication).

`realDeviceClass()` (the function that classifies the current viewport
as desktop/mobile/landscape) is called exactly once, synchronously, at
the very top of `initDevPanel()`, to set the panel's initially-active
tab (`editingDevice`). This project already has a DOCUMENTED gotcha that
`window.innerWidth`/`innerHeight` can read wrong at that exact early
script-parse moment (main.js's own renderer has a frame-by-frame self-
heal specifically because of this) -- but the dev panel's own tab-
selection had no equivalent correction: if the viewport misread once at
that moment, the wrong tab stayed "active" for the rest of the session,
since the existing `resize` event listener only re-syncs per-device
VALUES for a genuine later resize, never which tab is shown as active,
and doesn't fire at all if the viewport was simply wrong once at load
with no resize event following it.

Reproduced the exact mechanism live: this environment's own browser-
automation tool has its own separate, previously-documented quirk where
`window.innerWidth`/`innerHeight` read as 0 via its JS-exec query path
immediately after a fresh navigation (a tool characteristic, not a page
bug) -- used this to confirm a fresh load's `dp-tab-active` class was
briefly absent from every tab (consistent with `realDeviceClass()`
having computed something unexpected from `w=0,h=0` at the moment
`initDevPanel()` ran), before the page's own real dimensions became
available a few seconds later.

**Fix implemented:** added a short self-heal window (up to 30
`requestAnimationFrame` ticks, ~0.5s) right after the panel is built --
re-checks `realDeviceClass()` each frame and corrects `editingDevice`
(plus the visible active-tab styling) once if it differs, but stops
immediately the moment the user manually clicks a tab themselves (a new
`userSwitchedTab` flag), and stops after the frame budget regardless --
this is specifically a load-time correction, not a standing "tab always
follows window resize" behavior (which would break a legitimate
workflow: manually viewing Mobile settings, then resizing the desktop
browser window while doing so, should NOT snap back to Desktop).

Verified live: a fresh load on a genuine 1024x768 viewport correctly
shows the Desktop tab active; manually clicking Landscape afterward
switches and STAYS switched (checked again 600ms later, well past the
self-heal window) -- the fix doesn't fight a real user selection.
Bumped `devpanel.js`'s own cache-busting import query in `main.js`
(`?v=4` -> `?v=5`) and `index.html`'s (`?v=29` -> `?v=30`).

--------------------------------------------------------------------------------
2026-09-14 (continued)  -  ~1:55 AM - 2:07 AM EDT

Direct new feature request: "Every hand is oriented the same way more or
less (palm facing up)... I want to add a new checkbox. When Selected,
Every hand will always rotate to have the palm facing the cursor." Mid-
task, 2 direct corrections while investigating bone data: "when i say
rotate the hand. I dont mean use any of the rigs, I just meant rotate
the entire model" and "you shouldnt be doing any posing work" -- both
confirmed the approach already in progress (measuring bind-pose bone
POSITIONS only, as fixed reference vectors, exactly like the existing
`alignQuat` setup already does, never touching a bone's own live
rotation/curl) rather than requiring a course correction.

Investigated the existing cursor-tracking mechanism first: each hand's
WRAPPER (not the clone/skeleton) gets `lookAt()`-based rotation every
frame, aiming the `alignQuat`-aligned local -Z axis (the bind-pose
wrist-to-middle-fingertip direction) at the cursor. Needed an alternate
mode where the PALM-NORMAL direction, not the fingertip direction, is
what's aimed at the cursor -- without disturbing the existing lookAt
code's own shape, since it already works correctly for the default mode.

Measured the palm-plane normal the same way as `pointDir`/`alignQuat`
(bind-pose bone world positions at model load, identity-transform
frame): `(pinkyBase - wrist) x (indexBase - wrist)`, bones `rHand`/
`rIndex1`/`rPinky1`. Calibrated the cross-product SIGN live rather than
assuming it: transformed both candidate directions through `alignQuat`
and a real hand's own current `wrapper.quaternion` (a hand below the
cursor, default mode active) and checked which one measured facing AWAY
from the camera, matching this project's own already-confirmed reference
behavior from an earlier round ("hands under the cursor show the back of
their hand to the camera... this is correct"). `pinky x index` (that
exact order) was the outward palm normal.

Implemented as a single fixed correction quaternion composed onto the
EXISTING per-frame lookAt quaternion, not a parallel rotation system:
`palmFaceCorrectionQuat = Quaternion.setFromUnitVectors(palmNormalRaw
.applyQuaternion(alignQuat), (0,0,-1))`, computed once at model load
(same block as `alignQuat`). Given lookAt's own guarantee that its
quaternion maps local -Z to the cursor direction, and the correction
quaternion by construction maps the palm-normal direction to local -Z,
composing `desired.multiply(correctionQuat)` (three.js's own apply-
right-side-first order) redirects the SAME lookAt result so the palm
lands where the fingertip direction used to. Whole-wrapper rotation only
-- zero skeleton/pose/curl code touched, per the mid-task corrections.

Verified live, 2 ways before shipping: (1) mathematically --
independently re-derived the same correction via `skeleton.boneInverses`
(bind-pose extraction that works regardless of the skeleton's current
LIVE pose, unlike `getWorldPosition()` on a posed hand) and confirmed
`worldPalmNormal.dot(dirToCursor) = 0.999999995` for a hand below a
centered cursor -- correct to floating-point precision; (2) visually --
forced every hand's `wrapper.quaternion` directly (undamped) with the
feature on, cursor centered, and compared cropped/upscaled screen
regions above/below/left of the cursor: above vs. below hands showed a
clear vertical-flip relationship, left showed the hand's long axis
rotated to roughly horizontal -- matching the "roughly 180 above, roughly
90 to the side" pattern described in the request. Confirmed zero new
console errors in a fresh tab.

Added as `palmFacesCursor` (Cursor Tracking group, default OFF -- see
CODE_SUMMARY.txt's GOTCHAS for the full technical account, including the
quaternion-composition derivation and both verification passes in full).
Corrected 2 other CODE_SUMMARY.txt lines discovered stale while editing
this same section (architecture item #5/#6 still described the toon/
outline materials as "shared, not cloned," predating the wrist-clip-
plane fix earlier this session -- unrelated to this feature, fixed in
passing since directly adjacent). Cache-buster bumped `index.html`'s
`src/main.js?v=30` -> `?v=31`.

--------------------------------------------------------------------------------
2026-09-14 (continued)  -  ~2:17 AM - 2:34 AM EDT

Direct request: "look at the Dev Panel Template and implement any new
changes." Read `J:\CLAUDE\PROJECTS\.claude\TEMPLATE_DEV_PANEL.html`'s
own MAINTENANCE note and recent-dated comments first rather than diffing
the whole 3,654-line file against this project's 1,633-line
`devpanel.js` blindly -- found the template has grown substantially
since this project's own copy was made (structurally different too: the
template is a single self-contained HTML file, `devpanel.js` here
predates that consolidation as a standalone JS module). Checked the one
most concretely-described recent template change first (2026-09-14's
own "reordering starts only from the drag-handle icon" correction) --
confirmed live in `devpanel.js`'s own `setupReorder()` calls that this
project already does this correctly, nothing to port there.

Given the remaining delta was large (built-in "Dev Panel" chrome group
grown from ~16 to ~35+ controls; a new dynamic per-control Mobile/
Landscape visibility system; a whole-panel Named Setting States upgrade;
a Standard Text Settings battery), surfaced the scope to the user rather
than guessing how much to port, per this workspace's own §0a material-
change convention -- porting everything would mean a real re-
architecture of `devpanel.js`, not a quick sync. **User chose "Just the
Dev Panel chrome group."**

Read the template's own expanded group definition in full (its
`DEVPANEL_STYLE_CONTROLS`/`DEV_PANEL_STYLE_VAR_MAP` arrays) before
porting anything, rather than assuming every listed control belonged --
this caught that "Scroll Strength" is actually a DIFFERENT, unrelated
project's own scrollable-game-content wheel-speed multiplier that just
happened to live in the template's "Dev Panel" settings bucket, not a
genuine panel-chrome control; correctly excluded it (this project has no
equivalent scrollable content). Ported the other 24: per-category
(Title/Tab/Group/Settings/Button) Bold + Letter Spacing + Line Spacing,
Capitalize Title, Button Font Size/Height/Text Border, Setting Number
Font Size, and 4 new colors (Group/Button/Setting-Number/Tab Text
Color) -- added to `devpanel.js`'s own `builtInGroup` (16 -> 40
controls total), each wired to a new `--dp-*` CSS custom property in
`style.css` with a fallback matching this project's own current
hardcoded look, so the port is a genuine no-op until a control is
actually touched. Renamed the existing `toggleCaps()` helper to the
more accurate `toggleClass()` since it's now shared by both the caps
toggles and the new Bold toggles.

**Live verification immediately surfaced a real, independently
significant bug, unrelated to anything wrong in the port itself:** the
first test (a new color, a new Bold toggle, Capitalize Title) showed
every CSS custom property being set correctly, but the actual rendered
elements never changed. Diagnosed via `document.styleSheets` rather
than guessing: the browser was rendering a STALE CACHED `style.css`,
even though a fresh `fetch(url, {cache:'no-store'})` against the exact
same URL correctly returned the new rules -- `index.html`'s own
`<link rel="stylesheet" href="src/style.css">` has NEVER had a cache-
busting query string, unlike `main.js?v=N`/`devpanel.js?v=N`. This means
ANY prior style.css edit in this project's history could have silently
failed to take effect in an already-primed browser, not just this
round's own change. Fixed by adding `?v=2`; will bump on every future
style.css edit, matching the existing convention for the 2 JS files.

Re-verified everything in a genuinely fresh tab after the fix: all 3
sample controls (color, bold, capitalize) correctly changed their
target elements' computed styles; the UNTOUCHED default state produced
the exact same computed output as before this round (title/group font-
weight 600, button font-weight 400, matching the pre-existing hardcoded
look) -- confirming both correctness and the no-op-by-default guarantee.
Also spot-checked a slider (Button Height -> 40px, confirmed via
`getComputedStyle().minHeight`). Zero console errors in the final fresh-
tab check. Corrected `CODE_SUMMARY.txt` (new GOTCHAS entry covering both
the port and the cache-buster bug) and `PROJECT_PROGRESS.md`. Cache-
buster bumps: `devpanel.js?v=5` -> `?v=6`, `style.css` (new) `?v=2`,
`index.html`'s own `src/main.js?v=31` -> `?v=32`.

--------------------------------------------------------------------------------
2026-09-14 (continued once more)  -  ~2:41 AM - 3:01 AM EDT

4 direct follow-on requests, fired in quick succession: "1. Scroll
Strength is the srolling intensity witin the dev panel. impelment
that." / "2. Implement anything in the dev paneltemplate that regards
group and setting reordering, and nesting. I currently cant nest groups
within each other." / "3. Port the template's Dev Panel Group settings
names, ordering, groups, sub groups, group nesting, and ordering." / "4.
[mouse log -- see the NEXT entry, handled as its own round]" -- plus 2
more that landed mid-investigation: "and also port the Dev Panel
Group's actual settings such as sizes, spacing, and color" and "we
currently have some yellow text in our Dev Panel group. fix that. Just
match the template."

**Item 1 corrects a mistake from the round above:** that round excluded
"Scroll Strength" as a different, unrelated project's own game-content
mechanic, based on a shallow read of its wiring (`.dev-panel-scroll-
content`, which LOOKS generic). Re-read it properly this time and
confirmed it genuinely is the template's own name for its panel body's
scrollable area. Implemented for real: a wheel listener on `.dp-body`
reading `cfg.dp_scrollStrength` live, `e.preventDefault()` +
`scrollTop += deltaY * strength`, with `strength === 1` treated as "let
native scroll happen" rather than a no-op `* 1`. Verified live via
synthetic wheel events at 2 different strengths, confirming exact
`deltaY * strength` scroll deltas both times.

**Item 2**: read `setupReorder()`'s own group-level `getTargets`
callback in `buildDevPanel()` before touching anything, rather than
assuming a rewrite was needed. Found the entire 1-level nesting cap
lived in exactly one place: a `:scope >`-restricted target-body query
plus an explicit "already has children -> top-level only" exclusion.
Separately confirmed (by reading, not assuming) that `captureGroup()`/
`applyOrder()` -- the save/restore round-trip -- were ALREADY genuinely
recursive despite nearby comments claiming a matching "one level deep"
limitation; those comments described the drag-reorder's own cap's
downstream effect, not an actual limit in either function. Fixed by
removing the `:scope >` restriction (now matches every group's body at
any depth) and adding a `!item.contains(...)` filter to prevent a group
being dropped into itself or its own descendant. Corrected 4 now-stale
comments referencing "the same one-level cap" (2 in the outer panel's
own now-fixed code, 1 in the unrelated list-picker system which keeps
its own separate cap unchanged -- out of scope, confirmed via direct
reading that it's a genuinely different feature -- with its own comment
corrected since it used to claim the 2 systems shared one rule).
Verified live: a body already nested 2 levels deep is correctly offered
as a valid drop target by the fixed query.

**Items 3 (as amended by the 2 follow-on messages) + the yellow-text
report**: transcribed TEMPLATE_DEV_PANEL.html's own
`applyDefaultDevPanelSubgroupOrder()` function EXACTLY -- group names,
nesting (TEXT holds 5 further subgroups: Dev Panel Title/Group Title/
Setting Title/TABS/BUTTONS), and per-group control membership/order,
including porting "Button Text Border" under "Dev Panel Title" even
though that specific placement reads as a likely copy-paste artifact in
the template itself, since the request was to match the template as
given. New `organizeDevPanelSubgroups()` builds this once, right after
`buildDevPanel()`, relying on item 2's own unlimited-nesting fix for
TEXT's own 2nd-level children; a genuine saved order still correctly
takes precedence afterward via the existing `resetSettings()` call.
Added the one pre-existing template control the FIRST chrome-group port
round had simply missed: `dp_groupLabelBgColor`. Updated every existing
control's own default to the template's real current value (a
deliberate, disclosed visual change this round: accent `#7d8cff` ->
`#005f8f`, font -> Verdana, several caps/bold toggles false -> true) --
the opposite choice from the FIRST chrome-group port, which had
deliberately kept every default at this project's own prior look.

Cross-checking the template's own `DEV_PANEL_STYLE_SHARED_KEYS` array
directly (rather than guessing) while doing this ALSO explained the
"yellow text" report on its own: 4 letter-spacing controls (Tab/Group/
Settings/Button) had been wrongly marked `perDevice: true` in the first
port, incorrectly triggering this project's existing `.dp-row.dp-per-
device` yellow-label styling -- the template treats only Title's own
letter-spacing as per-tab, the other 4 as shared. Fixed by removing
`perDevice` from those 4 (Title's stays, faithfully preserving the
template's own asymmetry rather than "correcting" it to be consistent).

Verified everything live, but only after diagnosing a genuine false
alarm first: the very first test showed the new subgroup structure
completely empty and several new default values not taking effect,
which turned out to be a STALE `localStorage` blob left over from much
earlier in this same session's own testing (a manually-injected Copy
Settings dump from a prior investigation, still sitting in this
browser's storage) -- `resetSettings()`'s own genuine restore-from-save
behavior was correctly overriding this round's own fresh defaults and
structure with that old saved state, exactly as designed; not a bug.
Confirmed via direct `cfg` readback before spending time debugging the
wrong thing, cleared it, and re-verified clean: full nested structure
matches the template exactly; real values resolve correctly; the 4
corrected controls no longer appear in `.dp-row.dp-per-device`, Title's
still correctly does. Corrected `CODE_SUMMARY.txt` (new GOTCHAS entry,
plus a correction to the prior round's own wrong Scroll Strength
exclusion) and `PROJECT_PROGRESS.md`. Cache-buster bumps: `devpanel.js?v=6`
-> `?v=7`, `style.css?v=2` -> `?v=3`, `index.html`'s own
`src/main.js?v=32` -> `?v=33`.

--------------------------------------------------------------------------------
2026-09-14  -  ~03:00 AM - 03:14 AM EDT (bounded by checkpoints; see Verified note)

Mouse Tracking Log port -- direct request: "I updated the Dev Panel Template
to have a mouse log system similar to ours. Update ours to match the
template if there are features missing." Ported the template's click-
classification layer onto this project's existing Mouse Tracking Log
(which already had a click log + interval cursor-position log; the
template's own log was a separate, newer system built to extend that same
idea further) -- NOT a fork of devpanel.js's own generic engine, an
extension of `main.js`'s own existing log, since this project's log is
main.js-based by design.

Added:
- Replaced the single `pointerdown` click listener with a `pointerdown`/
  `pointerup` pair: `pointerdown` just records time/position/button/target;
  `pointerup` classifies what actually happened, since neither multi-click
  nor drag-release is knowable until release. Right-click logs immediately
  (`Right-click at (x,y) -> <trigger>`); a release held past
  `MOUSE_LOG_HELD_DRAG_MS` or moved past `MOUSE_LOG_MOVE_THRESHOLD_PX`
  (10px) logs as `Drag-release ... (heldMs:N)`; a quick left/middle click
  is debounced (`MOUSE_LOG_MULTICLICK_MS`) into Click/Double-click/Triple-
  click the same way the template's own version does, so a fast double-
  click doesn't log as 2 unrelated single clicks.
- `describeMouseLogTrigger(e)` -- the original inline trigger-description
  logic (Dev Panel interaction / Camera Pan / Camera Zoom-Dolly, per this
  project's own actual OrbitControls bindings) factored out unchanged so
  both the right-click and drag-release paths can reuse it.
- `logMouseLogViewportContext(reason)` -- ported from the template's own
  viewport-context pattern: one `Viewport (start): WxH` entry when the log
  widget is first built, and one more `Viewport (resize): WxH` entry only
  on a REAL size change (guarded against firing on every 'resize' event
  regardless of whether the size actually changed).
- A "Save" button next to the existing "Copy" button on the Mouse Tracking
  Log widget (`buildMouseTrackingLogWidget()`), same Blob+`<a download>`
  client-side file-save pattern as the template's own Save (kept this
  project's own flat-text log format rather than porting the template's
  separate headered-.md structure, since this log has no such sectioning
  to preserve).

Deliberately NOT ported: the template's full touch-gesture classification
(tap/double-tap/swipe/pinch) -- this project's CURSOR-TRACKING mechanic has
no touch-input equivalent by design (see this project's own CLAUDE.md,
same convention DOTFLICKO uses for its own mouse-only interaction), so
there is nothing for a touch gesture to actually describe here. Disclosed
as a judgment call, not silently dropped.

Verified live (fresh server after an unrelated static-server connection
reset, see GOTCHAS): single-click, double-click, and right-click all
classified correctly via synthetic `PointerEvent`s; a held-600ms release
logged as `Drag-release ... (heldMs:607)`; the widget's initial build
logged `Viewport (start): 1024x768`; a genuine `resize` dispatch logged
`Viewport (resize): 375x812` (the Browser pane's own viewport-emulation
tool changes `window.innerWidth`/`innerHeight` without dispatching a real
`resize` DOM event, confirmed separately -- not a bug in this project's
listener, which fires correctly on an actual resize); the Save button
fired its Blob/download code with no error (flashed "Saved!"). 2 stale
console errors (`net::ERR_CONNECTION_RESET`, "Failed to load hand model")
persisted from an earlier failed tab load this same session and were
confirmed stale, not new, by re-checking hand count (510, unchanged).
Cache-buster bump: `index.html`'s own `src/main.js?v=33` -> `?v=34`.

--------------------------------------------------------------------------------
2026-09-14  -  ~03:16 AM - 03:27 AM EDT (bounded by checkpoints; see Verified note)

Named Setting States ("Saved Dev Settings") -- direct request: "double
check the dev panel template in regards to Saved Dev Settings feature.
Implement that." Read TEMPLATE_DEV_PANEL.html's own `[JS-13b]` section
first: Save/Use/Delete/Set-as-Default for multiple named, full-panel-state
snapshots (CLAUDE.md §12d's optional upgrade, modeled on HANDO's own
per-control `buildListPickerRow()` generalized to the WHOLE panel's state).
Confirmed this project's `devpanel.js` had no existing scaffolding for it
(only the per-control list-picker, `dp-lp-group`, used elsewhere for
Saved Poses-style presets -- a genuinely different, narrower mechanism).

Added:
- `captureFullPanelState()`/`applyFullPanelState()` -- factored out of the
  existing `copySettings()` (which already built this exact snapshot
  shape: per-device values, group/row order, text-rename overrides,
  per-device panel geometry) so Copy Settings and the new feature share
  one capture/apply path rather than duplicating it. `copySettings()`
  itself now just calls `captureFullPanelState()` -- behavior-preserving,
  verified identical output shape.
- A new standalone collapsible "Saved Dev Settings" group, built directly
  (not via `createGroupElement()`, to skip its always-present drag handle,
  since this group is deliberately NOT part of the per-tab reorderable
  group/drag-reorder system -- one state snapshot covers the whole panel
  across all 3 device tabs at once). Sits directly under the Copy/Save/
  Reset action row, outside `groupsEl` entirely, matching the template's
  own placement exactly.
- A plain `<select>` (not a custom list widget, matching the template's
  own simpler choice here -- no Rename requested) + Save/Use/Delete/Set
  Default buttons. Save prompts for a name (confirms before overwriting
  an existing one); Use applies a state live without touching what Save/
  Reset would restore (a reversible "try it"); Delete removes a saved
  state only; Set Default applies the state AND runs it through the
  existing `saveSettings()`, making it what Reset/a fresh load restores.
  New localStorage key `handyDandies.devSavedStates` (via the existing
  `storageKeyPrefix` convention, alongside `.devSettings`/`.devPanelGeom.`).
- New `.dp-standalone-group select` CSS rule (style.css) -- the native
  select had no existing style scope to inherit from (the one prior
  `<select>` usage, the multi-value picker, is scoped to its own
  `.dp-multi-select-row`), so it rendered as an unstyled browser default
  without this.

Verified live: changed Dev Panel Accent Color, saved it as a named state
("Test1"), changed the color twice more, then (1) Use correctly restored
the saved color without writing to the Save/Reset baseline (confirmed
`handyDandies.devSettings` stayed empty/untouched through Use alone);
(2) Set Default correctly applied AND persisted the color into that same
baseline key; (3) Delete correctly removed the state from both the
dropdown and localStorage. Collapse/expand toggle on the new group
verified working independently of the per-tab group system. A completely
fresh tab (cleared state from a prior round) loaded cleanly: panel built,
510/510 hands, the new group present with a correctly-empty list (the
just-deleted state didn't reappear), and zero console errors at all --
confirming 2 console errors seen in an earlier same-session check
(`net::ERR_CONNECTION_RESET`, "Failed to load hand model") were stale
from an unrelated transient navigation failure, not caused by this round.
Cache-buster bumps: `devpanel.js?v=7` -> `?v=8` (via main.js's own import
line), `style.css?v=3` -> `?v=4`, `index.html`'s own `main.js?v=34` ->
`?v=35`.

--------------------------------------------------------------------------------
2026-09-14  -  ~03:35 AM - 03:48 AM EDT (bounded by checkpoints; see Verified note)

Dev Panel group correction round -- direct bug reports: "I still see yellow
text... I said match the Dev Panel group settings EXACTLY. I still see a
lot of floating settings within the Dev Panel group... in the panel UI
group. Remove any redundant settings... Match the exact settings for the
dev panel group to the template. take a look at the project Clicko...
match Clicko. this includes checkbox states, slider numbers, etc." Per
CLAUDE.md's own freshly-corrected §12i, Clicko is the Gold Standard for
this group's values/structure -- read Clicko's actual source directly
(`DEV_PANEL_STYLE_SHARED_KEYS`, `DEVPANEL_STYLE_CONTROLS`,
`DEVPANEL_MISC_CONTROLS` in its own index.html, plus its live
`data/processed/dev-panel-settings.json`) rather than re-deriving from
memory or CLAUDE.md's own prose summary, which turned out to disagree with
Clicko's real code in one place (letter-spacing shared-vs-per-tab split).

Audited all 41 real Dev Panel group fields 1:1 against Clicko's authoritative
source. Findings:
- **1 real perDevice bug**: `dp_scrollStrength` had no `perDevice` flag
  (rendered shared/no-yellow), but Clicko's own `DEVPANEL_STYLE_CONTROLS`
  registers a genuine per-tab Scroll Strength slider for all 3 tabs --
  fixed by adding `perDevice: true`. Every other field's perDevice status
  was already correct (cross-checked all 41 individually against Clicko's
  own `DEV_PANEL_STYLE_SHARED_KEYS` array) -- the earlier "yellow text"
  fix this same session was already accurate for those.
- **"Floating settings in Panel UI"**: live DOM inspection (fresh tab,
  cleared localStorage) found NO actual floating/orphaned rows -- PANEL UI
  correctly holds exactly Clicko's 4 fields (Background/Accent/Slider
  Color, Opacity), matching `organizeDevPanelSubgroups()`'s own
  assignment. Only `dp_textEditMode` sits outside any subgroup, by design
  (a standalone panel-level control, not a Clicko-ported field at all).
  Likely a stale browser/server cache showing a pre-session-fix state,
  not a current code defect -- cache-busters bumped again regardless.
- **Redundant settings**: none found -- audited the full 41-field set
  against Clicko's real 41 and confirmed exact 1:1 correspondence, no
  extras, no duplicates (the one known-redundant "Body Text Font Size"
  was already removed in an earlier round, confirmed still gone).
- **5 shared-value mismatches** (didn't match Clicko's real desktop
  values): Title Font Size 22->17, Group Title Font Size 11->14, Font
  (All Text) 'Verdana...'->'Helvetica, Arial, sans-serif' (+ full
  11-option list matching Clicko's own select exactly, was a 5-option
  subset), Title Letter Spacing 0->2.3, Group Letter Spacing 0->0.8.
- **New `defMobile`/`defLandscape` optional control-definition fields**
  (additive, non-breaking -- a control with neither behaves exactly as
  before): Clicko genuinely tunes 6 per-device fields differently per
  tab, not just per-device-independent from a shared starting point --
  Title Font Size (17/11/22), Group Title Font Size (14/11/11), Settings
  Title Font Size (12/10/12), Title Letter Spacing (2.3/0/0), Button
  Height (21/24/21), Scroll Strength (0.2/1/0.2). Previously every
  perDevice control seeded all 3 device stores from the same single
  `def`, so Mobile/Landscape silently cloned Desktop's tuning instead of
  Clicko's own real per-device values.
- **Named Setting States ("Saved Dev Settings") made movable + repositioned
  above "Dev Panel"** -- direct mid-task follow-up ("Save Dev Settings
  should also be movable. It should be above the Dev Panel group.").
  Rebuilt via the same `createGroupElement()` every other group uses
  (real drag-handle, real collapse/rename behavior) and inserted as
  `groupsEl`'s first child, instead of the earlier standalone
  hand-built group appended outside `groupsEl` entirely -- it's now a
  genuine member of the per-tab reorderable group/drag-reorder system,
  its position persisted through the normal Save/Reset order capture
  (`captureGroup()`/`applyOrder()`) same as any other group. Its own
  select+button row has no `data-key`, so only the GROUP's own position
  is captured/restored, never the row contents -- verified no interference
  with the Save/Use/Delete/Set Default mechanism itself.

Verified live (fresh tab, cleared localStorage, both fixes together):
topLevelGroups order is `["Saved Dev Settings", "Dev Panel", ...]`;
the group has a real `.dp-drag-handle` (generic reorder mechanism reused
unchanged from every other group, not independently re-tested via a full
synthetic drag); all 15 genuinely-per-tab fields (now including Scroll
Strength) show yellow, all 26 shared fields don't; PANEL UI holds exactly
4 rows; desktop/mobile/landscape tab values read back exactly matching
Clicko's real JSON (e.g. Title Font Size 17/11/22, Button Height 21/24/21,
Scroll Strength 0.2/1/0.2); 510/510 hands still rendering; zero new
console errors (2 stale ones persisted from an earlier unrelated
navigation failure this session, confirmed via hand-count readback).
Cache-buster bumps: `devpanel.js?v=8` -> `?v=9`, `style.css?v=4` -> `?v=5`,
`index.html`'s own `main.js?v=35` -> `?v=36`.

--------------------------------------------------------------------------------
2026-09-14  -  ~03:59 AM - 04:09 AM EDT (bounded by checkpoints; see Verified note)

Fixed "Palm Faces Cursor" rotating 180 degrees backwards -- direct user
report: "the palm face cursor is doing the exact opposite. rotate them
180 degrees." The feature's own palm-plane-normal calibration (from an
earlier round this same project) was `pinkyVec x indexVec` -- the exact
OPPOSITE of the true outward palm normal, so the feature aimed the BACK
of the hand at the cursor instead of the palm. Fixed by swapping the
cross-product operand order to `indexVec x pinkyVec`: a well-known
identity, a x b = -(b x a), makes this a clean, exact negation of the
prior vector -- since the palm normal and the back-of-hand normal are
themselves exact opposites on the same approximate hand-plane, negating
which one feeds the correction is precisely the 180-degree flip asked
for, with no other math touched (the lookAt/composition logic that maps
this normal onto the cursor direction was already correct and unchanged).

Verification note, disclosed honestly: this project's toon-shaded gray
mesh has no strongly distinct palm-vs-back surface texture (no palm
lines/creases rendered), so a literal "does this pixel-level render show
palm or back" screenshot comparison wasn't reliably conclusive at this
render fidelity -- attempted via a temporary close-up camera reposition
(frozen OrbitControls, one hand's wrapper manually rotated to face the
relocated camera) but the resulting silhouette was ambiguous either way.
Also discovered and disclosed a real limitation in the ORIGINAL
implementation's own dot-product self-check (the "confirmed... dot
product = 0.999999995" verification from that earlier round): that check
is mathematically tautological -- it always self-confirms regardless of
which cross-product order is used, since it re-measures the SAME formula
that built the correction in the first place, so it could never have
caught this sign bug even though it "passed." Relied instead on the
airtight mathematical guarantee (cross-product anticommutativity means
this change is a true, exact negation, not an approximation) plus the
user's own direct, specific report of the symptom and the exact fix
needed. Confirmed a clean, isolated diff (git diff reviewed: only the
comment and the one operand-order line changed) and a normal fresh-tab
load afterward (510/510 hands, zero new console errors).

Cache-buster bump: index.html's own main.js?v=36 -> ?v=37.

--------------------------------------------------------------------------------
2026-09-14  -  ~04:11 AM - 04:14 AM EDT (bounded by checkpoints; see Verified note)

Added "Palm Face Rotation (Deg)" slider (Cursor Tracking group, -180 to
180, default 0) -- direct follow-up to the 180-degree flip fix above:
"Provide me a slider to control that rotation actually." Rather than
trusting a fixed, code-baked calibration to be right (which just proved
wrong once already), the palm-facing correction is now a LIVE, per-frame
computation (`computePalmFaceCorrectionQuat()`) incorporating this
slider, so a future bad calibration (on this rig or a different model)
can be corrected from the panel instead of needing another code change.

Rotates the calibrated palm-normal direction around the hand's own
pointing axis (local -Z in the aligned frame, the same axis `pointDir`
itself sits on) before aiming it at the cursor -- perpendicular-ish to
the palm normal for a roughly-flat hand, so the slider sweeps the aimed
direction continuously from the palm (0 degrees) through edge-on (+-90)
to the exact opposite, the back of the hand (+-180), rather than an axis
that would leave the correction unchanged. Refactored the module-level
`palmFaceCorrectionQuat` (a fixed value computed once at model load) into
`palmNormalAligned` (the raw calibrated direction, still computed once)
plus a new `computePalmFaceCorrectionQuat()` function called once per
frame (not once per hand -- identical for every hand, like `alignQuat`
itself) inside the tracking block, only when Palm Faces Cursor is on.

Verified live: slider row renders with the correct key/range/default;
toggling the checkbox + slider through several values for ~100ms while
tracking ran produced no errors, 510/510 hands still updating normally.
Also ran a genuine INDEPENDENT cross-check (not the tautological kind
flagged in the entry above) confirming the roll-axis choice is
mathematically sound: built the OLD (pre-fix) formula's correction
quaternion completely separately, then built the NEW formula's output at
offset=180 degrees, and confirmed the two are the same rotation
(quaternion dot ~0.999966, i.e. ~1.0) -- proving the slider's 180-degree
end genuinely reproduces the exact old (wrong) behavior, and by
extension that 0 degrees genuinely gives the new (correct) one. Zero new
console errors in the final fresh-tab check.

Cache-buster bump: index.html's own main.js?v=37 -> ?v=38.

--------------------------------------------------------------------------------
2026-09-14  -  ~04:54 AM - 04:58 AM EDT (bounded by checkpoints; see Verified note)

Fixed Palm Face Rotation slider's roll axis -- direct follow-up: "The
palm face thing isnt rotating correctly. I guess i didnt specify th
rotation axis. So The rotation axis should be perpendicular to the wrist
cropping plane... So the rotation should be parallel to the crop plane."
The slider's roll axis had been a guess (local -Z, i.e. `pointDir`/
wrist-to-fingertip -- the same axis `alignQuat` itself is built from),
made without an explicit spec at the time. The real spec is the WRIST
CROP PLANE's own normal -- the exact forearm-to-wrist direction
`updateWristClipPlaneForHand()` already computes per-hand, per-frame,
for the Arm Length crop (`rForearmBend` -> `rHand`), NOT the
wrist-to-fingertip axis. Added `wristCropNormalAligned`, measured once
at model load in the same raw bind-pose frame as `pointDir`/
`palmNormalAligned`, and `computePalmFaceCorrectionQuat()` now rolls
around this axis instead.

Confirmed this is a real, substantive fix and not a no-op: measured
`pointDir` and the crop-plane normal directly on a live hand (in the
same reconstructed raw frame used for the prior round's verification)
and found they differ by 28.35 degrees in this rig's actual bind pose --
a genuine rig quirk (the forearm-to-wrist segment isn't perfectly
collinear with wrist-to-fingertip), not a rounding artifact. Also
confirmed the new axis is ~90.5 degrees from the palm normal (i.e.
essentially perpendicular), which is what makes the slider sweep the
aimed direction through a proper, non-degenerate full range rather than
a lopsided one. Offset=0 behavior is unchanged by construction (a
zero-angle rotation is the identity regardless of which axis it's around),
so the already-fixed default palm-facing orientation from the prior
round is untouched.

Verified live: fresh load, 510/510 hands, toggling the checkbox through
several slider values (45, 180) for ~100ms each with tracking running
produced no errors; zero console errors in the final check.

Cache-buster bump: index.html's own main.js?v=38 -> ?v=39.

--------------------------------------------------------------------------------
2026-09-14  -  ~05:00 AM - 05:06 AM EDT (bounded by checkpoints; see Verified note)

Two bundled direct requests:

**1. "Default" pose button.** "didnt i ask you for a way to set the pose
of the shown hands? if not, add a buttton in the pose selector,
'Default', that just sets the pose to all the hands." Checked project
history first: the user HAD asked for this once before (2026-09-13,
"when i click Use, it doesnt apply it to all the hands on screen") but
explicitly redirected it at the time toward a preview-only "Pose
Preview" viewer instead -- so no, there wasn't currently a way to apply
a pose to the actual field, that was a deliberate prior choice, not an
oversight. Added a new `type: 'button'` control ("Default") next to the
existing Saved Poses list-picker in the Pose group: resets every
`POSE_PRESET_KEYS` slider to its own `POSE_KEY_DEFAULTS` value (via
`syncValue()`, which updates `cfg`/`store`/the slider display but
deliberately doesn't fire `onChange` -- see its own devpanel.js comment),
then calls the existing `onWholeHandRotationChange()` once (already
re-derives `cloneBaseQuat` from the now-reset rotation sliders AND
re-applies every finger + the wrist internally) to actually re-pose.
Since Pose is shared across every hand already (see this group's own
top comment), one call resets the whole field -- no per-hand loop
needed in the new code at all, entirely reused existing infrastructure.
Verified live: forced 3 pose keys away from default, clicked the new
button, confirmed all 3 read back exactly at their code defaults.

**2. Palm Face Rotation slider, independent of Palm Faces Cursor.**
"make the palm face rotation slider work even if palm faces cursor is
turned off" / "it will just rotate every hand." The slider previously
had zero effect with that checkbox off, since its angle only ever
reached the hand composed INSIDE `computePalmFaceCorrectionQuat()`,
which itself is only ever called while the checkbox is on. Factored the
roll itself out into a new `computeRollQuat()` (just the rotation around
the wrist-crop-plane axis, no palm-normal involved) and changed the
render loop to use it directly when Palm Faces Cursor is off, instead of
skipping the correction entirely -- `computePalmFaceCorrectionQuat()`
still incorporates the same roll internally when the checkbox IS on, so
that path's behavior is unchanged.

Verified live: the intended live-render check (toggle the slider,
measure `hand.wrapper.quaternion` before/after) initially showed NO
change -- root-caused immediately to a known, already-documented
environment limitation (the Browser pane's own `requestAnimationFrame`
suspension while hidden, confirmed via `tabs_context` explicitly
reporting "the Browser pane is currently hidden"), not a code defect.
Verified the actual math directly instead, replicating the exact
render-loop composition externally (same raw-bind-pose-frame
reconstruction technique used for the axis-difference check 2 rounds
ago): the composed `desired` quaternion at offset=0 vs. offset=90 (with
Palm Faces Cursor off) differ by a quaternion dot product of exactly
0.7071 (= cos(45 degrees) = the precise mathematical signature of a real
90-degree rotation difference, not an approximation) -- confirming the
fix works correctly. Zero console errors.

Cache-buster bump: index.html's own main.js?v=40 -> ?v=41.

--------------------------------------------------------------------------------
2026-09-14  -  ~05:00 AM - 05:23 AM EDT (bounded by checkpoints; see Verified note)

Ported HANDO's Pose group subgroup structure -- direct request: "check
the project Hando's git save in regards to its Dev Panel, Pose settings.
I want you to port the subgroup names, nesting, setting groupings, and
order." First re-verified the prior round's uncommitted-at-the-time work
(a concurrent session had already committed it as 4b2281c/7538a1b while
this was being investigated -- confirmed via git log/fetch, not
re-committed) live: the roll-independent-of-checkbox math checked out
exactly (quaternion dot 0.7071 between offset=0/90, the precise
signature of a 90-degree difference) and the new Default pose button
correctly reset 3 forced-off-default keys back to their code defaults.

For the actual request: HANDO's own main.js Pose control array is flat,
with zero subgroup/nesting information -- the real structure only ever
existed in HANDO's own committed `data/processed/dev-panel-settings.json`
(its `order` array's "Pose" entry), reflecting how a live session there
had actually reorganized it via drag-and-drop. Read that directly: 6
subgroups, in order -- one bundling Whole-Hand Rotation (X/Y/Z) together
with the full Thumb slider set, then Wrist (hideWrist/wristBend/
wristSplay), then one subgroup per remaining finger (Index/Middle/Ring/
Pinky), each holding that finger's Curl/Splay/Splay2/Curl-Bias/Tip-Twist
sliders. HANDO's own subgroup names were still the unrenamed drag-drop
defaults ("New Group", "New Group (3)" through "(7)") -- disclosed this
honestly rather than porting meaningless placeholder text, and used
descriptive names instead (Whole-Hand Rotation & Thumb / Wrist / Index /
Middle / Ring / Pinky) while porting the grouping/order exactly as
found. Omitted HANDO's 5 `baseOnlyCurl*` sliders (don't exist as
controls in this project) and `hideWrist` from the ported Wrist
subgroup -- this project's own Hide Wrist is a separate, larger reactive
Arm Length system (Crop Wrist master toggle, Reactive checkbox, Min/Max
range, Length Scaling Curve), already deliberately excluded from what a
saved Pose captures per this group's own existing top comment; left it
exactly where it already sat.

Architecture: this project's own `organizeDevPanelSubgroups()` (used for
the built-in "Dev Panel" chrome group) is hardcoded to that one group's
name/structure and lives in the shared `devpanel.js` engine, which this
project's own file map says not to fork. Added a new, generic exported
`organizeGroupSubgroups(groupsEl, groupTitle, subgroupSpecs)` instead --
same "flat rows built by buildDevPanel(), organized into named
subgroups, once, before a real saved order takes precedence" shape, but
parameterized rather than hardcoded, plus a new optional
`initDevPanel(groups, { organizeSubgroups })` hook so a project supplies
its own spec without touching the shared engine's own internals. The
existing Dev-Panel-specific function was left completely untouched.

Verified live (fresh tab, cleared localStorage): the Pose group's direct
(un-nested) rows are exactly `savedPoses`, `resetPoseToDefaultBtn`,
`cropWristEnabled`, `hideWrist`, `reactiveArmLengthEnabled`,
`armLengthRange`, `armLengthCurve` -- confirming the Arm Length family
was correctly left untouched; all 6 subgroups nest exactly the expected
keys, in order, all collapsed by default; a slider inside a newly-nested
subgroup (Index's own Curl) still correctly commits to `cfg` and repose
the field; collapse/expand toggle and the group's own drag-handle both
work identically to any other group; zero console errors; 510/510 hands
still rendering.

Cache-buster bumps: `devpanel.js?v=9` -> `?v=10` (via main.js's own
import line), `index.html`'s own `main.js?v=41` -> `?v=42`.

--------------------------------------------------------------------------------
2026-09-14  -  ~05:31 AM - 05:37 AM EDT (bounded by checkpoints; see Verified note)

Redefined "Palm Faces Cursor" entirely -- direct correction after the
user tested it against their own real settings: "palm faces cursor isnt
working as i expect... every hand more or less faces Upwards [with the
cursor centered]. what i want is - imagine 8 hands surrounding my
cursor [at 45-degree increments]... The hand directly below would be
facing up (as it is currently)... above the cursor... 180... directly
to the right...90...the one to the left is -90. the one above and to
the right would have rotation 135... My Palm Face rotation slider wil
then just add onto that rotation number." This REPLACES the prior
3-round "aim a calibrated palm-normal vector at the cursor in full 3D"
mechanism entirely -- the real spec is a much simpler 2D "compass
needle" roll, not a 3D vector-aim.

Each hand now rotates, around the SAME wrist-crop-plane axis the Palm
Face Rotation slider already used, by an angle computed fresh per hand
per frame from THAT hand's own position relative to the live cursor
target, in the field's own world XY plane: `computeRadialRollDeg(hand,
cursor) = atan2(dx, -dy)` where `dx = hand.x - cursor.x, dy = hand.y -
cursor.y`. This exact formula was derived directly from the user's own
5 stated reference points before writing any code, then verified to
reproduce every one of them precisely, plus the 3 implied diagonals
(45/-45/-135 degrees) the user didn't state explicitly but which follow
the same pattern. The Palm Face Rotation slider now adds directly onto
this dynamic angle (still applies on its own, at 0 base, when Palm
Faces Cursor is off -- unchanged from the immediately preceding round).

Removed the now-dead `palmNormalAligned`/`computePalmFaceCorrectionQuat()`
measurement (the index/pinky-bone cross-product calibration from the
last 3 rounds) -- not just left unused, deleted outright, since it
answered a different question ("which face of the mesh is the palm")
that this simpler 2D-rotation spec doesn't need at all.

Verified live in 2 layers, since the Browser pane's own hidden-pane
rAF-suspension (documented 2 rounds ago) makes a real live-render check
unreliable: (1) the bare formula against all 8 of the user's
above/below/left/right/diagonal reference points, all exact; (2) the
FULL render-loop composition, externally reconstructed and checked via
quaternion dot products -- a hand directly below the cursor produces a
rotation identical to the pre-existing default lookAt (dot=1.0,
confirming "facing up, as it is currently" is preserved exactly); a
hand to the right of the cursor differs from default by exactly the
mathematical signature of a 90-degree rotation (dot=cos(45)=0.7071);
adding a 30-degree slider offset on top differs from the base case by
exactly the signature of a 30-degree rotation (dot=cos(15)=0.9659).
Zero console errors; 510/510 hands.

**Concurrent-session note**: `src/devpanel/devPanel.js` had a real
in-progress uncommitted change from another session (their own
`organizeGroupSubgroups` Pose-subgroup work, per their own `cb11b3c`/
`7fc8967` commits landing moments before this round started) sitting in
this same shared working tree throughout. Confirmed via `git diff`
before staging that this round's own uncommitted diff to `main.js`
contained ONLY this round's own intended changes (their already-committed
Pose-subgroup content was untouched, already baseline) -- staged and
committed `index.html`/`main.js` only, left `devpanel.js` completely
alone for them to commit on their own schedule.

Cache-buster bump: `index.html`'s own `main.js?v=42` -> `?v=43` (their
own `devpanel.js?v=10` -> `?v=11` import-line bump, for their own
pending devpanel.js work, was already present in the shared main.js
file and left untouched).

--------------------------------------------------------------------------------
2026-09-14  -  ~05:31 AM - 05:38 AM EDT (bounded by checkpoints; see Verified note)

Fixed the Pose subgroup port getting silently flattened on real page
loads -- direct report: "bro yuo didnt move the settings into the
groups which was half the point." Root cause: this project's own already
-documented "stale localStorage" gotcha, hit earlier this session with
the Dev Panel group's own subgroup port too (that time only ever worked
around by manually clearing localStorage in a test tab -- never actually
fixed in code, which is exactly why it resurfaced here). `resetSettings()`
runs AFTER `organizeGroupSubgroups()`/`organizeDevPanelSubgroups()` build
the fresh nested structure, and its own `applyOrder()` call -- restoring
whatever order was ALREADY saved from before either subgroup port existed
-- moves every listed row back out to a flat layout, since the stale
saved order has no idea the new subgroups exist.

Fixed both functions to be idempotent (reuse an existing same-titled
subgroup by querying for it first, instead of always calling
`createGroupElement()` unconditionally) and added a 2nd call to each,
right after `resetSettings()` runs in `initDevPanel()` -- so whatever a
stale order just flattened gets correctly re-nested immediately
afterward. A genuinely current saved order (one that already reflects
the nested structure, e.g. after the user drags a row elsewhere post-port
and saves) is unaffected either way -- this only ever moves a spec'd key
INTO its subgroup, never fights a real saved position.

Verified by deliberately staging a stale, pre-port, fully-flat Pose
order into `localStorage` (reconstructed to match exactly what a real
user's browser would have saved before this session's port existed) and
reloading: all 6 subgroups (Whole-Hand Rotation & Thumb / Wrist / Index /
Middle / Ring / Pinky) correctly re-nested their rows, with zero
duplicate/empty subgroup shells (confirmed via a duplicate-title check),
and the unrelated Dev Panel group's own MECHANICS/PANEL UI/TEXT structure
stayed correct throughout. 510/510 hands; zero new console errors (one
stale, already-documented transient server-connection-reset error was
present, unrelated).

**Concurrent-session note**: a different session committed a substantial,
unrelated redefinition of Palm Faces Cursor (a full 2D radial-angle
redesign, `624f87e`) in this same shared `main.js` while this fix was in
progress. Left it completely untouched -- confirmed via `git status`/
`git log` that it had already been committed and pushed by the time this
round was ready to commit, so no scoped-staging was even needed; this
round's own commit touched only `devpanel.js`.

Cache-buster bump: none needed this round -- the already-committed
`devpanel.js?v=11` import-line bump (from the concurrent session's own
`index.html`/`main.js` commit) already covers this fix's own file.

--------------------------------------------------------------------------------
2026-09-14  -  ~05:57 AM - 06:09 AM EDT (bounded by checkpoints; see Verified notes)

3 direct requests bundled into one round:

**1. Moved "Enable Label Rename Mode" to standalone panel chrome.**
Relocated from inside the built-in "Dev Panel" group to sit directly in
the panel body, above "+ Add Group" -- stays a fully registered control
(cfg/store/Copy/Save/Reset unaffected, zero new persistence code), just
physically moved via `body.insertBefore(row, addGroupRow)` right after
`buildDevPanel()`/`organizeDevPanelSubgroups()` build/organize it inside
Dev Panel first (where it's deliberately left as that group's one flat,
non-subgrouped row). Matches the shared template's own placement for
this exact checkbox. Verified live: confirmed its row is a direct
`.dp-body` child positioned between Copy/Save/Reset and Add Group, no
longer inside the Dev Panel group; toggling it still correctly flips
`cfg.dp_textEditMode`.

**2. Right-click to delete Arm Length curve dots.** Added a `contextmenu`
listener alongside the existing double-click delete (same "keep the 2
endpoints, only interior points removable" rule), `preventDefault()`ed
to suppress the browser's own native right-click menu over a dot.

**3. New "Responsive Wrist Splay" group** -- direct request: "Wrist will
Splay in response to distance from cursor. Provide similar settings as
the Wrist Crop/ arm Length group. call the new group 'Responsive Wrist
Splay'. By default, set minimum to 0 and maximum to -90. The closest
hands will be at -90 and furthest will be at 0." Mirrors Arm Length's
exact structure (master on/off, a Default value for when Reactive is
off, a Reactive-by-cursor-distance toggle, a Min/Max range widget, a
Distance->Value scaling curve widget) as its own separate top-level
group, per the request's own naming (not nested inside Pose the way Arm
Length is). Adds a per-hand EXTRA wrist-splay rotation on top of the
Pose group's own shared `wristSplay` slider (`applyWristPoseToSkeleton()`
gained an `extraSplayDeg` param, default 0 so every existing call site
is unaffected) -- reuses the exact same per-frame live cursor-distance
values (`live`/`minLiveDist`/`liveDistRange`) Arm Length already
computes in `updateRenderOrder()`, not a second distance pass, and
reuses `evaluateArmLengthCurve()` directly (already fully generic
despite the name -- distance in, curve value out, nothing Arm-Length-
specific about it).

The default range (min:0, max:-90) is deliberately NOT numerically
ordered -- "min"/"max" name which END of the distance curve each value
applies to (farthest/nearest, matching Arm Length's own min/max roles),
not a numeric min<=max constraint -- the underlying lerp works correctly
regardless of which endpoint is numerically larger. The new range
widget's own 2 handles do NOT cross-clamp each other the way Arm
Length's own range widget does (which assumes a numerically-ordered
0-100% scale) -- each handle moves independently across a -180 to 180
degree track, with a vertical tick marking 0 degrees since the track
(unlike Arm Length's all-positive one) spans negative and positive
values.

Verified live via a proper end-to-end check: initially measured the
nearest/farthest hands' `rHand` bone rotation via a raw Euler read and
got a constant -22.6-degree offset on BOTH ends (-112.6 near, -22.6 far,
instead of the expected -90/0) -- root-caused immediately (not assumed
correct) to the measurement technique itself: the rig's own bind pose
has a non-identity Z-component on this bone, so a raw Euler reading
conflates the rest pose with the applied rotation. Redid the
measurement correctly as a baseline-delta quaternion comparison
(capture the bone's quaternion with the feature OFF as the true
zero-reference, then compare against the feature-ON quaternion) and got
the exact expected result: nearest hand -90.0 degrees, farthest hand
exactly 0. Called `updateRenderOrder()` directly (exposed on
`window.__debug`) rather than relying on `requestAnimationFrame`, which
the Browser pane's own hidden-pane suspension (documented several
rounds ago) makes unreliable for this kind of live check. Zero new
console errors (one stale `net::ERR_CONNECTION_RESET` from an earlier
failed navigation attempt, confirmed stale via unchanged 510/510 hand
count on the successful reload).

Cache-buster bumps: `devpanel.js?v=11` -> `?v=12` (via main.js's own
import line), `index.html`'s own `main.js?v=43` -> `?v=45` (2 bumps
folded into 1 final commit -- `?v=44` was an intermediate step verified
mid-round, not separately pushed).

--------------------------------------------------------------------------------
2026-09-14  -  06:15:07 AM - 06:31:19 AM EDT (measured, this entry's own start/finish)

Added "Click Hold-Pose" and "Right-Click Hold-Pose" -- 2 new, fully
independent top-level dev-panel groups (direct request, 2 bundled
features: "when i click and hold, the hands will transition to one of my
saved poses... on release, they transition back to default pose... I can
choose if they all transition together, or if their transition time is
based on distance from cursor," then "On 2nd thought, provide 2 of these.
The 2nd will be right click and hold," then clarified further: "the 2nd
new clickhold pose sets should be their own setting groups").

Each group: a Target Pose dropdown (reads the existing "Saved Poses"
list-picker), Pose Transition Speed (Ms), a Pose Transition Start Time
curve (distance-from-cursor -> start time, 0-1 normalized) with its own
Min/Max Start Time (Ms) range, and matching Retransition Speed/Curve/
Range controls for the release-side animation back to default pose.
Setting a trigger's Min and Max start time equal collapses the stagger to
"every hand transitions together" -- satisfies the request's own
either/or requirement without a separate toggle.

Architecture: a small per-hand-per-trigger state machine (`hand._chp[p]`,
phases idle -> forward -> retransition -> idle). On pointerdown, snapshots
every hand's current Pose-group values (`trig.forwardSnapshot`) and each
hand gets its own forward-phase start delay, computed ONCE from a live
cursor-distance snapshot at that instant (not continuously re-evaluated,
so a moving cursor mid-hold doesn't reshuffle stagger order). On
pointerup, each hand's own last-applied interpolated pose becomes its own
retransition start (correctly handles releasing mid-transition, not just
after reaching the target). Reuses `applyCurlToSkeleton()`/
`applyWristPoseToSkeleton()`'s existing `values` override param (already
proven by Pose Preview) and `evaluateArmLengthCurve()` directly -- no new
posing or curve-evaluation code. mousedown/mouseup listeners are added as
parallel, non-blocking `window` listeners (never call preventDefault/
stopPropagation), matching the Mouse Tracking Log's own established
pattern -- OrbitControls' existing left/right-click-drag camera pan is
completely unaffected and both behaviors work simultaneously by design.

Scope decision (disclosed, not silent): per-hand pose interpolation
excludes Whole-Hand Rotation (modelRotX/Y/Z) -- those 3 keys drive a
single SHARED `cloneBaseQuat` (computed once from `cfg`) used by every
hand's own Arm Length position-compensation math, so making them
per-hand-interpolatable would need a materially larger change (a
per-hand cloneBaseQuat plus touching that compensation math) than this
feature asked for. Checked that every saved pose in the user's own
previously-pasted Copy Settings dump has modelRotX/Y/Z at exactly 0,
making this a low-impact exclusion in practice.

New generic (parameterized) dev-panel widgets: `buildGenericRangeBarWidget()`
and `buildGenericCurveWidget()`, added alongside (not replacing) the
existing project-specific Arm Length/Wrist Splay widget builders -- this
round needed 8 more widget instances (4 controls x 2 triggers), enough
that hand-copying a 3rd/4th/5th/6th time was judged worse than
parameterizing, unlike the single-instance Wrist Splay round 2 entries
above, which deliberately kept its own dedicated (duplicated) widgets.
The curve widget carries right-click-to-delete-point from the start (the
2nd, separate part of this session's own opening request, applied to the
original Arm Length curve widget too -- see that entry's own commit).

2 real bugs caught via live testing (a `node --check` pass alone can't
catch a temporal-dead-zone violation -- both were only visible once the
page was actually loaded in the browser):
- The very first module-load-time setup call (`CLICK_HOLD_KEYS.forEach(...)`,
  positioned near the other widgets' own setup calls early in the file)
  referenced `CLICK_HOLD_KEYS`, a `const` declared much later in the file
  alongside the rest of the state machine -- a ReferenceError, since a
  top-level `const` is not hoisted the way a `function` declaration is.
  Fixed by moving the setup call itself down to right after
  `clickHoldPoseTriggers`'s own declaration (where `buildClickHoldPoseWidgets`
  is defined), rather than trying to hoist the const earlier.
- The same call also invoked `parseClickHoldConfig()`, which reads
  `clickHoldPoseTriggers` -- same TDZ problem, same fix (moving the call,
  not the declaration).

Live-verified via `window.__debug.updateRenderOrder()` (direct calls,
bypassing the Browser pane's own rAF-suspends-while-hidden limitation)
plus real `PointerEvent` dispatch on `window` (exercising the actual
mousedown/mouseup listeners, not a hand-reconstructed simulation):
- Correct idle -> forward -> retransition -> idle phase transitions per
  hand, confirmed via `hand._chp.chp.phase` at each stage.
- Real pose interpolation, not just a state-flag flip: injected 2
  distinct test poses (curlIndex 0 vs. 90) via `cfg.savedPoses`, measured
  the `rIndex1` bone's quaternion after each full transition, and found
  an 81-degree delta between them -- confirms the target value genuinely
  drives the bone, not a fixed animation.
- Distinct per-hand start delays: sampled `forwardDelay` across 6 hands
  mid-hold (299/289/276/262/246/229 ms) -- confirms the distance-based
  stagger computes a real per-hand value, not one shared number.
- The "Saved Poses" list-picker's own Save/Delete/Rename/Overwrite
  actions didn't originally propagate to the 2 new Target Pose dropdowns
  (a `select`-type control's `<option>` list is only rebuilt on an
  explicit `refreshSelectOptions()` call, confirmed by reading
  `devPanel.js`'s own doc comment for that exported function) -- fixed by
  wiring `savedPoses`'s own `onChange` to call
  `refreshSelectOptions('chpTargetPose')` / `refreshSelectOptions('rchpTargetPose')`.
- Zero new console errors after all of the above (3 stale
  `CLICK_HOLD_KEYS`/`clickHoldPoseTriggers`/`ERR_CONNECTION_RESET`
  entries lingered from the 2 TDZ bugs above and an earlier failed
  navigation, confirmed stale by the unchanged 510/510 hand count and a
  fully clean reload afterward).

Cache-buster bumps: `index.html`'s own `main.js?v=45` -> `?v=48` (3
intermediate bumps during the TDZ-bug live-debug loop above, `?v=48` is
the final, verified, pushed value).

--------------------------------------------------------------------------------
2026-09-14  -  07:10:07 AM - 07:25:16 AM EDT (measured, this entry's own start/finish)

2 direct user follow-ups on Click Hold-Pose (previous entry): "I dont see
any of the saved poses in the dropdown for Click Hold Pose Target pose
selection. fix that" and "also in both of the new groups, provide a
checkbox to turn that feature on and off."

**Target Pose dropdown fix.** Root cause: a devPanel.js `select` control's
`<option>` list is only ever populated by an explicit `refreshSelectOptions()`
call. The 2 Target Pose dropdowns (`chpTargetPose`/`rchpTargetPose`) were
only wired to refresh reactively, on a FUTURE Save/Delete/Rename/Overwrite
of a saved pose (via `savedPoses`'s own `onChange`) -- never for poses that
already existed in `cfg.savedPoses` at page load. Fixed with a one-time
refresh call placed right after the Click-Hold-Pose state machine's own
`const` declarations (`clickHoldPoseTriggers` etc.) genuinely exist.

**Master on/off checkboxes.** Added `chpEnabled`/`rchpEnabled` (label:
"<Group Title> (Master On/Off)", default off) as the first control in each
group, gating `startClickHoldPose(p)` with a single `if (!cfg[`${p}Enabled`])
return` -- same master-toggle convention as Crop Wrist/Responsive Wrist
Splay.

**A real, only-partially-root-caused bug found during this round's own
live testing:** calling devPanel.js's `refreshSelectOptions('rchpTargetPose')`
throws inside its own `commit()`/`fillSelectOptions()` -- isolated via
repeated single-key testing in a fresh browser tab (`refreshSelectOptions('chpTargetPose')`
alone always succeeds; `refreshSelectOptions('rchpTargetPose')` alone
always throws, both calls otherwise structurally identical, same control
shape, same code path) but NOT fully root-caused to a specific line inside
devPanel.js despite extensive isolation (disabling the call entirely
fixes the load; the two controls' own definitions and registration are
byte-for-byte parallel). Given the user's own explicit instruction mid-
round ("dont verify, just fix and push") to stop the live-debugging loop,
resolved pragmatically rather than exhaustively: added a
`safeRefreshSelectOptions(key)` wrapper (try/catch + `console.error`) and
routed every `refreshSelectOptions()` call for these 2 keys through it, so
this mystery can never again take down the whole page load regardless of
its true cause. Flagged in PROJECT_PROGRESS.md's "What's next" for a
proper root-cause pass later, since a caught-and-logged crash is a
mitigation, not a fix, and the underlying devPanel.js issue could affect
some other, not-yet-discovered `select` control the same way.

Also found and fixed, during the same live-debugging session, that a test
tab can silently degrade after many repeated reloads under real crash
conditions (WebGL context accumulating `GL_INVALID_FRAMEBUFFER_OPERATION`
errors until Chrome stops reporting them entirely, "too many errors, no
more errors will be reported to the console for this context") -- a
console.log-based debug marker placed in the crashing tab never surfaced,
even though the exact same code verifiably ran fine (confirmed via the
same marker) in a freshly-opened tab. Not a code bug, but a new addition
to this project's own environment-quirks list: a tab that has crashed
and reloaded many times in one debugging session should not be trusted
for a "did my fix work" check -- open a fresh tab instead once a session
has accumulated many failed reloads.

Cache-buster bump: `index.html`'s own `main.js?v=48` -> `?v=58` (multiple
intermediate debug-marker/isolation-test bumps during the live-debugging
loop above; `?v=58` is the final, pushed value -- NOT independently
re-verified live in the browser per the user's own explicit instruction
to stop verifying and just push; `node --check` syntax validation was
still performed before every push).

--------------------------------------------------------------------------------
2026-09-14  -  Actual: NOT MEASURED (no start timestamp captured this round)

Direct request: "In Pose, the Default button should be next to the Save
overwrite etc etc. When i click it sets the selected pose as the default
pose. So its the pose on startup, as well as the pose that retransitions
default back to." Followed by 2 mid-turn clarifications: "and set
transition speed maxes to 700" / "as in the avialble slider range" / "not
the actual input setting" (confirms: the SLIDER's max bound, not its
default value) and "dont verify just fix ad push."

**Default button relocated + entirely redefined.** It used to be its own
DEV_GROUPS row in the Pose group ("next to Saved Poses"), resetting every
pose slider to its hardcoded code default. Now it's injected directly
into the Saved Poses list-picker's own button row (`buildPoseDefaultButton()`,
next to Save/Overwrite/Use/Rename/Delete) via plain DOM manipulation --
devPanel.js's generic list-picker builder has no config hook for an extra
button, and this project's own file-map convention says not to fork that
shared engine, so this follows the SAME "find-and-augment an existing
devPanel.js-built row" pattern the Arm Length/Wrist Splay/Click-Hold-Pose
custom widgets already use, rather than adding devPanel.js UI surface.

Clicking it now reads the currently-selected item via devPanel.js's own
`row.__item`/`.dp-list-picker-row-selected` DOM convention
(`getSelectedSavedPoseItem()` -- no devPanel.js change needed, that
convention was already there for its own internal use, just not
previously read externally), applies its values to every live Pose
slider + re-poses the field (`setSelectedPoseAsDefault()`, same
syncValue()+onWholeHandRotationChange() 2-step the old button used), and
calls a NEW `saveCurrentSettings()` export from devPanel.js to trigger a
real persisted save immediately (no separate manual Save/Sync click
needed) -- satisfying "the pose on startup" via this project's own
ALREADY-EXISTING restore-from-localStorage mechanism, no bespoke startup
logic required. `saveCurrentSettings()` is a genuinely new devPanel.js
export (not a UI change, one small addition to its host-hook surface,
mirroring the existing `syncValue()`/`refreshSelectOptions()` pattern):
`initDevPanel()`'s own internal `saveSettings()` closure now assigns
itself to a module-level `saveSettingsRef` the moment it's defined, and
the new export calls that reference if set.

"The pose that retransitions default back to": replaced the FROZEN
`POSE_KEY_DEFAULTS` constant (computed once from code `def` values, never
changes) at both of Click-Hold-Pose's own retransition-target call sites
with a new mutable `poseDefaultValues`, seeded from `cfg`'s OWN restored
Pose values right after `initDevPanel()` runs (so it already matches
"the pose on startup" the instant the page loads, by construction, no
separate seeding logic) and updated live by `setSelectedPoseAsDefault()`
so an in-progress or future retransition targets the new default
immediately, without waiting for a reload. `POSE_KEY_DEFAULTS` itself is
UNCHANGED and still used -- purely for its original, narrower job (the
per-key fallback when a saved/partial pose object is missing a key, e.g.
an older pose predating a newer slider), a genuinely different concept
from "what pose does retransition/startup use" that this round
deliberately did not conflate.

**Transition speed max lowered to 700ms.** `chpTransitionSpeedMs`/
`rchpTransitionSpeedMs`/`chpRetransitionSpeedMs`/`rchpRetransitionSpeedMs`
sliders' own `max` bound (the available slider RANGE, confirmed via 2
follow-up clarifications, not the default VALUE) lowered from 5000 to
700; each slider's own `def: 400` is unchanged and already sits under the
new ceiling, so no existing saved value gets silently clamped.

Per direct instruction ("dont verify just fix ad push"), this round was
NOT independently re-verified live in the browser -- `node --check`
syntax validation was performed on both changed files before pushing,
and the implementation was reasoned through statically against
devPanel.js's own existing, already-verified conventions (`row.__item`,
`.dp-list-picker-row-selected`, `syncValue()`'s documented semantics)
rather than live-tested end-to-end. No start timestamp was captured for
this round (see this entry's own header) -- logged to ETA History with
`Actual: NOT MEASURED`, per this project's own "never fabricate a
duration" rule.

Cache-buster bumps: `index.html`'s own `main.js?v=58` -> `?v=59`;
`main.js`'s own `devpanel.js?v=12` -> `?v=13` (via its import line).

--------------------------------------------------------------------------------
2026-09-14  -  07:59:12 AM - 08:07:37 AM EDT (measured, this entry's own start/finish)

Direct user report: "Works overall. For poses, the thumb pose doesnt
seem correct. I believe it has to do with the rotation that we had
used. Double check in reference to hando to see what the issue is."

**Root cause, found by direct comparison against HANDO (this project's
own reference implementation, sharing the identical rig asset):** this
project's own `applyAllFingerPoses()`, `previewPosePreset()`, and
`applyPoseValuesToHand()` all posed FINGERS before the WRIST in a single
combined call. `rThumb1`'s own PARENT bone is `rHand` -- the exact bone
`applyWristPoseToSkeleton()` rotates. `rotateOnTrueWorldAxis()` (used by
every finger's own Curl/Splay/Splay2) converts its world axis into the
bone's CURRENT local space via the bone's FULL world quaternion, which
depends on the ENTIRE parent chain's CURRENT `matrixWorld` -- so posing
the thumb before the wrist reaches its own new target rotation reads
whatever wrist rotation was left over from BEFORE that call, not the one
about to be set. The other 4 fingers are unaffected (not parented
directly to `rHand`), which is exactly why only the thumb looked wrong.

**This is the identical bug HANDO already found, diagnosed, and fixed**
(its own CHANGELOG.txt documents the same root cause, found via a direct
user report there too: "From Scissor 1.5 to Scissor 1 - Wrong (thumb
curled/splayed too much). On a second click, its even worse") -- it was
simply never ported here when this project's own `applyCurlToSkeleton`/
`applyWristPoseToSkeleton` were adapted from HANDO's code. Fixed at all
3 combined-call sites by swapping the order (wrist first, fingers
second): `applyAllFingerPoses()` (rebuild, Whole-Hand Rotation changes),
`previewPosePreset()` (Pose Preview / "Use" on a saved pose), and
`applyPoseValuesToHand()` (Click-Hold-Pose's own per-hand application).

**Live-verified with a real, quantitative before/after measurement** (not
just a visual check): with `wristBend=45`/`wristSplay=-20`/`thumbCurl=90`
applied to a freshly-rebuilt (bind-pose) skeleton -- the exact scenario
that triggers the bug, since a fresh skeleton's `rHand` starts untouched
-- captured `rThumb1`'s quaternion right after the rebuild, then
independently re-applied wrist bend/splay and thumb curl via their own
SEPARATE, guaranteed-correct individual slider calls (each one only ever
touches its own bone, no ordering ambiguity possible) to get a reference
quaternion. With the OLD (fingers-then-wrist) order, the rebuild's own
result differed from the reference by 33.0 degrees -- a real, large,
measurable error. Restored to the OLD order specifically to CONFIRM this
number (ruling out "the test just isn't sensitive enough to catch a
real bug" as an explanation for why it might read 0), then re-applied
the fix and re-ran the identical test: exactly 0.0 degrees of error.
510/510 hands still rendering (varied intentionally to 180 via the
Field Layout rebuild trigger used for this test, not a regression), zero
new console errors.

Cache-buster bump: `index.html`'s own `main.js?v=59` -> `?v=62` (2
intermediate bumps during the before/after live-comparison test above;
`?v=62` is the final, verified, pushed value).

--------------------------------------------------------------------------------
2026-09-14  -  08:19:47 AM - 08:32:10 AM EDT (measured, this entry's own start/finish -- includes an interleaved re-investigation of a separate user bug report, logged as its own entry above)

**Re-investigated the retransition-thumb bug report once more first** (a
NEW, more specific report arrived mid-round: "the thumb error is still
an issue... Startup pose is correct while the retransitioned pose is
incorrect even though they should be exactly the same"). Added
`poseDefaultValues`/`setSelectedPoseAsDefault`/`getSelectedSavedPoseItem`
to `window.__debug` (none were exposed before) and ran the most
realistic test possible: injected a fake selected list-picker row, called
the REAL `setSelectedPoseAsDefault()` button logic, persisted it through
a genuine page reload, then ran a real Click-Hold-Pose trigger/release
cycle varying all 5 thumb-specific keys plus wrist bend/splay together --
0.0 degree delta between true startup and post-retransition pose, same
as every earlier test. User confirmed a hard refresh didn't change the
symptom, ruling out stale cache; per the user's own choice, this was set
aside (not resolved) to proceed with the new feature below -- still an
open item, see PROJECT_PROGRESS.md's "What's next".

**Added "Click Pose" / "Double-Click Pose"** -- 2 new dev-panel groups,
a fire-and-forget variant of Click Hold-Pose (direct follow-up request).
Unlike Click Hold-Pose (hold to transition, release to retransition), a
single click or double-click starts every hand's own FULL sequence at
once: transition to target, PAUSE at the target for a configurable
duration (new "Pause Duration At Target (Ms)" slider -- a single flat
value, not a curve+range pair, per the request's own "*D* pause
duration*"), then retransition back to default -- the whole sequence
then runs to completion per-hand regardless of what the mouse does
afterward. Direct requirement, implemented literally: "Retransition
start time of a single [hand] does not consider whether every hand has
completed their transition. It is independent to that hand specifically"
-- each hand's own pause-end and retransition-stagger are computed from
THAT hand's own live cursor distance at the moment IT specifically
finishes its own pause, never from a shared/global snapshot. Both
triggers sit on the LEFT mouse button, distinguished from each other by
click COUNT (not left/right button the way Click Hold-Pose's own 2
groups are) -- reuses the Mouse Tracking Log's own existing
`MOUSE_LOG_MULTICLICK_MS` (350ms) debounce window for click-vs-double-
click disambiguation, rather than inventing a new threshold. Reuses
every existing piece of Click-Hold-Pose's own machinery directly
(`computeStartDelayMs`, `lerpPoseValues`, `applyPoseValuesToHand`,
`poseDefaultValues`) -- the only genuinely new code is the 3-phase
(forward -> paused -> retransition, one more phase than Click Hold-
Pose's own forward/retransition pair) per-hand state machine and the
click-count trigger detection.

**Also disabled OrbitControls' own Pan while a Click-Hold-Pose trigger
is active** (direct request: "moving the cursor should not trigger any
panning [during a hold]... it should continue holding the held state").
This project's OrbitControls config maps BOTH left and right mouse
buttons to Pan -- the exact same buttons Click Hold-Pose's own
pointerdown/pointerup listeners use, so a hold-and-drag was panning the
camera underneath the pose transition. `controls.enablePan` now toggles
false on `startClickHoldPose()`, restored true on `endClickHoldPose()`
only once NEITHER trigger (chp/rchp) is still active. Confirmed, by
reading `animate()`'s own code (not assumed), that cursor-tracking
rotation (`hand.wrapper.quaternion`) is a fully separate per-frame step
that never went through OrbitControls to begin with -- it already keeps
following the cursor throughout ANY pose-transition state, including
mid-transition, matching the request's own follow-up clarification
("even a hand in mid transition should continue to rotate to follow the
cursor") with zero code change needed for that specific part.

Live-verified: the full forward->paused->retransition->idle phase
sequence, with the target pose's own real values genuinely applied at
each stage (not just a flag flip); click vs. double-click correctly
disambiguated (2 rapid clicks enter only the dblclick phase, click stays
idle); Pan correctly disabled during a hold and restored on release. All
4 new curve/range widgets and the Pause Duration slider confirmed
constructed (SVG/slider elements present in each row). Zero new console
errors, 510/510 hands still rendering.

Cache-buster bumps: `index.html`'s own `main.js?v=62` -> `?v=64` (`?v=63`
was the debug-exposure-only intermediate step for the re-investigation
above).

--------------------------------------------------------------------------------
2026-09-14  -  08:37:53 AM - 08:50:24 AM EDT (measured, this entry's own start/finish)

**Finally root-caused the "thumb pose still looks wrong" saga** -- the
user's own follow-up finally pinned it down: "thumb pose of all poses
except startup still looks wrong," and, once asked, "i am using imported
poses from Hando so i cant [re-save a fresh pose to test]." That single
fact reframed the whole investigation: every prior test this session
used SYNTHETIC test poses with modelRotX/Y/Z left at 0, which never
exercised the actual gap.

**Root cause:** Click-Hold-Pose and Click-Pose's own `lerpPoseValues()`
deliberately EXCLUDED modelRotX/Y/Z from interpolation -- a disclosed
scope decision from when Click-Hold-Pose was first built, since those 3
keys drive a single SHARED `cloneBaseQuat` used by every hand's own Arm
Length position-compensation math, and making them per-hand was flagged
at the time as "a materially larger change than requested." A pose
imported from HANDO commonly bakes nonzero Whole-Hand Rotation into the
gesture itself (unlike the user's own native poses, previously checked
and found to universally have modelRotX/Y/Z at 0 -- that check simply
never covered imported poses). During a transition to such a pose, the
finger/wrist articulation correctly animated to the target, but the
finger CURL AXES stayed pre-rotated by the OLD, unchanged SHARED basis
the whole time -- anatomically wrong the instant a pose implies a
different whole-hand orientation than the field's current shared
sliders say. "Startup" (nothing ever transitioning) never showed the
gap since there was nothing for it to diverge from -- confirmed via
direct test: mid-transition, `cfg.modelRotY` measured unchanged (still
0) throughout, while the target pose's own modelRotY was 60.

**User explicitly chose the full fix over 2 smaller alternatives**
(asked directly, since properly fixing this means revisiting the
original disclosed architectural tradeoff): "Make it interpolate
per-hand like everything else," over "snap it instantly, no smooth
transition" or "leave as-is, re-tune poses instead."

**Implementation:** each hand now gets its own `currentBaseQuat` (set at
creation in `rebuildField()`), kept identical to the single shared
`cloneBaseQuat` every frame for any hand NOT currently mid a pose-
transition -- zero behavior change for ordinary Whole-Hand Rotation
usage, verified directly (0.0 degrees of error against an independently
computed reference). `lerpPoseValues()` now genuinely interpolates
modelRotX/Y/Z like every other key (the exclusion is gone); a new
`computeBaseQuatFromValues()` helper (same formula as
`updateCloneBaseQuat()`, just reading a passed-in values object instead
of live `cfg`) computes a per-hand basis from a transition's own
currently-interpolated modelRot, written into `hand.currentBaseQuat`
inside `applyPoseValuesToHand()`. `applyHandArmLength()` now reads the
per-hand basis instead of the shared one directly, accepting a
disclosed ~1-frame lag on the ARM-LENGTH POSITION specifically during an
active transition (it runs before this frame's own pose-override step
updates `currentBaseQuat`) -- imperceptible at 60fps, and avoids
restructuring the per-hand render-order loop's own established bone-
posing sequence just to eliminate a sub-16ms lag on a position, never
the fingers (which read the fresh value the same frame they're posed).

**Live-verified across the full lifecycle:** a target pose with
modelRotY=60 rotates the hand's own ACTUAL rendered orientation
(`hand.clone.quaternion`) by exactly 60.0 degrees during the transition
(previously 0), while the shared field-wide `cfg.modelRotY` stays
untouched the entire time; a hand never touched by any trigger stays
exactly synced to the shared basis (0.0 degrees delta); after a full
retransition cycle completes, a hand's own basis correctly reverts to
match the shared/default orientation again (0.0 degrees); ordinary,
non-transition slider-driven Whole-Hand Rotation was independently
re-verified afterward and measures exactly 0.0 degrees of error against
a freshly, independently computed reference quaternion -- confirming no
regression to the feature this change had to touch. Zero new console
errors, 510/510 hands still rendering throughout.

Cache-buster bump: `index.html`'s own `main.js?v=64` -> `?v=65`.

--------------------------------------------------------------------------------
2026-09-14  -  09:39:00 AM - 09:53:36 AM EDT (measured, this entry's own start/finish)

Direct user suggestion broke the "thumb still wrong" case wide open:
"maybe check hando for pose settings. maybe there is an extra setting
you dont have." Direct comparison against HANDO's own Pose group found
exactly that: HANDO has a per-finger "Base-Only Curl" slider (5, one per
finger) that this project's own Pose-group port had deliberately omitted
-- disclosed at the time ("don't exist as controls in this project
yet") but never circled back to. A pose imported from HANDO using Base-
Only Curl on the thumb had that data silently dropped -- this project
had no control or key to store or apply it at all.

**Ported Base-Only Curl from HANDO exactly.** Added `baseOnlyCurl{Finger}`
sliders (5, matching HANDO's own `min:-200,max:200,def:0`) in the same
position HANDO uses (right after Curl Bias, before Tip Twist); a new
`FINGER_BASE_ONLY_CURL_KEY` map; the same additive, base-joint-only
rotation in `applyCurlToSkeleton()` HANDO itself uses (a 2nd
`rotateOnTrueWorldAxis` call on joint 0 only, same curl axis, composing
on top of whatever regular Curl+Bias already did); added all 5 keys to
`POSE_PRESET_KEYS` (so saved/imported poses capture and restore them)
and to their own per-finger Pose subgroups. `values[key] || 0` guards
against `NaN` for any older saved pose predating this key.

**Separately, a genuine, correctly-diagnosed interference bug:** direct
report, "for click hold, when i release, it seems to trigger the correct
release, but then it calls it again. I suspect it is click hold
interferring with click triggers." Releasing a Click-Hold-Pose hold
fires the same native `pointerup` Click Pose/Double-Click Pose also
listens for on the left button; that listener had no way to tell a
hold-release apart from a genuine standalone click, so every release
also fired Click Pose's own sequence on top. Fixed with a
`lastPointerupWasHoldRelease` flag set by the (earlier-registered)
Click-Hold-Pose pointerup listener and read by the Click Pose listener.

**That first fix introduced a real regression, caught immediately by
the user:** "i tested the new build and now i cant trigger click nor
double click. i thinkn the click hold trigger probably overrides it" --
correct again. Click-Hold-Pose has no minimum press duration of its own
(`trig.active` goes true the instant the button goes down), so an
instant click -- with both features enabled together -- ALSO left
`.active` true right up until release, making the original fix suppress
EVERY click, not just genuine holds. Corrected by requiring the hold to
have actually lasted a perceptible duration (reusing
`MOUSE_LOG_HELD_DRAG_MS`, the Mouse Tracking Log's own existing "quick
tap vs. real hold" threshold) before treating a release as suppressible.

Live-verified: Base-Only Curl produces a clean, isolated base-joint
rotation (45deg on rThumb1, exactly 0deg on rThumb2, with every other
thumb slider genuinely zeroed); a Click-Hold-Pose transition to a pose
with `baseOnlyCurlThumb` set now measures 0.0 degrees of error against
the "Default" button applying the identical pose; a quick (~50ms) click
correctly enters Click Pose's own 'forward' phase; a genuine sustained
(~700ms) hold still correctly suppresses the extra click. Also
separately investigated a user report of cursor-tracking sticking near
the browser edge -- direct measurement found `cursorTarget` continues
moving smoothly and continuously all the way to the true last-pixel
edge (NDC reaching 0.9984), no sign of a code-level threshold; flagged
to the user as likely a physical mouse/monitor-edge limit or a dev-
panel-position interaction, pending more specifics.

**Despite all of the above, the user reports the thumb is STILL wrong**
for Click-Hold/Click-Pose transition targets and for what a retransition
lands on, even though the identical pose applied via "Default" looks
correct. This is now the 4th round of exhaustive, verified-correct
testing (wrist-order fix, per-hand Whole-Hand Rotation, Base-Only Curl,
each independently measuring exactly 0.0 degrees of error against a
"Default"-equivalent reference) without reproducing what the user is
actually seeing. Genuinely blocked without the user's own actual saved-
pose JSON (Copy Settings) and a screenshot -- see PROJECT_PROGRESS.md's
"What's next" for the explicit ask.

Cache-buster bumps: `index.html`'s own `main.js?v=65` -> `?v=68` (2
intermediate debug-exposure bumps for live investigation).

--------------------------------------------------------------------------------
2026-09-14  -  10:03 AM - 10:54 AM EDT (approximate, spanning several interleaved user exchanges; not independently re-measured per sub-task)

**THE "THUMB POSE LOOKS WRONG" SAGA IS FINALLY, GENUINELY RESOLVED --
root cause found, and it was never a bug in any of the code this
session touched.** Root cause: **Responsive Wrist Splay was enabled**
in the user's own session. That feature adds a live, continuously-
recalculated wrist rotation based on cursor distance, independent of
whatever pose is active -- and since it recomputes every frame during
an active Click-Hold-Pose/Click-Pose transition (while "Default"'s own
one-time application doesn't include it at the instant it's clicked),
any cursor movement between "when Default was set" and "when a
transition runs" changes the live extra splay, producing a real,
visible thumb shift completely independent of which pose was
targeted. Confirmed decisively by the user's own 2 tests: (1) setting
the Click Pose target to the SAME pose as Default still produced an
immediate thumb shift on click -- impossible if the bug were in pose
DATA, since interpolating between 2 identical states is a mathematical
no-op; (2) "every other finger is in the correct pose, its just the
thumb" -- the thumb is the ONLY finger parented to the wrist bone
(`rHand`), so it's the ONLY finger any extra wrist rotation can ever
visibly affect. Disabling Responsive Wrist Splay confirmed the fix
("yeah thats it. good catch.").

**This means every one of the real, verified fixes shipped across this
entire investigation (wrist-before-fingers ordering; per-hand Whole-
Hand Rotation during transitions; HANDO's own Base-Only Curl, ported
after being found missing entirely; 2 rounds of Click-Hold/Click-Pose
interference) were genuine, real bugs, independently confirmed correct,
and NONE of them were the actual cause of what the user kept
reporting.** All 4 remain correctly fixed and still matter for other
scenarios (imported poses that DO use Whole-Hand Rotation or Base-Only
Curl, for instance) -- this entry does not retract any of them, it
closes out the ONE thread that kept resurfacing across all of them.

Also shipped in this same stretch, before the root cause was found:

**Decoupled cursor target X/Y from Cursor Target Depth** (direct
request, after a live-quantified explanation of the old parallax
behavior with real numbers: a corner hand needing to face rightward
despite the cursor being unambiguously further left on screen). X/Y now
always come from a raycast to a plane permanently fixed at z=0 (the
hands' own depth -- "the true cursor xy"); Z is set directly from
`targetDepthFactor * fieldRadius` as a plain scalar, fully decoupled.
`targetPlane` no longer needs `updateTargetPlane()` to track the
configured depth (removed entirely, 2 call sites cleaned up) since both
axes are now computed fresh, independently, every frame. Live-verified:
X/Y at a fixed screen position now exactly matches a raycast to z=0
(previously scaled down by the depth-shift); Z still correctly reflects
targetDepthFactor.

**New, known, ACCEPTED side effect of that same fix (fix deferred,
explicitly grouped with a future round per direct request, "dont do
that right now. group it with the next changes i ask for"):** the
Target Marker (the visual dot showing `cursorTarget`) no longer stays
pixel-aligned with the true mouse position when Target Depth isn't 0 --
because the marker's own position IS `cursorTarget`, and that point is
no longer guaranteed to lie ON the original camera ray now that Z is
set independently of X/Y (previously it always did, by construction,
since every axis came from one single raycast hit). Planned fix
(NOT YET IMPLEMENTED): give the Target Marker its own position, driven
by the TRUE raycast-to-actual-depth hit (always pixel-perfect under the
cursor), fully independent from `cursorTarget` (which keeps driving the
hands with the new decoupled X/Y-depth behavior).

**Click Pose's own 350ms debounce delay removed when Double-Click Pose
is disabled** (direct report: "why is there a delay between the click
and the triggered pose transition, even if the minimum transition time
is set to 0"). Every click used to wait the full multi-click debounce
window before triggering, even with no double-click feature enabled to
disambiguate against -- pure, avoidable latency in that case. Now fires
immediately when `cfg.dblclickEnabled` is false. (Separately explained,
no code change: Click Hold-Pose's own delay source, when present, is
"Pose Transition Min/Max Start Time" -- defaulting to up to 300ms --
which is independent of "Transition Speed" and easy to conflate with
it.)

**Added a "Diagnose" button** (Saved Poses list-picker, next to
"Default") -- a TEMPORARY debugging aid built specifically to get real
data from the user's own actual saved poses rather than this session's
own repeatedly-inconclusive synthetic approximations. Runs the "Default
path" vs. "transition path" comparison directly on whichever pose is
currently selected, reporting each finger's base-joint mismatch via
`alert()`. Not actually needed in the end (the root cause was found
through the user's own 2 decisive manual tests before the button was
ever used) -- left in place for now since it may still be useful for a
future investigation; consider removing it once confirmed unneeded long-
term.

Cache-buster bumps: `index.html`'s own `main.js?v=68` -> `?v=71` across
this stretch.

--------------------------------------------------------------------------------
2026-09-14  -  11:2x AM EDT (start not captured -- Actual: NOT MEASURED)

**Responsive Wrist Splay now freezes for the duration of an explicit pose
transition, instead of fighting with it.** Direct follow-up question after
the thumb saga's resolution above: "is there a way around it such that i
can have both functioning correctly." Investigated the actual mechanism
(not just the symptom) before proposing anything: `applyWristPoseToSkeleton()`
already composes the pose's own `wristSplay` and Responsive Wrist Splay's
`extraSplayDeg` into ONE `rotateZ()` call, fully resolved BEFORE any finger
(including the thumb) is posed relative to the wrist -- so "pose the thumb
first, then apply splay" was already mathematically true; reordering it
further couldn't have changed anything. The real cause: `extraSplayDeg`
was being recomputed **live, every single frame**, for the entire duration
of a Click-Hold-Pose or Click-Pose/Double-Click-Pose transition -- so any
cursor movement during the hold/transition kept shifting the wrist target
out from under the pose's own interpolation, visibly as a moving thumb
(the one finger parented to the wrist bone).

Fix: capture `extraSplayDeg` ONCE, at the exact instant a transition
triggers (`chp.frozenSplayDeg` for Click Hold-Pose, set alongside its
existing `forwardDelay` capture in `updateClickHoldPoseForHand()`;
`cp.frozenSplayDeg` for Click/Double-Click Pose, set alongside its existing
per-hand `forwardDelay` in `triggerClickPose()`), then reuse that one
frozen value through every phase of that hand's own sequence (forward,
pause, retransition) instead of calling `computeResponsiveWristSplayDeg()`
again each frame. The per-frame idle-loop path (`updateRenderOrder()`'s own
un-overridden branch) is untouched -- an idle hand keeps tracking
Responsive Wrist Splay fully live, exactly as before. Live-verified in
browser: with Responsive Wrist Splay enabled and a Click Pose target equal
to the current pose (the user's own diagnostic technique from the thumb
saga above), `frozenSplayDeg` was confirmed nonzero and IDENTICAL before
and after moving the cursor far away mid-'forward'-phase, while a
different, untriggered ("idle") hand's own wrist bone quaternion was
confirmed to still change in response to the same cursor move -- both
behaviors now coexist as intended.

Cache-buster bump: `index.html`'s own `main.js?v=71` -> `?v=72`.

--------------------------------------------------------------------------------
2026-09-14  -  12:0x PM EDT (start not captured -- Actual: NOT MEASURED)

**THE ACTUAL, FINAL ROOT CAUSE of the "thumb pose looks wrong" saga --
found and fixed, independent of cursor movement.** Direct follow-up
report after the previous entry's freeze fix: "doesnt work. thumb still
messed up" -- then, when asked whether it happens with the cursor held
still: "yeah im not talking about moving during the transition. im just
talk about regular static click, duble click or clickhold." This ruled
out the previous entry's own diagnosis (Responsive Wrist Splay
recomputing live DURING a transition) as the explanation, since with a
genuinely static cursor that recompute produces the identical value every
time regardless of freezing -- the freeze fix was real and still correct
for cursor-movement cases, but wasn't the (whole) story.

Found by reading `updateRenderOrder()`'s own idle branch (the per-frame
path for a hand NOT in any active pose transition) line by line: it
re-applies the wrist bone's live Responsive Wrist Splay rotation EVERY
SINGLE FRAME, but never re-applies finger curl -- curl is normally only
recomputed on a slider change or "Default" click
(`applyAllFingerPoses()`), not every frame. `rotateOnTrueWorldAxis()`
converts a curl's intended WORLD-space axis into a LOCAL bone rotation
using `bone.getWorldQuaternion()` -- the bone's CURRENT parent-chain
world orientation at the exact instant curl is computed (same mechanism
as the original wrist-before-fingers fix, many rounds ago). `rThumb1` is
the only finger bone parented directly to the wrist bone (`rHand`), so
it's the only one whose local rotation goes stale the moment the wrist
keeps rotating afterward -- exactly matching "every other finger is
correct, it's just the thumb," and exactly independent of cursor
movement (a nonzero live Responsive Wrist Splay value sits there
whether the cursor is moving or not). "Default" looks correct for
exactly the one frame it's applied (extraSplayDeg=0 in
`applyAllFingerPoses()`'s own call), then the wrist keeps moving via
live splay every frame after that while the thumb doesn't follow -- and
a hand returning to idle after any Click-Hold-Pose/Click-Pose sequence
shows the identical desync immediately, for the same reason.

Fix: the idle branch now also re-applies the THUMB's curl (only the
thumb -- the other 4 fingers aren't parented to `rHand` and are provably
unaffected, confirmed by the original wrist-ordering fix, so refreshing
all 5 every frame for every hand isn't needed and would cost real
performance at hundreds of hands for no benefit) immediately after the
wrist update, mirroring the same "wrist then fingers, together, every
frame" pattern `applyPoseValuesToHand()` already uses during an active
transition.

**Verified with a genuinely independent double-check, static cursor
throughout (no pointer events at all), reproducing the user's own exact
repro conditions:** built 2 separate reference computations for the
thumb's WORLD-space orientation at a hand with a large (90-degree) live
Responsive Wrist Splay swing -- (A) the real route: apply "Default", then
run 5 real idle frames via `updateRenderOrder()`; (B) an independent
reference: set the wrist directly to its final live value in one shot,
then apply thumb curl fresh against THAT, via the same exposed functions
called directly, not through the frame loop. Result: **0.0 degrees**
of world-space difference between A and B. Confirmed the test was
actually discriminating by reproducing the OLD (pre-fix) behavior the
same way (wrist-only per-"frame" update, thumb curl never refreshed) and
comparing against the same reference B: **12.5 degrees of real,
measurable error** -- a genuine, visually-significant bug, not a
rounding artifact, now fixed.

Also exposed 3 more functions on `window.__debug`
(`applyAllFingerPoses`, `applyPoseValuesToHand`, `cloneBaseQuat` getter,
`triggerClickPose`, `startClickHoldPose`, `endClickHoldPose`,
`updateClickPoseForHand`, `updateClickHoldPoseForHand`, `getOrInitHandCP`,
`getOrInitHandCHP`, `computeResponsiveWristSplayDeg`,
`applyWristPoseToSkeleton`, `applyCurlToSkeleton`, `FINGER_NAMES`,
`FINGER_JOINTS`) to make this and future investigations directly
testable without depending on real pointer-event timing or the Browser
pane's own rAF-suspends-while-hidden limitation.

**Environment note, not a code issue:** this round's own live-browser
verification was repeatedly blocked by the local static file server
(`python3 -m http.server`) intermittently truncating/resetting the
connection for `main.js` and `devpanel.js` specifically (confirmed via
direct `curl` retries outside the browser tool too -- same file,
same intermittent truncation, unrelated to any code change) -- worked
around by starting a completely fresh server instance on an alternate
port (`static-alt2`, 8422) rather than continuing to retry the
already-hammered one.

Cache-buster bumps: `index.html`'s own `main.js?v=72` -> `?v=75` across
this round (2 intermediate debug-exposure bumps).

--------------------------------------------------------------------------------
2026-09-14  -  21:03:32 - 21:14:39 EDT (11m07s)

**Set the user's own real Copy Settings dump as the project's code
defaults.** Direct request after the user pasted their full settings
JSON and asked to make it the default, then push. Translated every
listed value into `main.js`'s `DEV_GROUPS` `def:` fields: Field Layout,
Cursor Tracking, all 5 finger Pose subgroups + Wrist, Responsive Wrist
Splay's real range/curve, all 11 saved poses (Point/ThumbsUp/
MiddleFinger/Fist/Neutral/Neutral - Bent Back/Open Palm/Preflick3/
SCISSOR 1/Drag1/Fist - Bent Back), Camera, Lighting (Toon Shading/
Outline/Background/Debug already matched, no change needed there).

`makeClickHoldPoseGroup()`/`makeClickPoseGroup()` needed a real code
change, not just data: both factories previously had ONE shared
hardcoded `def:` per field, used identically for chp/rchp (one factory)
and click/dblclick (the other) -- fine when every instance wanted the
same generic default, but the user's real config needs 4 genuinely
different target poses and speeds (chp->Point, rchp->Neutral - Bent
Back, click->Open Palm, dblclick->ThumbsUp). Added an optional
`defaults = {}` parameter to each factory (backward compatible --
every field falls back to its old generic default via `??` when a key
isn't provided) and passed each of the 4 real call sites its own
`defaults` object.

Deliberately left `dp_*` Dev Panel chrome cosmetics (fonts/colors/
spacing) untouched -- they live in the shared `devpanel.js` engine this
project's own convention says never to fork, and changing that code
default wouldn't even affect the user's own browser anyway (their
localStorage already overrides it).

**Verified live, not just written:** cleared `localStorage` on a test
port, hard-reloaded with a corrected top-level cache-bust (caught a
real miss mid-verification -- a bare `/` navigation served a STALE
`index.html` still pointing at `main.js?v=75`, exactly the kind of
staleness this project's own CLAUDE.md has warned about repeatedly;
fixed by navigating to the `?t=` cache-busted URL instead), then read
back every changed `cfg` value directly via `window.__debug` --
`fieldRows`/`fieldCols` (255-hand field, 15x17, confirmed by actual
hand count), all 4 click/hold trigger configs, Responsive Wrist Splay's
range, all 11 saved pose names, camera/lighting values, and
`poseDefaultValues.thumbCurl` correctly deriving 79 (Fist) -- every
single one matched the pasted dump exactly. A screenshot confirmed the
field renders as expected (a fist-shaped field fanning toward the
cursor).

Cache-buster bump: `index.html`'s own `main.js?v=75` -> `?v=76`.

--------------------------------------------------------------------------------
2026-09-14  -  (start not captured -- Actual: NOT MEASURED)

**The thumb-only idle-refresh fix from earlier this same day was real
but too narrow -- fixed to cover all 5 fingers.** The user tested on a
phone for the first time (guaranteed-clean cache, ruling out the
staleness this project has repeatedly hit) and reported the same
problem, "kind of slightly worse," with "the other fingers are all also
slightly uncurled" and the decisive confirmation: "if I turn off the
responsive wrist splay, then it's fine." The user immediately flagged
their own "other fingers" observation as possibly unreliable perception
-- but a direct, independent skeleton inspection (not the user's
perception) settled it: every finger's base joint is parented to its own
"carpal" bone (rCarpal1 for index, rCarpal2 for middle, rCarpal3 for
ring, rCarpal4 for pinky), and every one of those carpal bones is itself
a child of the wrist bone (rHand). The earlier round's belief that "only
the thumb is parented to the wrist bone" (stated by an even earlier
round of this saga and never re-verified) was wrong -- true only in the
narrow sense that the thumb attaches DIRECTLY while the other 4 go
through one intermediate carpal bone each, but every finger is still a
structural descendant of the wrist and therefore depends on the wrist's
current world orientation the same way `rotateOnTrueWorldAxis()` was
already established to require.

Fix: the idle branch's per-frame finger-curl refresh (added earlier
today for the thumb only) now loops over all of `FINGER_NAMES`.

**Verified live with the user's own real settings already loaded as the
project's defaults** (all 4 triggers enabled simultaneously, matching
their real config, not a simplified single-feature test): 0.0000 degrees
of drift for all 5 fingers across a 60-frame idle "just sitting there"
wait (confirms the idle-loop desync itself is now fully closed), and
0.0000 degrees of round-trip error for all 5 fingers across 11 hands
spread through the field after a real, full click-to-Open-Palm-and-back
sequence.

Performance note, not yet addressed: this refreshes 5 fingers' curl
(instead of 1) per idle hand per frame -- a real cost increase at
hundreds of hands, accepted for correctness; worth profiling later if
frame rate becomes a concern, not attempted this round.

Cache-buster bump: `index.html`'s own `main.js?v=76` -> `?v=77`.

--------------------------------------------------------------------------------
2026-09-14  -  (start not captured -- Actual: NOT MEASURED)

**THE ACTUAL, FINAL, REAL root cause of the entire thumb/finger-pose
saga, found via a decisive cross-project clue.** The user tested the
all-5-fingers idle-refresh fix on desktop and reported it was actually
WORSE than before -- "in Fist pose, every finger is less curled than it
was before when it looked like just the thumb" -- then, critically:
"i actually checked the same issue on Hando and it occurs there too. so
its not just a problem with our project. Its not resolved in hando
either." Hando is a completely separate codebase this session has never
touched -- that single fact ruled out every fix attempted across this
entire saga (wrist-ordering; per-hand Whole-Hand Rotation; Base-Only
Curl; Click-Hold/Click-Pose interference; the idle-loop refreshes) as
the actual cause, since none of that code exists in Hando. It pointed
directly at something the 2 projects structurally SHARE.

Found it in `applyCurlToSkeleton()`: the curl/splay axis conversion uses
`baseQuat` (the whole-hand's PRE-wrist orientation -- alignQuat + Whole-
Hand Rotation only) as its reference frame -- ported VERBATIM from
HANDO's own convention, with the code's own existing comment explicitly
noting this was deliberate, to match "HANDO's own anatomical intent
exactly." That shared convention itself is anatomically wrong: `baseQuat`
never includes the wrist bone's own current bend/splay rotation, yet
every finger's base joint is a descendant of the wrist bone (`rHand`,
via its own "carpal" bone -- see the same day's earlier correction).
Anatomically, curl direction should rotate WITH the wrist/palm -- closing
a fist still closes toward YOUR OWN palm no matter how your wrist is
bent. Using a wrist-independent axis means the same curl % increasingly
"misses" the real (rotated) palm the further the wrist moves away from
wherever a pose's curl values happened to be tuned by eye -- exactly
matching "if I turn off responsive wrist splay, it's fine" (extraSplay
is always 0 there, so the mismatch never appears) and "every finger [is]
less curled" (every finger shares this same axis convention -- fixing
only the thumb, as the previous round did, could only ever address a
fraction of the problem, which is exactly why it looked "worse": the
user was now comparing a correctly-tracking thumb against 4 fingers that
still weren't).

Fix: `applyCurlToSkeleton()` now reads the WRIST BONE's own current
world rotation (`wrapperQuat` excluded, the exact same technique
`rotateOnTrueWorldAxis()` already used) as the curl/splay axis reference,
instead of the static `baseQuat`. This already incorporates `baseQuat`
as its own prefix (the wrist bone is a descendant of the mesh clone that
`baseQuat` orients), so Whole-Hand Rotation's existing behavior is
completely unaffected -- only wrist bend/splay awareness is added on top.

**Verified with a genuinely independent, anatomically-meaningful test --
not another self-consistency check.** Measured the angle between a
curled finger's own segment direction and the wrist bone's own reference
axis, at `wristSplay=0` vs. `wristSplay=-70` (confirmed the wrist itself
rotated a real 70.000 degrees between the two): the finger-to-wrist angle
measured **78.006 degrees at BOTH**, proving curl now genuinely tracks
wrist rotation instead of staying fixed in world space -- the exact
anatomical invariant that should hold. Re-ran the full existing
regression suite (idle-drift + real click-transition round-trip, all 5
fingers, 11 hands spread through the field): still exactly 0.0 degrees,
confirming this change didn't regress either of the same-day per-frame
desync fixes.

**Disclosed, expected side effect:** this changes the rendered appearance
of any existing saved pose that has a nonzero `wristBend`/`wristSplay`
(their curl values were tuned by eye against the OLD, anatomically-
incorrect behavior) -- not a new bug, a direct and correct consequence
of fixing the actual reference frame. Not yet independently confirmed by
the user on a real device/browser at the time of this entry.

Cache-buster bump: `index.html`'s own `main.js?v=77` -> `?v=78`.

--------------------------------------------------------------------------------
2026-09-14  -  (start not captured -- Actual: NOT MEASURED)

**Cross-checked HANDO's own recent changes for pose-export
compatibility, per direct request ("can you look into hando's new
changes to make sure you two are matching since i will be exporting
poses from hando"), and found 2 real things to fix.**

**(1) The curl-axis fix from the previous entry used a DIFFERENT formula
than HANDO's own concurrent fix for the identical bug.** HANDO was
independently fixed the same day (its own commit `3262675`) for the
exact same root cause found here -- but via a different technique:
HANDO computes the wrist's LOCAL delta-from-its-own-rest rotation
(`wristRestQuat^-1 * wristBone.quaternion`) and composes it with
`modelRoot.quaternion` (delta applied first), instead of reading the
wrist bone's full WORLD quaternion the way this project's own fix just
had. Directly measured whether these 2 independently-derived
"wrist-relative" fixes actually agree: **they don't** -- `rHand`'s own
rest quaternion is far from identity
(`[-0.078, 0.688, -0.086, 0.716]`), so the 2 formulas diverge by a
measured 88.5 degrees for the same wrist state. Since the actual goal
here is pose-export PARITY with HANDO, not an independently-"correct"
convention, switched to HANDO's exact formula. Verified against 2
independent reconstructions of HANDO's own math (not by calling this
project's own code as its own reference): a direct formula comparison
and a full end-to-end bone-rotation reconstruction both measured exactly
0.0 degrees of difference from the real production output. Re-ran the
existing idle-drift + click-round-trip regression suite: still 0.0
degrees.

**(2) HANDO has a real feature this project had no concept of at all:
"Tip-Only Curl"** (4 sliders -- Index/Middle/Ring/Pinky, no thumb), the
mirror of Base-Only Curl (additive rotation on the LAST joint instead of
the first). Same failure mode as the earlier-this-saga Base-Only Curl
gap: a pose exported from HANDO using this slider would have had that
data silently dropped on import here. Ported: `FINGER_TIP_ONLY_CURL_KEY`
map, 4 new sliders (placed between Base-Only Curl and Tip Twist in each
finger's own subgroup, matching HANDO's own ordering), the additive
`rotateOnTrueWorldAxis()` call on the last joint, and the 4 new keys
added to `POSE_PRESET_KEYS` and each finger's subgroup. Verified in
isolation: setting only `tipOnlyCurlIndex` to 60% rotated ONLY the last
joint (`rIndex3`) by exactly 42.0 degrees (70-degree max * 0.6, matching
`FINGER_MAX_DEG.index[2]`) while joints 0 and 1 measured exactly 0.0
degrees of change. Regression suite re-run again: still 0.0 degrees.

**Also checked and confirmed NO mismatch (informational, no code
change needed):** HANDO's own wrist-before-fingers ordering matches this
project's own convention exactly (both apply wrist pose before finger
curl in their live pose-application pass); HANDO has no equivalent of
Responsive Wrist Splay (its own wrist splay is purely static/slider-set
-- its own bug report that triggered its fix was a static
`wristSplay=-50` + a tween, not anything "reactive," consistent with
this project's own finding that ANY nonzero wrist rotation triggers the
mismatch, not specifically a LIVE one); HANDO's own saved-pose format
additionally carries `hideWrist` and 6 arm/shoulder/elbow keys this
project has no rigging for at all -- these are harmless extra fields on
an imported pose (silently ignored, not applied), not a compatibility
problem.

Cache-buster bumps: `index.html`'s own `main.js?v=78` -> `?v=81` across
this round (one intermediate debug-exposure bump).

--------------------------------------------------------------------------------
2026-09-15  -  (start not captured -- Actual: NOT MEASURED)

**Added git-tracked Save Settings, ported from HANDO's own Vercel
serverless setup.** Direct request: "Look at hando and see how the save
button saves dev panel settings to git. Implement. I have given vercel
the tokens." Until now this project used localStorage-only persistence
(CLAUDE.md §12l's baseline) -- a Save from one device/browser was never
visible on any other.

Investigated HANDO's actual implementation via a dedicated research pass
before writing anything: `api/save-settings.js` (a Vercel serverless
function using GitHub's Contents API -- GET reads the settings file live
from GitHub, not the deployed static snapshot, specifically so a Save
right after a deploy isn't invisible until the next redeploy; POST does
a GET-for-sha then a PUT to commit), gated by `GITHUB_TOKEN` (repo
write access) and `DEV_PANEL_SAVE_SECRET` (anti-spam, sent as an
`X-Dev-Panel-Secret` header, checked against the same literal value
baked into the client). Confirmed this project's own `devPanel.js`
ALREADY carries the exact same generic `remoteSave` client engine
(`remoteSaveSnapshot()`, `fetchRemoteSettingsUntilSuccess()`) --
shared workspace-wide, so no engine changes were needed at all, only:
(1) `api/save-settings.js`, ported near-verbatim with only
`DEFAULT_REPO`/`DEFAULT_BRANCH` changed to `LeisHo/HandyDandies`/`main`;
(2) `src/main.js` -- added the same `DEV_PANEL_SAVE_SECRET`/
`SAVE_SETTINGS_ENDPOINT` constants HANDO/DICKOCLICKO/OKCILCOKCID already
share (one workspace-wide secret, not per-project) and passed
`remoteSave: { endpoint, secret }` into `initDevPanel()`; (3) a seeded
`data/processed/dev-panel-settings.json`, populated with the user's own
real Copy Settings dump (pasted earlier this session) rather than an
empty object -- so the very first Save/Reset restores their actual
tuned state instead of looking wiped.

**Verified the full client-side mechanism end-to-end** -- the real
GitHub write can't be tested without an actual Vercel deployment, so
built a local mock of `/api/save-settings` (in-memory, same response
shapes as the real function) and drove the REAL production code through
it: GET-on-load correctly restored every seeded value (thumbCurl=79,
chpTargetPose="Point", 11 saved poses) through the real boot path;
changed a real slider (not by poking `cfg` directly -- confirmed that
approach doesn't reach `devPanel.js`'s own internal store at all, a
genuine test-methodology trap caught mid-verification) and clicked the
real Save button, which correctly flashed "Saved!" and POSTed a payload
where ONLY the changed device's field changed (desktop's `thumbCurl`
went 79->42, mobile/landscape stayed untouched at 79) -- proving the
GET-merge-POST logic actually preserves other fields rather than blind-
overwriting. Separately confirmed the app still boots cleanly with no
`/api/` route present at all (the plain local static server used
throughout this session) -- background retries 404 silently forever
without blocking the page, matching HANDO's own documented behavior for
this exact case.

Documented setup in README.md (new "Dev panel Save button setup"
section: exact `GITHUB_TOKEN`/`DEV_PANEL_SAVE_SECRET` env var names and
the shared secret value, noted no `vercel.json` is needed here since
this project's entry point is already at repo root, unlike HANDO's own
`src/index.html`). **Not yet confirmed working against the REAL live
Vercel deployment** -- the user has already configured the Vercel
environment variables ("I have given vercel the tokens") but the actual
production round-trip (real GitHub commit landing in the repo) hasn't
been observed by this session; worth a real Save-button click on the
live deployed URL to confirm.

--------------------------------------------------------------------------------
2026-09-15  -  (start not captured -- Actual: NOT MEASURED)

**Confirmed live: git-tracked Save Settings (previous entry) is genuinely
working in production.** Pushing this round's own commit was rejected
(non-fast-forward) because 3 real auto-commits ("Update dev-panel-
settings.json via Save Settings") had already landed on the remote from
actual production traffic -- exactly HANDO's own documented pattern.
Merged cleanly (the auto-commits only touch the settings JSON, this
round's own commit didn't) and pushed. No longer an open question.

**Fixed a critical correctness bug in the curl-axis math shipped earlier
today, via a direct handoff from a concurrent session working on HANDO.**
That earlier fix computed `axisRefQuat = baseQuat * (wristRest^-1 *
wristBone.quaternion)` -- treating a LOCAL (body-frame) rotation delta as
if it were already a WORLD-frame operator. Those only agree when the
wrist's own rest pose is the identity quaternion, which it isn't here
(confirmed earlier: `rHand`'s rest quaternion is `[-0.078, 0.688, -0.086,
0.716]`). HANDO's own team found and fixed the identical bug in HANDO
itself the same day and handed off the corrected math: a genuine
conjugation, `worldDelta = W1 * delta * W1^-1`, where W1 is the wrist
bone's own CURRENT WORLD quaternion. Adapted (not copied verbatim) for
this project's own extra `wrapperQuat` layer (HANDO has none) by
factoring wrapperQuat back out of W1 before conjugating -- the same
technique `rotateOnTrueWorldAxis()` already uses.

While implementing this, live testing caught a 2ND, self-introduced bug:
since W1 already includes `baseQuat` as its own ancestor, premultiplying
`baseQuat` onto `worldDelta` again double-applied it -- confirmed both
algebraically and empirically (a real 1-6 degree relative-to-wrist error
across joints with the double-apply; exactly 0.0 degrees once removed).

**Verified with the exact methodology the handoff itself described:**
compared every finger joint's orientation RELATIVE TO THE WRIST at
wristSplay=0 vs. wristSplay=-50 (identical finger values held constant
across the comparison, multiple fingers, every curl/splay/bias/base-
only/mid-only/tip-only/tip-twist slider simultaneously nonzero) -- 0.0000
degrees of difference on every joint tested (thumb, index, pinky, all 3
segments each). Re-ran the existing idle-drift + click-round-trip
regression suite: still 0.0 degrees, confirming this didn't regress
anything from earlier today.

**Re-checked HANDO's own Pose group for new sliders** (direct request)
and found HANDO completed its Base-Only/Mid-Only/Tip-Only joint-isolation
set for all 5 fingers including the thumb (this project only had Base-
Only for all 5, and Tip-Only excluding the thumb -- ported earlier today
matching HANDO's THEN-current scope). Ported the 2 gaps: `tipOnlyCurlThumb`
and `midOnlyCurl{Thumb,Index,Middle,Ring,Pinky}` (5 new sliders, joint-1
targeting, identical mechanism to Base-Only/Tip-Only). Added to
`POSE_PRESET_KEYS` and each finger's own Pose subgroup.

**Added Camera presets (Save/Use/Overwrite/Rename/Delete/Default) + Lock
Camera Pan + Lock Camera Zoom + Set Default Camera As Max Extents**,
direct request. Saved Cameras list-picker mirrors Saved Poses' exact UI
shape (`captureCameraPreset()`/`applyCameraPreset()`), applied directly
to the live camera+pan-target rather than a preview model. Lock Pan/Zoom
compose correctly with the pre-existing Click-Hold-Pose pan-disable (a
hold still always disables pan; releasing it now respects a genuine
permanent lock instead of unconditionally restoring pan).

Max Extents went through several rounds of direct correction before
landing on its final model: zoom-out is capped via OrbitControls' own
native `maxDistance` (zooming in stays completely free -- no snap, no
reset, just a standard max-distance clamp); pan is capped via a custom
per-frame clamp using that SAME radius, since OrbitControls has no
native pan-bounding option (free movement back toward the default
target always stays free; only moving further OUTWARD than the radius
hits a hard wall -- clamped in place, not reset/snapped, per direct
correction: "I dont get snapped back. that only applies to zoom" was
itself further corrected to "the max i can zoom out is the extents", a
plain cap, not a teleport).

Verified live: capture/apply preset round-trip lands exactly back at the
captured camera position AND pan target; Lock Pan/Zoom checkboxes clicked
through the real UI correctly toggle `controls.enablePan`/`enableZoom`;
Max Extents zoom clamps exactly to the default's own distance (260.171)
while zooming in to 0.3x that distance stays completely unclamped; Max
Extents pan clamps a target set to 3x the allowed radius down to exactly
the boundary, while a target already within bounds (0.5x radius) is left
completely untouched.

Cache-buster bumps: `index.html`'s own `main.js?v=82` -> `?v=85` across
this round.

--------------------------------------------------------------------------------
<next entry follows the same divider + dated-header pattern>

--------------------------------------------------------------------------------
2026-09-15  -  01:54:19 - 02:11:58 EDT (17m39s, measured; spans investigation,
root-cause discovery, fix, and verification)

**Found and fixed the ACTUAL wrist-splay/curl bug this whole saga was
chasing -- previous "corrected" conjugation formula collapsed to identity
at wristBend=wristSplay=0 exactly, silently discarding `baseQuat`
(alignQuat) from every finger's curl axis.** User reports of "still have
wrist splay issues" persisted even after the prior round's conjugation
fix (`W1 * delta * W1^-1`) passed every relative-to-wrist invariance test
run against it, including live production tests with real saved poses.
Direct user reports narrowed it down over several exchanges: "even fist
doesnt look right... with no responsive wrist splay turned on" and "the
ones with 0 [wristSplay] as the input seem to look fine" turned out to
have it backwards in direction but right in spirit -- poses with
wristSplay=0 (e.g. "Fist") were the ones rendering broken (flat, wing-
like wedge shapes instead of a closed fist), while nonzero-wristSplay
poses ("Neutral - Bent Back", "Fist - Bent Back") rendered correctly.

Root cause: at delta = identity (wristBend = wristSplay = 0 exactly), the
conjugation `W1 * delta * W1^-1` collapses to IDENTITY for ANY W1 at all
-- confirmed live via direct computation (axisRefQuat read back as
[0,0,0,1] instead of baseQuat's actual [-0.712,0.021,0,0.702]). The
relative-to-wrist invariance test used throughout this whole saga only
checks that a finger's orientation relative to the wrist stays constant
as wristSplay CHANGES -- a systematically biased but internally self-
consistent formula can satisfy that test while still being wrong in
absolute terms, which is exactly what happened: every test case run this
entire session (Fist - Bent Back, Neutral - Bent Back, the Click-Hold-
Pose round trip) happened to use a nonzero wristSplay, so the delta=I
degenerate case was never exercised until a real saved pose ("Fist")
finally hit it.

Fix (`applyCurlToSkeleton()`, `src/main.js`): conjugate `delta` by
`wristRestForAxis` (R) alone instead of by the wrist bone's full world
quat (W1) -- `baseQuat * R * delta * R^-1` -- which correctly reduces to
exactly `baseQuat` when delta = I (matching the pre-existing no-wrist-
bone fallback branch, which has always used `baseQuat` directly for that
case) while still rotating the curl axis to track the wrist's actual
bend/splay when delta != I. No longer needs the wrist bone's world
quaternion or `wrapperQuat` for this computation at all.

Verified 3 ways: (1) direct computation -- axisRefQuat now matches
baseQuat exactly at delta=I, <1e-6 per component, both live-production-
read and the new formula computed side by side; (2) visual -- "Fist"
applied via direct cfg values on a fresh local dev-server load now
renders as an actual compact closed fist across the whole field, versus
flat wedge/wing shapes before the fix (screenshot comparison); (3) the
PRE-EXISTING relative-to-wrist invariance check (comparing a finger's
orientation relative to the wrist between wristSplay=0 and wristSplay=-50
with every other slider held identical) still holds at 0.0000 degrees
under the new formula -- the fix does not regress the previously-verified
nonzero-delta case.

Also confirmed, this round: the `wristSplayResponsiveEnabled` /
`wristSplayReactiveEnabled` true-vs-false discrepancy noted at the top of
this investigation was a timing artifact (read moments before vs. after a
real user Save), not a real bug -- resolved once the user's own save
landed and a fresh page load matched. Ruled out as contributing factors,
per direct user correction: the "shoulder/elbow/forearm" and "Hide Wrist"
slider families HANDO has that this project doesn't port 1:1 (`hideWrist`
here is a structurally different, live-reactive-by-cursor-distance Arm
Length feature, not a static per-pose value like HANDO's) -- "shoulder
elbow and forearm isnt relevatn... mostly its the wrist and finger
settings that matter."

Cache-buster bump: `index.html`'s own `main.js?v=85` -> `?v=86`.

--------------------------------------------------------------------------------
2026-09-15  -  02:45:28 - 02:58:49 EDT (13m21s, measured)

**Found and fixed a 2nd, unrelated bug that had been masquerading as the
wrist-splay issue this whole time: `FINGER_SIGN.middle`/`.ring` were
still `-1`, never synced with HANDO's own 2026-09-14 sign flip.** User
report after implementing the delta=identity fix (previous entry): "Poses
still look good in Hando after implementation, while mine still look bad.
anything with a wrist splay still looks fucked" -- then, once "Neutral"
was set as Default and "Fist" tested via Click-Pose specifically: "even
fist doesnt look right... with no responsive wrist splay turned on" --
then, decisively, "Point and MiddleFinger also look wrong" (both have
wristSplay=0, ruling out anything wrist-splay-specific) and finally
"anything with a wrist splay still looks fucked" was narrowed further:
"Fist (0 splay) and Fist-Bent Back (-50) look messed up" -- both, not
just the nonzero one.

Direct A/B test settled it: checked out a scratch git worktree at the
commit BEFORE the delta=identity conjugation fix, applied the exact same
"Point" pose data there, and got the SAME fragmented, self-intersecting
geometry as on current production -- proving the conjugation fix (this
file's previous entry) was NOT the cause of this specific breakage, and
something else, present in BOTH the old and new formula, was responsible.

Root cause, found by diffing HANDY DANDIES's `applyCurlToSkeleton()`
constants against HANDO's own: HANDO flipped `FINGER_SIGN.middle`/`.ring`
from `-1` to `1` on 2026-09-14 (direct HANDO user request: "for middle
finger and ring finger curl, inverse the number. right now, curling it is
negative numbers") and migrated its own existing saved poses' curlMiddle/
curlRing values to preserve their visual meaning under the new sign. This
project's own `FINGER_SIGN` was never updated to match -- but every pose
in this project's own saved-pose list was captured/imported from HANDO
AFTER that migration, so all of them (Point, MiddleFinger, Fist, Fist -
Bent Back, ThumbsUp, Wave 1-6...) already use positive curlMiddle/
curlRing for an actual curl, consistent with HANDO's NEW convention --
meaning middle/ring curled BACKWARD on every one of them here, on top of
whatever curl the other correctly-signed fingers were doing, producing
exactly the chaotic, fragmented look reported. "Neutral"/"Neutral - Bent
Back" (all curl values 0, sign irrelevant at 0) were the only 2 poses
unaffected -- coincidentally the same 2 poses this whole wrist-splay
investigation had been testing against, which is why this went unnoticed
until poses with real curl values (Point, Fist) were finally checked.

Fixed with a pure sign flip (`FINGER_SIGN.middle`/`.ring`: `-1` -> `1`),
no data migration needed here (per the above -- this project's own saved
poses already assume the new convention). Verified: "Point" applied via
the real saved-pose data now renders as a correctly-formed pointing hand
(extended index, curled fist body, visible thumb) both in a scratch
worktree test and on live production; "Fist - Bent Back" (wristSplay=-50)
renders as a proper closed fist with fingertips visibly curling toward
the palm; the pre-existing relative-to-wrist invariance check (from the
delta=identity fix) still holds at 0.0000 degrees -- this fix doesn't
touch the wrist-axis math at all, only the finger-curl sign table.

Cache-buster bump: `index.html`'s own `main.js?v=86` -> `?v=87`.

--------------------------------------------------------------------------------
2026-09-15  -  03:09:52 - 03:21:56 EDT (12m04s, measured)

**Fixed Click-Pose's start-of-transition stutter and a mouse-log click/
drag mislabeling bug, both from a direct user report with a screen
recording and mouse log attached.**

**Click-Pose stutter.** User: "Default pose is Neutral-BentBack. Target
pose is Fist. The fist looks good, But i noticed that right after te
click, the closest hand seems to start some sort of animation
transition, but stops after a split second, then the click-pose function
runs smoothly." Root cause: `startClickHoldPose()` fires unconditionally
on EVERY left `pointerdown` (needed so a genuine hold-and-drag disables
camera pan immediately) -- with no way to tell a plain click apart from
the start of a real hold at press time. The closest hand's own distance-
based start delay can be exactly 0ms, so it began visibly transitioning
toward Click Hold-Pose's own target pose ("Fist - Bent Back" -- a
DIFFERENT pose than Click Pose's own "Fist") on the very next frame, then
reversed the instant the real, brief click released (`endClickHoldPose()`
on `pointerup`) -- all before Click Pose's own (correctly debounced,
per the 2026-09-14 entry) transition even began. Fixed by adding a new
"Hold Confirm Delay (Ms)" slider per trigger (chp/rchp, default 150ms):
`updateClickHoldPoseForHand()` now refuses to leave 'idle' until the hold
has genuinely been sustained past it, and `endClickHoldPose()` skips
starting a retransition for a hand that never left 'idle' (avoiding a
pointless lerp-to-itself using stale/undefined `lastAppliedValues`). The
camera-pan lock -- `startClickHoldPose()`'s OTHER job -- still engages
immediately on pointerdown, unaffected, per the original direct request
that moving the cursor during a hold must never pan even before the hold
is confirmed. Verified live: a real fast click via the actual DOM
pointerdown/pointerup path leaves the closest hand's CHP phase at 'idle'
throughout (confirmed by direct inspection, not just visual judgment).

**Mouse log click/drag mislabeling**, same report: "how come clicks are
logging as Pans even though its just a click, not even a click hold
drag." Root cause: `describeMouseLogTrigger()` was called once per
`pointerup`, BEFORE click-vs-drag was even classified, and always
described button 0/1/2 as "Camera Pan"/"Camera Zoom-Dolly (OrbitControls,
X-drag)" regardless of whether anything was actually dragged -- every
call site, including the genuine "Click at"/"Double-click at" lines,
reused that same drag-worded string. Fixed by passing a `wasDrag` flag
(the caller already computes heldMs/moved) so a plain click is described
by button alone ("Canvas (Left Click)") and the Camera Pan/Zoom-Dolly
wording is reserved for genuine drags. Side effect, also correct: a quick
right-click still gets its own immediate "Right-click at" line, but a
GENUINE right-drag (previously always mislabeled as a plain right-click,
regardless of heldMs) now correctly falls through to "Drag-release ...
Camera Pan (OrbitControls, right-drag)". Verified live: a real click logs
"Click at (x, y) -> Canvas (Left Click)"; a real drag still logs
"Drag-release at (x, y) -> Camera Pan (OrbitControls, left-drag)
(heldMs:2014)".

Cache-buster bump: `index.html`'s own `main.js?v=87` -> `?v=88`.

--------------------------------------------------------------------------------
2026-09-15  -  03:39:38 - 03:53:41 EDT (14m03s, measured)

**New feature: Tween + Double Click Hold Tween.** Direct user request,
modeled on HANDO's own "Tween / Export" dev-panel group but deliberately
narrower ("We dont need that. we just need poses") and with a different
trigger mechanic than HANDO's manual preview slider.

**Tween group** (new DEV_GROUPS entry): `tweenPoses`, a 'multi-select'
control (same generic devpanel/devPanel.js type HANDO's own tween group
uses -- a growable list of dropdowns backed by one ordered array of saved-
pose names) extended here with drag-to-reorder, direct request ("allow me
to click and drag to reorder these"). devpanel/devPanel.js's own
`renderMultiSelectRows()`/`buildMultiSelectRow()` gained a dedicated
`.dp-ms-row-handle` drag icon per row (same §12e "handle icon only, never
the row itself" convention as list-picker's own `.dp-lp-row-handle`) and
now calls the SAME generic `setupReorder()` list-picker rows/groups
already use, resyncing `entry.values` from the post-drop DOM order rather
than assuming index `i` still matches. `savedTweenSequences` is a
'list-picker' capturing ONLY `tweenPoses` (`captureTweenSequencePreset()`/
`useTweenSequencePreset()`, main.js) -- no camera/lighting/toon fields at
all, unlike HANDO's equivalent (this project has no per-pose camera/
lighting capture to bundle in the first place).

**Double Click Hold group** (new DEV_GROUPS entry): Master On/Off, a
"Tween Selector" dropdown (options sourced from `savedTweenSequences`),
and a single "Tween Speed (Ms)" slider. 2 rounds of direct clarification
resolved the exact behavior: (1) the sequence always tweens FROM THE
DEFAULT POSE through the selected sequence's own poses in order --
`startDoubleClickHoldTween()` prepends `poseDefaultValues` to the
resolved pose list, so even a 1-pose sequence tweens default->pose1; (2)
on release, every hand retransitions back to default from WHATEVER pose
it's currently in, at the SAME speed as the forward tween -- no separate
retransition-speed control, unlike Click-Hold-Pose's own 2 groups.
Applies IDENTICALLY to every hand at once (no per-hand distance stagger,
unlike Click-Hold-Pose/Click-Pose) -- `updateDoubleClickHoldTween()` is
computed ONCE per frame in `animate()`, not once per hand, and the result
applied to every hand via the existing `applyPoseValuesToHand()`
pipeline (same one Click-Hold-Pose/Click-Pose already use -- no new
skeleton-posing math needed).

**Gesture detection**: "double click hold" (click, release, click-and-
HOLD -- distinct from Click-Pose's own fire-and-forget 'dblclick', which
recognizes at the 2nd RELEASE, and from Click-Hold-Pose's single press-
and-hold). Implemented as a self-contained window pointerdown/up pair:
`dcHoldLastCleanUpTime` records the last left pointerup that was a clean,
quick click (reusing the Mouse Tracking Log's own MOUSE_LOG_MULTICLICK_MS/
MOUSE_LOG_HELD_DRAG_MS/MOUSE_LOG_MOVE_THRESHOLD_PX thresholds for
consistency, not fresh constants); a left pointerdown arriving within
that window starts the tween immediately -- deliberately independent of
chp/click/dblclick's own state (a disclosed simplification, same
"whichever runs last this frame wins" pattern already used for chp vs.
rchp).

Verified live on a local dev server via real dispatched PointerEvents
(down, up, down-and-hold, up) rather than synthetic function calls: drag-
reorder on `tweenPoses` correctly resynced `cfg.tweenPoses` from DOM
order (confirmed via `commit()`'s own downstream state); saving a Tween
Sequence correctly captured only `tweenPoses`; the Double Click Hold
Tween Selector dropdown correctly picked up the newly-saved sequence
name; performing the actual gesture visibly posed every hand in the
field IDENTICALLY (confirmed via screenshot -- a uniform pose across the
whole field, unlike Click-Hold-Pose's own per-hand staggered look) and
releasing correctly retransitioned every hand back to its resting pose,
with no console errors beyond the pre-existing, unrelated
`api/save-settings` 404s (expected on a plain static server, no Vercel
endpoint behind it).

Cache-buster bumps: `index.html`'s own `main.js?v=88` -> `?v=89`,
`style.css?v=5` -> `?v=6`; main.js's own `devpanel/devPanel.js?v=13` ->
`?v=14` import.

--------------------------------------------------------------------------------
2026-09-15  -  04:01:12 - 04:09:38 EDT (8m26s, measured)

**Double Click Hold: added a Loop checkbox.** Direct follow-up request:
"provide a checkbox to set if it continues to loop or not. As in if i
keep the double click hold, it will continue looping through the tween,
not including the initial default pose. If unchecked, it runs through
the tween once." Unchecked (the pre-existing behavior, still the
default): the hand holds at the final pose once reached. Checked: once
the initial default->pose1->...->poseN pass completes,
`updateDoubleClickHoldTween()` hands off to a new 'looping' phase that
cycles through JUST the named poses (never re-including default) --
poseN->pose1->pose2->...->poseN, wrapping, via a new `lerpLoopSequence()`
helper -- for as long as the hold continues, at the SAME per-segment pace
as the initial pass (`loopSegmentMs = speedMs / namedPoses.length`, so
looping doesn't visibly speed up/slow down relative to lap 1).

Caught and fixed one real bug before ever running it: the loop's own
cyclic clock originally started at `tCyclic = 0`, which maps to
loopPoses[0]->loopPoses[1] -- since the forward pass had JUST ended
exactly at the last pose, this would have produced a visible backward
jump/pop the instant looping began, instead of continuing smoothly.
Fixed by offsetting the loop's start position to `segments - 1` (the WRAP
segment, last pose -> first pose), so motion continues seamlessly with no
pop, confirmed via manual trace of the segment-index math at the
phase-transition boundary.

**Live-verification limitation, disclosed:** confirmed the combined app
loads and runs with 0 console errors through an extended hold (well past
the loop threshold) and a release, and confirmed the Loop checkbox
renders and is wired to `cfg.dcHoldLoop` correctly. Could NOT confirm
frame-by-frame visual animation during the loop via this session's
browser-automation tool -- repeated `canvas.toDataURL()` samples a full
second apart, taken via `javascript_tool`, came back byte-identical even
mid-hold, consistent with this project's own already-documented gotcha
("this session's own browser-automation tool reports
`document.hidden`/`innerWidth === 0`... a characteristic of that tool,
not a page bug" -- and an earlier session's own note that the render
loop can go genuinely paused during JS-exec-based polling specifically).
Correctness here rests on the manually-traced segment math above and the
error-free extended hold, not a confirmed live frame capture -- worth a
real-device check.

**Also present in this commit: a "Right Click" trigger group and an
idle-repose performance fix, from a CONCURRENT session working in this
same local working tree while this task was in progress** (detected via
an on-disk file-change notice mid-edit; confirmed via `git diff` that
both sets of changes are complete, non-overlapping, and coexist without
conflict). Right Click (`rcEnabled`/`rcMode`/`rcTargetPose`/
`rcTweenSelector`/timing controls) is a 3rd fire-and-forget Click-Pose-
family instance on the right button, with a Mode dropdown choosing a
single Target Pose or a full Tween Sequence as its target -- reuses
`resolveTweenSequencePoses()`/`lerpTweenSequence()` from the Tween
feature above. The performance fix replaces `updateRenderOrder()`'s own
unconditional-every-frame idle repose with a `needsIdleRepose` gate
(`cfg.wristSplayResponsiveEnabled || hand._wasOverriddenLastFrame`),
noting a measured ~38ms/frame cost at 255 hands. Neither was authored or
independently verified by this entry's own author -- documented here
only because it landed in the same uncommitted working tree and commit;
see whichever session did that work for its own fuller account if one
gets written separately.

Cache-buster bump: `index.html`'s own `main.js?v=91` -> the concurrent
session's own `?v=92` (its bump superseded this entry's `?v=91` on the
same line before either was committed -- harmless, since a cache-buster
is just a monotonically-increasing key, not semantic content).

--------------------------------------------------------------------------------
2026-09-15  -  (start not captured -- session resumed from a compacted
summary; Actual: NOT MEASURED)

**Fuller account of the Right Click + idle-repose performance fix**
(originally landed via a concurrent session's commit, disclosed above as
unauthored/unverified by that entry's own author) -- this entry is that
work's own author, verifying it live after the fact.

**Performance fix, confirmed with real measurements (`performance.now()`
around direct `updateRenderOrder()` calls, 255 hands, 50ms gaps between
calls -- same isolated-timing method this project's own earlier lag
investigation established as reliable):** with Responsive Wrist Splay ON,
~30-44ms/call (matches the ~38ms/frame figure already on record); with it
OFF, ~0.2-0.7ms/call -- the idle repose gate (`needsIdleRepose =
cfg.wristSplayResponsiveEnabled || hand._wasOverriddenLastFrame`)
eliminates essentially the entire cost when the feature is off, confirmed
directly rather than assumed from the code alone. `hand._wasOverriddenLastFrame`
forces exactly one repose the frame after a Click-Hold/Click-Pose
transition ends, so a hand returning to idle still re-syncs its
(possibly stale) finger curl before the gate can skip it again.

**Right Click, verified live via `window.__debug`:** `triggerClickPose('rc')`
in Single Pose mode correctly entered 'forward' with a real
`forwardSnapshot` and no `tweenPoses`; manually stepping
`updateRenderOrder()` (this session's browser-automation tool has its own
already-documented rAF-timing unreliability, so real frames were driven
directly rather than waited on) carried the phase through
forward->paused->retransition, with `lastAppliedValues.curlIndex`
reaching the target pose's own value (96, "Fist") then back down through
retransition. Tween mode (a fabricated 2-pose test sequence via
`cfg.savedTweenSequences`) resolved `cp.tweenPoses` to length 3
([forwardSnapshot, pose1, pose2]) as designed, and the same phase
machine played it through to 'idle' with a plausible final
`lastAppliedValues.curlIndex` (89) on the way back to default. The Mode
dropdown's conditional UI (Target Pose visible only in Single Pose mode,
Tween Sequence only in Tween mode) was confirmed toggling live via a
direct `change` event, using an inline `style.display` toggle rather than
the `hidden` attribute -- `.dp-row { display: flex }` in style.css is an
author rule that silently wins the cascade over the UA stylesheet's
`[hidden] { display: none }` at equal specificity regardless of source
order, so `.hidden = true` alone would have done nothing.

**Bug found and fixed post-hoc: 3 empty dropdowns.** `rcMode`,
`rcTargetPose`, and `rcTweenSelector` all rendered with zero `<option>`s
on page load (confirmed via direct DOM inspection, not assumed) -- the
same TDZ-timing issue already worked around for `chpTargetPose`/
`rchpTargetPose`/`clickTargetPose`/`dblclickTargetPose` (a `select`
control's initial `<option>` list is populated during `initDevPanel()`'s
own restore-from-storage step, which runs before `const cfg = ...`
finishes assigning, so any closure reading `cfg` -- or, it turns out,
ANY select built this early, even `rcMode`'s own plain literal array with
no `cfg` dependency at all -- ends up empty and never gets a 2nd chance
outside an explicit one-time re-populate). Fixed by adding the same 3
missing `safeRefreshSelectOptions()` calls to the existing one-time
list right after `cfg` is real. Committed separately (`5daac10`) since
the feature commit itself had already been pushed by the other session
before this fix was found.

Live-tested end to end on the local static dev server (`?dev=1`); 0
console errors beyond the pre-existing, unrelated `api/save-settings`
404s (expected -- no Vercel endpoint behind this local server, matches
this project's own documented single-tier Save Settings design).

Cache-buster bump: `index.html`'s own `main.js?v=92` -> `?v=93`.

--------------------------------------------------------------------------------
2026-09-15  -  04:21:00 - 04:30:25 EDT (9m25s, measured)

**Investigated direct user report: "all my click functions stopped
working."** No single confirmed root cause on production, but found and
closed one real, concrete crash path and added a general-purpose
backstop against the whole class of failure this symptom best matches.

**What was verified NOT broken**, directly on live production via the
`window.__debug` hook (already exposed by an earlier commit): called
`triggerClickPose('click')` and a real dispatched click both correctly
advanced `hand._cp.click.phase` through forward -> paused -> retransition
and wrote real, non-default rotation values onto the actual skeleton
bones (confirmed on `rThumb1` directly) -- the entire trigger pipeline
(gesture detection -> `triggerClickPose` -> `updateClickPoseForHand` ->
`applyPoseValuesToHand` -> skeleton) is provably correct and unbroken.
Zero console errors on a fresh production load.

**What live-frame verification could NOT settle:** `renderer.info.render.frame`
appeared stuck across repeated samples, and even an independent
`requestAnimationFrame` probe (nothing to do with this app's own code)
showed zero increments over 2 full seconds under this session's own
browser-automation tool -- matching (and further confirming, via the
generic probe) this project's own already-documented gotcha that this
tool's rAF-based live capture goes unreliable under its own JS-exec
polling. Manually stepping `updateRenderOrder()` directly (bypassing rAF
entirely, via `window.__debug`) confirmed the update pipeline itself
works regardless -- but this means the tool available in this session
could not confirm or rule out a genuinely stalled render loop in the
user's own real browser.

**One real, concrete crash path found and fixed via code review:**
`updateDoubleClickHoldTween()`'s own `speedMs`/`loopSegmentMs`
(Double Click Hold's Loop mode, added 2 entries ago) divide directly by
`cfg.dcHoldTweenSpeedMs` with no guard against it being `NaN`/undefined
(e.g. a stale saved-settings value predating that control). A NaN here
cascades: NaN `tCyclic` -> `poses[NaN]` -> `undefined` -> `lerpPoseValues()`
throwing on `undefined[key]`. Because this throw happens INSIDE
`animate()`'s per-frame body, and recurs on EVERY frame for as long as a
hand keeps looping, `composer.render()` never gets reached again --
the screen freezes at whatever last rendered while every click still
correctly (but invisibly) updates state underneath, exactly matching
"stopped working." Fixed with `Number.isFinite()` guards (fallback to
the control's own 800ms default) at both division sites.

**General-purpose backstop, not tied to the specific bug above:**
wrapped `animate()`'s entire per-frame body in try/catch. Previously, ANY
future per-frame exception (from this bug, a different one, or one not
written yet) would have the same silent-freeze signature -- `console.error`
never shown to a non-technical user, screen just stops updating, "click
functions stopped working" with no visible cause. Now: whatever throws
gets logged loudly and the loop keeps running -- the current frame's
render is skipped, but the NEXT frame gets a clean chance rather than
repeating the same throw forever (rAF was already re-scheduled at the
very top of `animate()`, so this doesn't change WHETHER the loop
survives a one-off throw, only whether a RECURRING one does, and whether
either kind is now visible in the console instead of silent).

**Disclosed limitation:** could not directly reproduce the user's exact
reported freeze in this session's own environment (fresh production
loads never exhibited a recurring per-frame exception). This fix closes
a real, concrete gap consistent with the reported symptom rather than a
confirmed-reproduced root cause -- ask the user to confirm whether this
resolves it, and if it recurs, `console.error`'s own new "animate() frame
threw" message plus its stack trace should now pinpoint the exact cause
directly, rather than requiring another investigation like this one.

Cache-buster bump: `index.html`'s own `main.js?v=93` -> `?v=94`.

--------------------------------------------------------------------------------
2026-09-15  -  06:09:41 - 07:04:17 EDT (54m36s, measured -- ran well over
its own ~15-25min opening ETA; see this entry's own closing note for why)

**Root-caused and fixed the ACTUAL "all my click functions stopped
working" bug**, following a much more specific direct user report this
time: "1. Startup (hard refresh) 2. I see the hands but with a weird
surface texture as if there is overlapping 3d models... rotation etc
looks off. 3. I click, then suddenly it looks correct... Palm following
cursor works. 4. I click or click hold to test click function. Nothing
happens."

**Reproduced directly on a fresh production load, visually** (a real
screenshot, not a JS-exec query -- see this entry's own "tool
limitation" note below for why that distinction mattered): every hand
rendered as long, straight, spike-like shapes radiating outward -- the
raw, un-posed GLB bind pose (straight fingers/forearm), not this
project's actual default pose. Confirmed via `window.__debug`:
`hands[0].currentBaseQuat` read exactly `[0,0,0,1]` (IDENTITY) on this
fresh load, while `cloneBaseQuat` (the correct, already-computed shared
value) read `[-0.712, 0.021, 0, 0.702]` -- and `cfg.wristSplayResponsiveEnabled`
was `false` for this real user's actual saved settings.

**Root cause:** `hand.currentBaseQuat` is seeded from `cloneBaseQuat` at
hand-creation time (`rebuildField()`) -- if `cloneBaseQuat` is STILL its
own module-load-time identity default at that exact moment (cfg hasn't
finished restoring from the git-tracked settings log yet when the field
first builds -- a real, plausible race, not a hypothetical), every
hand's own snapshot freezes on that stale identity FOREVER, unless
something re-syncs it. Before the previous entry's idle-repose
performance gate existed, that re-sync ran UNCONDITIONALLY every single
frame, so a stale snapshot self-corrected within 1 frame, invisibly --
nobody had ever seen this bug because it never survived long enough to
be visible. That gate's `needsIdleRepose = cfg.wristSplayResponsiveEnabled
|| hand._wasOverriddenLastFrame` broke this guarantee for anyone with
Responsive Wrist Splay off: neither condition is true on a hand's very
first real frame, so the resync never runs at all until SOMETHING ELSE
incidentally triggers it -- exactly what a click does (either
`_wasOverriddenLastFrame` forcing 1 catch-up frame after the click ends,
or the click's own `applyPoseValuesToHand` independently recomputing a
correct `currentBaseQuat` from its own target pose). Neither is a real
fix; both are lucky side effects that happen to look like "the first
click fixes it."

**Fix:** added `hand.everReposed` (false at creation, see
`rebuildField()`'s own comment on the `hand` object literal) and folded
it into the gate: `needsIdleRepose = cfg.wristSplayResponsiveEnabled ||
hand._wasOverriddenLastFrame || !hand.everReposed`. Forces exactly ONE
guaranteed full repose per hand regardless of the other 2 conditions,
then never forces it again -- restores the original per-hand correctness
guarantee without reintroducing the ~38ms/frame cost the performance fix
was written to eliminate.

**Verified directly** (bypassing the render loop entirely, per this
project's own established "manually step the update function instead of
trusting live rAF" methodology): simulated the exact broken state on a
real hand (`currentBaseQuat` forced to identity, `everReposed`/
`_wasOverriddenLastFrame` both false, `wristSplayResponsiveEnabled`
false) and called `updateRenderOrder()` ONCE, with no click involved --
`currentBaseQuat` correctly self-healed to match `cloneBaseQuat`, and
`everReposed` flipped true.

**Tool limitation, disclosed (why this took as long as it did):** this
session's own browser-automation tool repeatedly reported
`document.hidden: true` / `window.innerWidth: 0` on tabs that were
genuinely fronted and interactive -- the SAME already-documented gotcha
from earlier this project, re-confirmed multiple times before a plain
`computer{screenshot}` call (which this project's own CLAUDE.md already
notes works correctly even when the JS-exec path doesn't) finally showed
the real, reproducible visual bug directly. A `GL_INVALID_FRAMEBUFFER_OPERATION:
... Attachment has zero size` WebGL warning also appeared on that same
fresh load -- real (browser/GPU-level, not JS-exec-spoofable) but NOT
investigated further this round, since the screenshot + `currentBaseQuat`
evidence already fully explained the reported symptom on its own; worth
a separate look if a startup sizing issue is ever reported on its own.

Cache-buster bump: `index.html`'s own `main.js?v=94` -> `?v=95`.

--------------------------------------------------------------------------------
2026-09-15  -  07:08:20 - 07:12:46 EDT (4m26s, measured)

**Found and fixed the REAL "click functinos still not working" bug --
distinct from both prior rounds' fixes.** Direct user report with a fresh
mouse log showing repeated genuine clicks/holds producing no visible
effect. The previous entry's `currentBaseQuat`/`everReposed` fix was real
and correct (confirmed fixing the startup appearance) but didn't touch
this: the idle-repose performance gate (landed via a concurrent session's
commit, `4d25b9a`) replaced the original `if (!overridden) { ...idle
repose... }` with `if (needsIdleRepose)`, where `needsIdleRepose` never
actually included `!overridden` as a condition.

Concretely: `hand._wasOverriddenLastFrame` is true starting the 2nd frame
of ANY active trigger sequence (chp/rchp/click/dblclick/rc/dcHold) and
stays true for every frame the sequence keeps running. Since
`needsIdleRepose = cfg.wristSplayResponsiveEnabled || hand._wasOverriddenLastFrame
|| !hand.everReposed` doesn't check `overridden` for the CURRENT frame,
it was ALSO true on every one of those frames -- so the idle-repose
branch ran immediately after `applyPoseValuesToHand()` had just written
that frame's own in-progress transition pose, and overwrote it right
back to `cfg`'s plain default values. Net effect: every trigger's pose
only ever stuck for a single frame before being stomped back to default,
frame after frame, at normal framerate indistinguishable from "the click
did nothing at all."

Fixed with one added condition: `const needsIdleRepose = !overridden &&
(cfg.wristSplayResponsiveEnabled || hand._wasOverriddenLastFrame ||
!hand.everReposed)` -- restores the original mutual exclusion (a hand is
EITHER actively driven by a trigger this frame OR eligible for idle
repose, never both) without touching any of the earlier fix's 3
legitimate conditions.

**Also fixed, found in passing while verifying the above:** a concurrent
session's own rename (`updateRightClickModeVisibility()` ->
`updateClickTriggerModeVisibility(p, extraSinglePoseKeys)`, generalizing
Right Click's Mode dropdown to every Click-family trigger group) missed
1 call site -- the ORIGINAL `rcMode` control's own `onChange`, still
calling the old, now-undefined function name. Threw a live
`ReferenceError` (caught by `safeRefreshSelectOptions()`'s own try/catch,
so contained -- but Right Click's own mode-specific rows silently stopped
toggling). Fixed by updating that one call site to the new signature.

**Verified directly** (bypassing the render loop, same "manually step
the update function" methodology as the previous 2 entries): triggered a
real Click Pose transition toward "ThumbsUp" and stepped `updateRenderOrder()`
with REAL elapsed time between calls (not a tight synchronous loop, which
would never show meaningful `progress` advancement) -- `thumbCurl`
progressed smoothly and monotonically (78.95 -> 54.69 -> 34.27 -> 11.37
-> -9.19 -> -26.86 -> -37, reaching the target and pausing), with no
snap-back to the idle default value at any intermediate frame.

Note: a concurrent session was actively committing to this same file
throughout this investigation (generalizing Mode/Tween-sequence support
from Right Click to every other Click-family trigger group) -- confirmed
via repeated `git diff` checks that its changes and this fix's own
changes never overlapped or conflicted.

Cache-buster bump: `index.html`'s own `main.js?v=95` -> `?v=97` (an
intermediate `?v=96` was superseded mid-fix by this entry's own 2nd
change, same as the earlier `?v=91`/`?v=92` concurrent-bump note).

--------------------------------------------------------------------------------
2026-09-15  -  07:04:45 - 07:16:14 EDT (11m29s, measured)

**Mode dropdown (Single Pose / Tween) ported to all 4 remaining Click-
family groups: Click Hold-Pose, Right-Click Hold-Pose, Click Pose,
Double-Click Pose.** Direct follow-up request: "reference the right
click pose Mode selector. i want you to implement it to all click
functions in the dev panel." (Double Click Hold Tween is NOT part of
this -- it's already Tween-only/shared-across-hands by design, no Single
Pose concept to toggle.)

`makeClickPoseGroup()` (click/dblclick) and `makeClickHoldPoseGroup()`
(chp/rchp) both gained a `${p}Mode` select + `${p}TweenSelector` select,
identical in shape to Right Click's own. Click Pose/Double-Click Pose
reused the phase machine `updateClickPoseForHand()`/`triggerClickPose()`
already generalized for Right Click (`cfg[${p}Mode] === 'Tween'` branches
to `lerpTweenSequence()` instead of a single named Target Pose) --
genuinely free for those 2. Click Hold-Pose/Right-Click Hold-Pose needed
their OWN generalization, a separate 2-phase (forward/retransition, no
pause) hold-based machine: `startClickHoldPose()` now resolves
`trig.tweenPoses` once per hold-start (`[liveSnapshot, ...namedPoses]`,
same "start from wherever the hand currently is" convention as Single
Pose mode, not Double Click Hold Tween's always-from-default one), and
`updateClickHoldPoseForHand()`'s own 'forward' branch plays through it
via `lerpTweenSequence()` for as long as the button stays held, exactly
mirroring how it already played toward a single target.

**Mid-task correction, applied to ALL 5 groups (chp/rchp/click/dblclick/
rc) including retrofitting Right Click's own earlier version:** "the pose
transition, hold, retransition UI should only show when Single Pose is
selected." Originally (Right Click's first version) only Target Pose vs.
Tween Sequence toggled; this widened it to the WHOLE block -- Pose
Transition Speed/Curve/Range, Pause Duration (Click Pose family only),
Pose Retransition Speed/Curve/Range -- none of which describes playing
through a Tween Sequence either. Generalized the old, Right-Click-only
`updateRightClickModeVisibility()` into `updateClickTriggerModeVisibility(p,
extraSinglePoseKeys)`, called by all 5 groups' own Mode `onChange` plus a
one-time init pass; `extraSinglePoseKeys` is the one shape difference
(`['PauseDurationMs']` for the 3-phase Click Pose family, `[]` for the
2-phase Hold-Pose family). The underlying cfg values for a hidden control
aren't reset or disabled -- Tween mode still runs at whatever
Transition/Retransition Speed was last set, just without a visible
control while Tween is selected, so switching modes back and forth loses
no tuning.

Also added the same 8 new selects' worth of TDZ-empty-dropdown calls
(`chpMode`/`chpTweenSelector`/`rchpMode`/`rchpTweenSelector`/`clickMode`/
`clickTweenSelector`/`dblclickMode`/`dblclickTweenSelector`) to the
existing one-time post-`cfg` refresh list and the `savedTweenSequences`
onChange refresh list -- same pre-`cfg`-exists timing gap already fixed
for Right Click's own 3 selects earlier the same day.

**Landed via a concurrent session's commit (`241eae8`) alongside its own
unrelated `!overridden` bug fix** (see that entry, directly above --
found and fixed while this feature was mid-edit in the same working
tree; confirmed via that entry's own note that the two never conflicted).
**Verified live by this feature's own author, after the fact, via
`window.__debug`:** all 8 new selects populate with real options on page
load (`chpMode`/`rchpMode`/`clickMode`/`dblclickMode`: `['Single Pose',
'Tween']`; `chpTargetPose`: the 11 real saved-pose names; the 4
TweenSelectors legitimately empty, no Tween Sequences saved yet).
Switching `chp`'s and `click`'s own Mode selects to 'Tween' via a real
`change` event correctly hid all 7-8 single-pose-only rows and revealed
Tween Sequence for both. Click Hold-Pose's own new Tween mechanism
verified end to end with a fabricated 2-pose test sequence: holding
(`startClickHoldPose('chp')`) played `forward` through to the final
pose's own curl value (89, "ThumbsUp") and held there (no pause phase, as
designed); releasing (`endClickHoldPose('chp')`) correctly entered
`retransition` and settled to `idle`. Click Pose's own Single Pose mode
re-confirmed unaffected (curlIndex progressing toward "Fist"'s value, 96,
mid-transition). 0 console errors beyond the pre-existing, unrelated
`api/save-settings` 404s.

Cache-buster bump: `index.html`'s own `main.js?v=97` -> `?v=98`.

--------------------------------------------------------------------------------
<next entry follows the same divider + dated-header pattern>

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.
