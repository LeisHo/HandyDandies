HANDY DANDIES -- CODE SUMMARY
================================================================================

Status: see ../README.md for the project-structure layout and how to run it;
this file is a quick orientation pointer into the actual code, not a
duplicate of the README's tree. Reflects the initial build plus a same-day
follow-on round (camera pan/zoom, layout offsets, Toon Shading/Outline/full
Lighting), 2026-09-12.

--------------------------------------------------------------------------------
FILE MAP

- `index.html` -- entry point: import map pinning three.js@0.169.0 via the
  unpkg CDN (no build step), the `<canvas id="viewport">`, a loading
  indicator, and the `<script type="module" src="src/main.js">` tag. Its
  own cache-busting query string (`?v=N`) is bumped by hand whenever a fix
  doesn't appear to take effect after reload -- see GOTCHAS.
- `src/main.js` -- all real logic: dev-panel group definitions, scene/
  camera/lighting setup, OrbitControls (pan+zoom only), cursor-to-3D-target
  raycasting, model loading + per-instance skeleton cloning, grid layout
  with row-offset patterns, toon material + outline mesh/pass, and the
  render loop. ~430 lines as of this entry.
- `src/style.css` -- page chrome (canvas/loading text) + the dev panel's
  full CSS (`.dp-*` classes) -- copied verbatim from HANDO's own
  `src/style.css`, itself the workspace's generic dev-panel styling
  (CLAUDE.md §12). No project-specific CSS beyond the page chrome.
- `src/devpanel/devPanel.js` -- the generic, project-agnostic dev-panel
  engine (CLAUDE.md §12a-12h). Copied verbatim from HANDO's own copy -- do
  not fork it for project-specific behavior; add new controls via
  `main.js`'s own `DEV_GROUPS` instead.
- `data/processed/HAND3D/Hand2.glb` -- the rigged hand asset, copied from
  HANDO's own `data/HAND3D/HAND-021/Hand2.glb`. See PROJECT_SUMMARY.txt's
  Data Sources section.
- `src/index.html`, `data/raw/`, `scripts/{active,archive}/`, `logs/`,
  `results/`, `tests/` -- empty, standard project skeleton (CLAUDE.md §11),
  not yet used by anything.

--------------------------------------------------------------------------------
ARCHITECTURE

Inside `main.js`, in roughly this order:

1. **Dev panel** (`DEV_GROUPS`, `initDevPanel()`) -- Field Layout (rows,
   columns, ROW spacing and COLUMN spacing as 2 independent sliders, hand
   scale, Alternate/Progressive row-offset controls + a mode-switch
   checkbox), Cursor Tracking, Camera (X/Y/Z position + FOV only -- see #2),
   Lighting (full parity with HANDO's own group, including Key Light Aim
   Height), Toon Shading (steps/threshold/floor/ceiling/base tint/texture
   influence/tint/rim intensity-power-color -- ported from HANDO), Outline
   (enabled/use-pass/color/hull-thickness/pass thickness-strength-glow --
   ported from HANDO), Background, Debug (grid helper, wireframe).
   `initDevPanel()`'s onChange callbacks are detached and reattached around
   the init call itself (see GOTCHAS).
2. **Camera: pan + zoom only, no orbit** -- `OrbitControls` with
   `enableRotate: false`, `mouseButtons: { LEFT: PAN, MIDDLE: DOLLY, RIGHT:
   PAN }`, `touches: { ONE: PAN, TWO: DOLLY_PAN }` (gives mobile one-finger-
   drag-pan/two-finger-pinch-zoom for free -- no custom touch code). Camera
   X/Y/Z/FOV/Zoom dev-panel sliders all DRIVE the camera (`applyCameraControl()`)
   and are kept in sync FROM the live camera every frame
   (`syncCameraPanelFromLive()`, using the imported `syncValue()` -- it
   skips a row currently focused, so this never fights a live slider edit).
   `cameraZoom` specifically reads/writes `camera.position.distanceTo(
   controls.target)` via `setCameraDistance()` (moves the camera along its
   existing camera-to-target line, leaving `controls.target` untouched) --
   the exact same axis OrbitControls' own scroll/pinch dolly already moves
   the camera along, so the slider and a scroll tick are indistinguishable
   to anything reading `camera.position` afterward. Ported from HANDO's own
   2-way Camera-group pattern, simplified (no Yaw/Pitch, since a
   non-orbiting camera has no "look direction" concept to expose).
3. **Camera/lighting/target-plane framing is frozen after the FIRST build**
   (`framedOnce` flag in `rebuildField()`) -- direct request ("Field layout
   shouldn't affect view scale"). Rows/columns/spacing/offset changes only
   ever touch `relayoutField()` (hand positions/scale), never camera or
   `sceneState.fieldRadius` again after the first build.
4. **Cursor -> 3D target** (`cursorNDC`, `updateCursorTarget()`) -- a
   `window` `pointermove` listener updates `cursorNDC`; each frame, a
   `THREE.Raycaster` casts from the camera through `cursorNDC` and
   intersects a `THREE.Plane` (`targetPlane`) positioned in front of the
   hand field, at a depth of `fieldRadius * cfg.targetDepthFactor` (frozen
   per #3). A REAL 3D point with a depth offset from the hands' own z=0
   plane -- critical for the requested pitch/reveal effect.
5. **Model loading + bind-pose measurement** (the `GLTFLoader().load(...)`
   callback) -- finds the skinned mesh, hides stray meshes, measures the
   bind-pose "pointing" direction (bone `rHand` -> bone `rMid3`, world
   space, root at identity transform at this point). `alignQuat` rotates
   that direction onto local -Z; `handLengthRaw` is its measured length
   (used for both auto-scaling AND the outline hull-thickness reference,
   replacing HANDO's own bounding-radius reference since each hand instance
   is its own separately-scaled object here). Also builds the ONE shared
   `toonMaterial` here (`createToonMaterial(skinned.material.map)`).
   Same block also measures `palmFaceCorrectionQuat` (Cursor Tracking's
   own "Palm Faces Cursor" checkbox, see GOTCHAS) -- the bind-pose palm-
   plane normal (`(pinky-base - wrist) x (index-base - wrist)`, bones
   `rPinky1`/`rIndex1`/`rHand`), rotated onto local -Z the same way
   `pointDir` is, so it can be composed onto the live lookAt quaternion
   as a fixed correction when that mode is on.
6. **Field layout** (`rebuildField()`, `relayoutField()`,
   `computeBaseScale()`) -- `rebuildField()` clears and recreates every hand
   instance: `cloneSkeletal(modelRoot)` (three.js `SkeletonUtils.clone`,
   imported as `{ clone as cloneSkeletal }` -- see GOTCHAS), `clone`'s own
   skinned mesh gets its own `.material = toonMaterial.clone()` (own
   `clippingPlanes`/`onBeforeCompile`, corrected from an earlier shared-
   material design -- see GOTCHAS), plus its own `outlineMesh`
   (`buildOutlineMesh()`, a `sourceMesh.clone()` sharing that hand's own
   skeleton, using its own cloned `outlineMaterial` too) parented under
   `clone` so it inherits the bind-pose `alignQuat` automatically. `relayoutField()` (cheaper --
   repositions/rescales only) lays out a centered grid using INDEPENDENT
   `cfg.rowSpacing`/`cfg.columnSpacing`, applying a per-row X offset: in
   Alternate mode, odd rows shift by a fixed `cfg.alternateRowOffset`; in
   Progressive mode, row `r` shifts by `r * cfg.progressiveRowOffset`
   (mutually exclusive via `cfg.useProgressiveOffset`).
   `computeBaseScale()` uses a FIXED reference length
   (`HAND_SCALE_REFERENCE_LENGTH = 8`, chosen to match this project's own
   original spacing defaults so existing settings don't visually jump) --
   per direct request, hand scale and grid spacing are fully independent:
   dragging Row Spacing or Column Spacing only repositions hands via
   `relayoutField()`, never rescales them; only `cfg.handScale` does that.
   (An earlier version derived the scale reference from `Math.min(
   rowSpacing, columnSpacing)`, so dragging either spacing slider silently
   changed hand size too -- confirmed removed directly: forcing Row
   Spacing 10->30 and Column Spacing 11.5->2 left `computeBaseScale()`'s
   result numerically unchanged, while the Hand Scale slider still visibly
   changes it.)
7. **Toon material + outline** -- `createToonMaterial()`/
   `makeGradientTexture()`/`rebuildGradientMap()` and the outline
   `ShaderMaterial` + inverted-hull shaders are ported from HANDO. Each
   field hand gets its own `.clone()` of both materials (own
   `clippingPlanes`, for Hide Wrist/Arm Length -- see GOTCHAS for why a
   shared material broke this); the single Pose Preview hand uses the
   template materials directly. `forEachToonMaterial()`/
   `forEachOutlineMaterial()` keep every existing "change a shared
   material setting" dev-panel control (base tint, wireframe, outline
   color/thickness, gradient rebuild) working across all the per-hand
   clones plus the template.
   Both materials run with `depthTest: true` (the default) -- see #9 below
   for the depth-buffer-per-hand technique that makes cross-hand render
   ordering work without needing to disable it. `updateOutlineVisibility()`
   toggles between the 2
   outline techniques: hull-mesh `.visible` per instance, or a single
   `OutlinePass` with `selectedObjects` set to every hand's own skinned
   mesh at once (cheaper for many instances than N extra hull meshes,
   though both are implemented and dev-panel toggleable, matching HANDO's
   own choice; both default OFF currently).
8. **Render loop** (`animate()`) -- self-heals renderer/composer size every
   frame (see GOTCHAS' `window.innerWidth` entry) before `controls.update()`,
   `syncCameraPanelFromLive()`, the cursor-target recompute, the per-hand
   look-at slerp (via `new THREE.Matrix4().lookAt(...)`, not
   `Object3D.lookAt()` -- see below), the per-hand render-order assignment
   (#9), and finally `composer.render()` (RenderPass -> OutlinePass ->
   OutputPass) instead of a plain `renderer.render()`, now that
   post-processing exists.
9. **Cursor-distance render ordering** (`updateRenderOrder()`) -- per direct
   request, hands FURTHEST from the cursor target render on top of nearer
   ones, not whichever is actually closer to the camera. Each hand's
   `effectiveRenderOrder` (persisted on the hand object, not recomputed
   from scratch every frame) normally tracks its own live
   `wrapper.position.distanceTo(cursorTarget)`; `skinnedMesh.renderOrder`
   reads that value directly, `outlineMesh.renderOrder` reads it minus a
   small epsilon so a hand's own outline always draws just before (under)
   its own fill. This has a visual effect despite both materials keeping
   `depthTest: true` (#7) because each hand's fill AND outline mesh also
   get an `onBeforeRender = (r) => r.clearDepth()` hook (attached in
   `rebuildField()`) -- clearing the depth buffer immediately before that
   hand's own draw call means a hand's own triangles still depth-test
   correctly against EACH OTHER (real self-occlusion), while stacking
   BETWEEN hands is decided purely by draw order (`renderOrder`) since
   each hand starts from a fresh depth buffer and therefore always wins
   the draw over whatever was rendered immediately before it. An earlier
   version of this project used `depthTest: false` directly instead (no
   per-hand depth clear) -- simpler, but broke self-occlusion WITHIN a
   single hand's own geometry (see GOTCHAS for the real bug this caused
   and how it was found/fixed). See GOTCHAS for the residual tradeoff the
   current technique still has.
10. **"Prevent Reordering Flash"** (`cfg.preventReorderFlash`, Debug group,
    `REORDER_SMOOTHING` constant) -- when on, `updateRenderOrder()`
    approximates every hand's on-screen footprint as a circle, using the
    mesh's own MEASURED bounding sphere (`handBoundsCenterLocal`/
    `handBoundsRadiusLocal`, computed once at load via `THREE.Box3` +
    `.getBoundingSphere()` on the original skinned mesh, in the same
    identity-transform frame `alignQuat`/`handLengthRaw` are measured in)
    -- NOT `wrapper.position` and NOT a radius derived from the wrist-to-
    fingertip bone distance (see GOTCHAS for why that undersized/mis-
    centered version was a real, reported bug). Each frame, the sphere's
    LOCAL center is put through this hand's actual transform chain (scale
    -> `alignQuat` -> live `wrapper.quaternion` -> `wrapper.position`) --
    a sphere's radius only needs scaling, not rotating -- then projected
    to screen pixels via standard perspective scaling: `pxPerWorldUnit =
    canvasHeight / (2 * tan(vFov/2) * distanceToCamera)`. An O(n^2)
    pairwise check then flags `hand.isOverlapping` for any
    hand currently touching another on screen. A hand that ISN'T
    overlapping anything just snaps straight to its own live cursor
    distance (no lag, fully responsive); a hand that IS overlapping
    something blends toward its live distance instead of snapping
    (`effectiveRenderOrder += (live - effectiveRenderOrder) *
    REORDER_SMOOTHING`, an exponential lerp, `REORDER_SMOOTHING = 0.15`).
    Nothing is ever derived from ANOTHER hand's value -- every hand's own
    order is always purely a function of its OWN live distance (blended or
    not), which is what makes this version structurally immune to both
    failure modes below.
    **This is the 3rd design for this feature -- the first 2 are kept in
    the function's own comment as a measured cautionary tale.** v1 froze a
    hand's ENTIRE order the instant it overlapped ANYTHING, not resuming
    until every overlap cleared -- in a dense/zoomed-in view where most
    hands are always touching some neighbor, no hand ever got a single
    overlap-free frame to unfreeze on, so the WHOLE FIELD froze solid
    PERMANENTLY the moment the checkbox was turned on (confirmed: a
    240-frame simulated sweep at closer zoom showed all 510 hands frozen,
    0 updates for the entire run). v2/v3 tried a per-PAIR lock instead
    (remembering which of 2 overlapping hands should stay on top, nudging
    or unconditionally pinning the other to `partner + epsilon`) -- v3's
    unconditional pin seemed to fix v2's own instability, but in a dense
    field, chains of simultaneous locks (A-under-B-under-C-under-D...)
    compound: every hand in a long chain collapses toward whichever hand
    anchors that chain, discarding real distances almost entirely.
    Measured directly: sampling ~87,000 random pairs whose TRUE distances
    differed by more than 15 world units (a large, unambiguous gap), 21%
    STILL rendered in the wrong relative order under v3 -- worse than no
    fix at all, and exactly the reported regression ("certain hands render
    above a hand further from the cursor" + "flashing... worse than
    before"). The current smoothing design was measured against the same
    87,000-pair test at under 0.1% wrong-order rate, 0 stuck hands across
    150 simulated frames, and roughly half the frame-to-frame flip rate
    (among hands ACTUALLY overlapping on screen) compared to no fix at
    all. Trade-off, disclosed: this is no longer a literal hard freeze
    ("won't change until they clear") -- it's a fast blend that mostly
    prevents instant pops without the 2 failure modes a literal freeze/pin
    kept producing at this field's actual density.
    **A DEEPER, STRUCTURAL limitation surfaced after this design was
    already shipping and the overlap geometry was separately fixed (see
    GOTCHAS): smoothing the NUMBER does not and CANNOT smooth the VISUAL
    SWAP.** `renderOrder` is a discrete sort key -- a hand is drawn
    strictly before or after another, with no partial/blended state in
    between (this whole feature exists because `depthTest:false` removes
    the GPU's own continuous, per-pixel depth resolution). Traced a real
    flip frame-by-frame: the gap between 2 overlapping hands' smoothed
    values shrank perfectly cleanly and monotonically every single frame
    (-1.24 -> -0.92 -> -0.58 -> -0.23 -> +0.11 -> +0.43, zero discontinuity
    anywhere) -- yet the instant that gap crossed zero, the actual draw
    order (and therefore the visible stacking) flipped completely, in one
    frame, same as it would with NO smoothing applied at all. Smoothing
    demonstrably still helps (see the measured wrong-order/flip-rate
    numbers above) by damping NOISE-driven back-and-forth crossings and by
    correcting a hand's numeric order to actually match true distance --
    but it cannot make a GENUINE crossing (the cursor's movement actually
    causing 2 hands' true relative order to swap) look like anything other
    than a hard, instant pop, because the render mechanism has no
    continuous transition to interpolate through. Eliminating that
    specific residual pop would need a fundamentally different technique
    (e.g. a real alpha cross-fade between the 2 competing hands during the
    crossing window, which conflicts with the current opaque/
    `depthTest:false` approach and hasn't been attempted) -- a materially
    bigger change than anything else in this feature's history, flagged to
    the user as a scope/architecture decision rather than attempted
    unilaterally.

11. **Pose** (Pose dev-panel group) -- ported from HANDO's own identical
    group (same rig/bone names, same `Hand2.glb` asset): 25 finger sliders
    (Curl/Splay/2nd-Segment-Splay/Curl-Bias/Tip-Twist x5 fingers),
    Wrist Bend/Splay, Whole-Hand Rotation X/Y/Z, and Hide Wrist. Applies
    IDENTICALLY to every hand for now, per direct request -- there is no
    per-hand pose override yet. `boneRestQuat` (captured once, from the
    ORIGINAL un-cloned skeleton's bind pose, right after load and BEFORE
    `rebuildField()` ever clones it) is shared across every hand's own
    distinct skeleton clone, unlike HANDO's per-model `restQuatMap` (which
    only exists there to support 2 DIFFERENT model files with slightly
    different rest poses -- not a factor here, one GLB cloned N times).
    `applyCurl(fingerName)`/`applyWristPose()` loop over every hand in
    `hands`, calling the same `applyCurlToSkeleton()`/
    `applyWristPoseToSkeleton()` HANDO itself uses (ported near-verbatim,
    including all of HANDO's own live-measured per-finger axis/sign/
    magnitude tables) against that hand's own skeleton.

    **A real bug, found and fixed this round, that HANDO's own identical
    code never had to deal with:** HANDO's finger-curl axes are pre-
    rotated by `modelRoot.quaternion` (its own single whole-hand
    orientation) so a "world" curl axis stays anatomically fixed
    regardless of how the hand is posed -- but HANDO has no OTHER rotation
    layer on top of that. This project's own hands have one:
    `wrapper.quaternion`, the per-hand, per-frame cursor-tracking look-at
    (see `animate()`). Confirmed directly, live: with the SAME slider
    values, 2 different hands (facing 2 different directions at that
    moment) produced 2 DIFFERENT local bone quaternions for the identical
    finger -- proof the curl axis was leaking each hand's own CURRENT
    cursor-facing direction into what should be a purely anatomical
    (pose-only) rotation. Fixed by extending `rotateOnTrueWorldAxis()`
    with an optional `excludeQuat` parameter (that hand's own
    `wrapper.quaternion`) factored out of the bone's world quaternion
    before the axis conversion -- the algebra shows `wrapper` and the
    pre-rotation by `cloneBaseQuat` (below) cancel exactly, leaving the
    axis fixed relative to the mesh's own raw/bind-pose frame regardless
    of EITHER rotation. Re-verified after the fix: the same 2-hands test
    now produces byte-identical local bone quaternions. Tip Twist's own
    axis (computed live from 2 bones' actual current positions, not a
    fixed WORLD_X/Y/Z constant) does NOT get this treatment -- it's
    already a true world-space direction and excluding anything from it
    would be wrong.

    **Whole-Hand Rotation X/Y/Z (`updateCloneBaseQuat()`)** -- changes
    `cloneBaseQuat` only, applied to every hand's own `clone.quaternion`
    every frame alongside the Arm Length position update (#12 below).
    `cloneBaseQuat = alignQuat * wholeHandRotQuat` is identical for every
    hand -- only needs recomputing when a Whole-Hand Rotation slider
    actually changes, never `wrapper`'s own transform (the per-frame
    cursor look-at owns that exclusively). See GOTCHAS for a real,
    measured trade-off between Hide Wrist's own grid-alignment requirement
    and HANDO's own palm-center rotation pivot.
12. **Arm Length / Hide Wrist, made reactive** (direct follow-up request:
    "make the crop or arm length dependent on distance from the cursor, so
    the closer it is the shorter the arm length") -- what was a single
    slider-triggered, shared-across-all-hands calculation is now PER-HAND,
    PER-FRAME (`applyHandArmLength()`, called from `updateRenderOrder()`'s
    own existing per-hand loop, reusing the live cursor-distance value that
    loop already computes for render ordering -- no separate distance calc
    needed). `computeArmLengthT(hand, distanceToCursor)` returns this
    hand's current crop fraction: reactive OFF returns `cfg.hideWrist/100`
    (renamed "Default Arm Length" in the panel, unchanged behavior from
    before this round); reactive ON maps the hand's live cursor distance
    (normalized against `sceneState.fieldRadius * ARM_LENGTH_DISTANCE_NORM`
    -- a judgment call, not a derived constant, documented as such in the
    code) through `armLengthCurveParsed` (a user-edited piecewise-linear
    curve, X=normalized distance, Y=raw crop fraction) into
    `armLengthRangeParsed`'s [min,max] bounds. The underlying math
    (`applyHandArmLength()`) is otherwise identical to the original
    single-hideT version -- same cut-point-lands-exactly-on-the-grid-point
    derivation, just parameterized by a value that can now differ per hand
    and per frame instead of being one shared constant.

    **2 new dev-panel widgets that are NOT devPanel.js control types** --
    Min/Max Arm Length (a 2-handle range bar) and Length Scaling Curve (a
    draggable-point curve editor, piecewise-linear, X=distance/Y=crop).
    Neither UI shape exists in the shared, reused-verbatim `devpanel.js`
    engine, and per this project's own convention ("do not fork it, add
    controls via main.js's own DEV_GROUPS instead") neither was added
    there. Both are registered as plain `type:'text'` controls (`cfg[key]`
    holds a JSON string, e.g. `{"min":30,"max":90}` or
    `[{"x":0,"y":1},{"x":1,"y":0}]`) with their OWN custom DOM/SVG and
    pointer-drag handling laid on top of that control's own (hidden)
    generic text input by `buildArmLengthWidgets()`, called once right
    after `initDevPanel()`. Interacting with either widget calls
    `commitTextControl()`, which sets the hidden input's `.value` and
    dispatches a real `'input'` event -- routing through devPanel.js's own
    existing `commit()`/localStorage-save machinery exactly as if the user
    had typed into a plain text box, so Copy/Save/Reset all keep working
    without either widget knowing anything about devPanel.js internals.
    `cfg.armLengthRange`/`cfg.armLengthCurve` (JSON strings) are parsed
    once into `armLengthRangeParsed`/`armLengthCurveParsed` (cached,
    module-level) by `parseArmLengthConfig()` -- called from each control's
    own `onChange` and once right after `initDevPanel()` -- rather than
    re-parsed 510 times a frame inside `computeArmLengthT()`. See GOTCHAS
    for a real TDZ crash this caching introduced (and where these 3 cached
    vars actually had to live to avoid it) and for why devPanel.js's own
    Reset/Copy-restore path needed a small per-frame resync poll
    (`armLengthWidgetResyncs`, polled once per frame from `animate()`)
    since it writes the hidden input's value directly without firing an
    `'input'` event either widget could otherwise just listen for.

Data flow: cursor position -> NDC -> raycast against a plane in front of
the field -> `cursorTarget` (world point) -> per-hand look-at quaternion ->
slerp toward `wrapper.quaternion` -> render. The model's own bind-pose
alignment (`alignQuat`), per-hand grid position/scale, and camera/lighting
framing are all set once (at load / on their own specific control's
change) and never revisited by an unrelated control's change.

--------------------------------------------------------------------------------
UNTOUCHABLE SYSTEMS

None formally designated yet.

--------------------------------------------------------------------------------
GOTCHAS

- **FIXED (was previously misdiagnosed twice -- see CHANGELOG.txt for the
  full 3-round account).** Fine, noisy-looking surface detail in the
  toon-shaded gray/shadow areas (reported directly, with a screenshot) was
  real `depthTest:false` self-occlusion bleed-through, exactly as the user
  described once asked directly: "I can somewhat see through the hand
  itself and the noise represents the obscured surfaces behind the
  frontmost surface." Two earlier theories in this same entry (real mesh
  geometry revealed by grazing light; this project's zero-ambient/full-
  contrast toon recipe vs. HANDO's softer one) were both directly
  disproven by the user, who confirmed HANDO shows no such noise even at
  `ambientIntensity:0`/`toonSteps:2` (matching this project's own
  defaults) across many different hand poses -- ruling out both lighting
  recipe and asset geometry as the cause. Root cause, confirmed by a
  targeted pixel-diff (not a coarse whole-canvas histogram, which is why
  an earlier pass in this same investigation wrongly looked like only an
  8% effect): isolated a single hand in its bind pose, where a finger
  genuinely overlaps the palm in screen space, and diffed every pixel in
  that overlap region between `depthTest:true` and `depthTest:false`.
  8.2% of pixels differed, spatially clustered exactly on the overlap --
  with `depthTest:false`, those pixels rendered as bright/lit (255) where
  `depthTest:true` correctly rendered them as shadowed (132): a wrongly-
  drawn, differently-lit BACK surface was bleeding through the front
  surface, because `depthTest:false` (needed so cursor-distance render
  order works BETWEEN different hands) also disables the GPU's depth test
  WITHIN a single hand's own triangles, leaving self-occlusion to whatever
  order the GPU happens to submit that hand's own triangles in -- which is
  neither guaranteed nor correct. HANDO uses real `depthTest:true`, so its
  self-occlusion is always correct regardless of pose.

  **Fix implemented** (see `createToonMaterial()`, `ensureOutlineMaterial()`,
  and the `onBeforeRender` hooks in `rebuildField()`): both hand materials
  are back to `depthTest:true` (the default), restoring correct self-
  occlusion. Cross-hand stacking (farthest-from-cursor draws on top) is
  now achieved differently: each hand's own fill mesh AND outline mesh get
  an `onBeforeRender = (renderer) => renderer.clearDepth()` hook, wiping
  the depth buffer immediately before that hand's own draw call. This
  means a hand's own triangles are correctly depth-tested against EACH
  OTHER (self-occlusion fixed), while stacking BETWEEN hands is still
  decided purely by draw order (`renderOrder`, unchanged, still set every
  frame from cursor distance in `updateRenderOrder()`) rather than real
  camera depth -- a hand drawn later always wins the overlap against a
  hand drawn earlier, exactly reproducing the old `depthTest:false`
  cross-hand behavior without its self-occlusion side effect.

  Verified directly, live: (1) the same finger-over-palm region that
  showed an 8.2% bleed-through split (132 vs 255) now reads a single
  clean luminance value (132) across all sampled pixels -- self-occlusion
  confirmed fixed; (2) positioned 2 isolated hands at the SAME screen
  position but 60 world units apart in real camera depth, gave the
  PHYSICALLY FARTHER one the HIGHER `renderOrder` and a `MeshBasicMaterial`
  red tint (to remove any ambiguity from the shared toon shader's own
  tint-uniform machinery) -- confirmed the farther-but-later-drawn hand
  visually wins 100% of the sampled overlap pixels despite being 60 units
  further from camera, proving cross-hand render-order stacking still
  works correctly under the new technique; (3) confirmed zero console
  errors on a fresh reload and that `renderOrder` still correlates
  perfectly with cursor distance across all 510 hands (0 inversions across
  509 sorted pairs). One known trade-off, same category as an existing
  documented one: if two hands' `renderOrder` values land extremely close
  together (the near-tie case "Prevent Reordering Flash" already exists to
  soften), their draw sequence could in principle interleave in a way that
  briefly disrupts the per-hand depth-clear grouping -- a minor, transient
  edge case, not a new class of bug, and not separately reproduced.
- **FIXED — real "arms disappearing" bug, present since Hide Wrist/Arm
  Length shipped, MISDIAGNOSED twice as perspective foreshortening before
  being root-caused (see CHANGELOG.txt for the full account, including the
  2 earlier "not a bug" reports this corrects).** User pushed back
  directly ("bro. dont you see in the video arms dissappearing
  completely?"), then gave the decisive clue once asked to point at a
  specific moment: "the dissappearng happens when my mouse is very far
  from the grid... furthest hands should be pointing at the cursor. so
  they would not be pointing at the camera" -- correctly ruling out
  foreshortening-by-orientation as the explanation. Root cause, confirmed
  live via the debug hook (reproduced exactly with the user's own pasted
  Copy Settings dump loaded into `localStorage`, not guessed defaults):
  `clippingPlanes` lives on the MATERIAL, not the mesh, but every field
  hand's fill mesh (and every outline mesh) shared ONE material instance
  with ONE shared `wristClipPlane`, mutated per-hand via `onBeforeRender`
  right before that hand's own draw call. Three.js does not re-resolve a
  shared material's clipping-plane uniform per individual draw call the
  way the original design assumed -- with 289 hands sharing one material,
  direct measurement showed only 2/289 hands actually rendered anything
  at their own correct, independently-verified screen position; stripping
  `clippingPlanes` off the shared material entirely (as a falsification
  test) immediately restored 288/289. This happened regardless of
  `cropWristEnabled`/`reactiveArmLengthEnabled` (both were OFF in the
  user's own settings the whole time) since `updateWristClipPlaneForHand()`
  and the `clippingPlanes` assignment run unconditionally for every hand.

  **Fix implemented:** every field hand now gets its own `.clone()` of the
  toon material AND the outline material (see `rebuildField()`), each
  with its own `clippingPlanes: [hand.wristClipPlane]` -- a genuine
  per-hand `THREE.Plane` instance (`hand.wristClipPlane`), not a shared
  one. (An initial `customProgramCacheKey` addition meant to let all the
  clones share one compiled program was REMOVED the next day -- see the
  entry below this one, it broke the toon material's own `onBeforeCompile`
  customization entirely.) Every existing "change a shared material
  setting" control (Toon Base Tint,
  Show Wireframe, Outline Color, Hull Outline Thickness, the toon gradient
  rebuild on Toon Steps/Threshold/Shadow Floor/Light Ceiling) now goes
  through 2 small helpers, `forEachToonMaterial()`/
  `forEachOutlineMaterial()`, which apply the change to the template
  material AND every per-hand clone -- the single Pose Preview hand still
  uses the template materials directly (its own `clippingPlanes: []`,
  since it never applies Hide Wrist/Arm Length and has no per-hand
  conflict to begin with, being the only user of its own material).

  Verified live: reproduced the exact failure with the user's own real
  settings (17x17 field, camera FOV 35 at Z=352.46, etc, loaded via their
  pasted Copy Settings dump) and cursor at their own logged (49,47) --
  before the fix, 2/289 hands visible at their calculated screen position;
  after, 276/289 (the remaining 13 are plausible legitimate off-screen/
  overlap cases in a dense 17x17 grid, not re-investigated separately).
  Confirmed the ACTUAL Arm Length feature (Crop Wrist ON + Reactive ON)
  still produces the correct monotonic gradient afterward (nearest-to-
  cursor hand T=0.9, farthest T=0.3, unchanged from before this fix) --
  the bug was in the SHARING of the clip state, not the per-hand T-value
  math itself, which was already correct.
- **FIXED — the per-hand material cloning above (previous entry) broke
  Key Light Color, Toon Base Tint, rim lighting, and the toon-tint/
  texture duotone blend for every field hand, same day.** User-reported:
  "Keylight color no longer seems to work. When I set it to white, I
  still see color being rendered on the hands. Though when I set it to
  red, they do turn red." First hypothesis (the `customProgramCacheKey`
  hack added in the previous entry, meant to keep all 289 clones sharing
  one compiled program) turned out to be a RED HERRING -- removing it
  entirely made no difference, confirmed live by reading back a field
  hand's own actual compiled fragment shader source
  (`gl.getShaderSource()` on `renderer.properties.get(material)
  .currentProgram.fragmentShader`) both before and after: neither
  contained the `toonLuma`/duotone-blend code `createToonMaterial()`'s
  own `onBeforeCompile` is supposed to inject, even with a forced
  PER-MATERIAL-UNIQUE cache key (`material.uuid`) that should have ruled
  out any cross-material sharing explanation entirely. Real root cause,
  found by then inspecting the clone's own `onBeforeCompile` property
  directly: it was three.js's stock `Material.prototype`
  `onBeforeCompile(){}` no-op, not the custom function at all --
  **`Material.clone()`/`.copy()` does not carry over a custom
  `onBeforeCompile` override**, since it isn't part of the fixed property
  list three.js's own `.copy()` implementation handles; every
  `toonMaterial.clone()` in `rebuildField()` silently lost the
  customization the moment it was cloned, reverting every field hand to
  plain unmodified texture-mapped MeshToonMaterial rendering (rim light
  and the tint/texture blend gone; direct lighting like Key Light Color
  still worked normally, since that's a separate, standard three.js
  mechanism untouched by any of this -- explains why red visibly tinted
  the hands while white didn't neutralize them: white light doesn't hide
  an already-colored texture, only the (broken) duotone override could).
  Fixed by explicitly re-assigning `skinnedMesh.material.onBeforeCompile
  = toonMaterial.onBeforeCompile` right after `.clone()` in
  `rebuildField()`. Verified live: sampling the same hand's own rendered
  pixel while toggling Key Light Color between white and red now gives
  neutral gray `(132,132,132)` and red-tinted `(132,0,0)` respectively,
  fully reversible; re-confirmed the wrist-clip-plane fix (previous
  entry) still holds afterward (274/289 hands visible at the same test
  cursor position, consistent with the prior 276/289 measurement). No
  `customProgramCacheKey` in the fixed version -- three.js compiles one
  program per hand's own material now, same cost as it would have without
  ever attempting that optimization.
- **The wrist-to-fingertip bone distance (`handLengthRaw`) badly undersells
  this asset's real rendered size -- `Hand2.glb`'s visible mesh includes a
  full forearm below the wrist, so the actual bounding-sphere radius
  (~23 world units, measured directly via `Box3.getBoundingSphere()`) is
  roughly 2.7x `handLengthRaw` itself, and the sphere's true CENTER sits
  ~13 world units from `wrapper.position`.** The reordering-flash overlap
  check originally used `wrapper.position` as its circle's center and
  `handLengthRaw * 0.5` as its radius -- undersized by ~5x and badly
  mis-centered, so it missed the thumb (and most of the forearm) at many
  rotations, meaning those overlaps got ZERO flash-prevention at all. A
  real, reported bug ("the thumb suddenly pops above the other hand...
  only happens when I move the cursor left and right") traced directly to
  this: yawing (left/right) swings the true off-center mass (thumb side)
  much further across the screen than pitching does, so the mis-centered
  circle's error was most visible on that axis specifically. Fixed by
  measuring the mesh's own `Box3`/bounding sphere once at load
  (`handBoundsCenterLocal`/`handBoundsRadiusLocal`) and transforming its
  center through the hand's real per-frame transform chain every frame,
  instead of assuming `wrapper.position` -- confirmed directly: the same
  grid-neighbor overlap check that found 326,012 overlap-observations
  across a 150-frame sweep with the old circle found 975,875 with the
  corrected one (3x more genuine overlaps now actually detected and
  protected), while the wrong-order and flip-rate metrics held or
  improved slightly. `handLengthRaw` itself was deliberately left
  unchanged (still drives grid scale-fitting and outline thickness) --
  this fix only touches the reordering-flash overlap circle.
- **When the browser-automation tool's own pane is not currently displayed
  at all (confirmed via its own `tabs_context` result: "The Browser pane is
  currently hidden"), `requestAnimationFrame` never fires -- ZERO
  callbacks, confirmed with a vanilla rAF-counting script unrelated to this
  project's own code -- even on a brand-new, explicitly-foregrounded tab.**
  This is a DIFFERENT symptom than the already-documented "hidden TAB"
  case (where `document.hidden`/`window.innerWidth` read as hidden/0): here
  `document.hidden` can read `false` while rendering is still suspended,
  because it's the whole PANE, not the tab, that isn't shown. Consequence
  that cost real debugging time: since this project's own self-healing
  resize fix (see the `window.innerWidth` entry below) lives INSIDE
  `animate()`, it never gets a chance to run either while the pane is
  hidden -- so the camera can get stuck with a NaN projection matrix
  (constructed with `aspect = window.innerWidth/window.innerHeight = 0/0`)
  indefinitely, and anything using `.project(camera)` (e.g. the reorder-
  flash screen-space overlap check) then produces NaN/null-look ing
  results (JSON-serialized as `null` since `NaN` isn't valid JSON) that
  have NOTHING to do with the actual feature logic being tested. Fix when this happens mid-debugging: manually repair the camera
  via a debug hook (`camera.aspect = knownGoodRatio;
  camera.updateProjectionMatrix()`) before trusting any projection-based
  calculation -- don't conclude a feature is broken until the camera state
  itself is confirmed sane.
- **SUPERSEDED -- this entry's own prediction came true and was fixed; kept
  for history.** This project originally used `depthTest: false` on both
  hand materials to make cursor-distance render ordering visible, on the
  theory that this model's mostly-convex static bind pose wouldn't show a
  visible self-occlusion artifact. That theory was wrong: a real user-
  reported bug ("I can somewhat see through the hand... the noise
  represents the obscured surfaces behind the frontmost surface") traced
  directly to this -- confirmed via targeted pixel-diff on a finger-over-
  palm overlap region (8.2% of pixels showing a wrongly-lit back surface
  bleeding through the front one). Fixed by restoring `depthTest: true` on
  both materials and instead giving each hand's own fill + outline mesh an
  `onBeforeRender = (r) => r.clearDepth()` hook (see #7/#9 above and
  `rebuildField()`), so self-occlusion is correct (real depth test, per
  hand) while cross-hand stacking is still decided by draw order alone.
  `depthWrite` stays ON as before, still meaningful for `OutlinePass`'s own
  internal depth comparisons.
- `three/addons/utils/SkeletonUtils.js` (three@0.169.0, pinned in
  `index.html`'s import map) does NOT export a `SkeletonUtils` namespace
  object -- it exports `clone`/`retarget`/`retargetClip` directly as named
  exports (confirmed by fetching and reading the actual served module
  source). Use `import { clone as cloneSkeletal } from
  'three/addons/utils/SkeletonUtils.js'`.
- `initDevPanel()` can fire a restored control's `onChange` synchronously
  before scene objects (`camera`/`keyLight`/etc.) exist yet -- a TDZ crash.
  `main.js` detaches every control's `onChange` into a `Map` immediately
  before calling `initDevPanel()` and reattaches them right after -- the
  same mitigation HANDO's own `main.js` uses. Any NEW `DEV_GROUPS` control
  is automatically covered.
- The model's bind-pose "pointing" direction was MEASURED (`rHand` ->
  `rMid3`), not assumed. Re-verify if `Hand2.glb` is ever swapped for a
  different export of the "same" rig -- HANDO's own GOTCHAS document a real
  case where 2 exports of nominally the same rig had different rest-pose
  quaternions for the same bone names.
- Each hand instance's `clone.quaternion` (bind-pose alignment, set ONCE)
  and its `wrapper.quaternion` (live cursor-tracking rotation, touched every
  frame) are deliberately 2 separate transforms -- don't collapse them.
- **`window.innerWidth`/`innerHeight` can read 0 at script-parse time in
  this dev environment (a real, intermittent early-page-life quirk, same
  class already documented in DOTFLICKO's own CODE_SUMMARY) -- confirmed
  directly: constructing the renderer/`EffectComposer`/`OutlinePass` at 0x0
  left every internal render target permanently zero-sized
  (`renderer.getSize()` reporting `[0,0]`, `GL_INVALID_FRAMEBUFFER_OPERATION:
  ...Attachment has zero size` spamming the console) until a real `resize`
  event happened to fire.** Fixed by self-healing every frame in
  `animate()`: compare `renderer.getSize()` against the CURRENT
  `window.innerWidth`/`innerHeight` and re-apply the full resize (camera
  aspect, renderer, composer, outlinePass resolution) on any mismatch,
  factored into one `applyRendererSize()` function shared by both the
  `resize` event listener and this per-frame check.
- **Separately (NOT the same bug as above, and NOT fixed by the self-heal):
  this session's own browser-automation tool reports `document.hidden ===
  true` and `window.innerWidth/innerHeight === 0` whenever queried via its
  JS-execution tool -- confirmed on a FRESH, explicitly-foregrounded tab,
  not just a backgrounded one -- while a `screenshot` call on the exact same
  tab at the exact same moment shows correct, fully-rendered output at the
  real viewport size.** This is a characteristic of how that specific tool
  drives/queries the page, not a bug in this project's own code -- a real
  visitor's browser never reports its own actively-viewed tab as hidden.
  Don't trust `document.hidden`/`window.innerWidth` read via that tool's JS
  execution as ground truth for "is this page actually broken" -- cross-
  check against an actual screenshot first, the same "don't trust one
  signal over a fresh visual check" lesson as the stale-console-error
  gotcha below, generalized to viewport/visibility state too.
- A local static server / ES module imports can serve a stale cached copy
  of `main.js` even after a hard reload, especially across a same-URL
  reload within the SAME tab. A bare `location.reload()` is not reliably
  sufficient for a multi-file ES-module page; bump the top-level URL's own
  cache-busting query string (`index.html?t=N`) when a fix doesn't appear
  to take effect. Separately, `read_console_messages` in this workspace's
  own browser tooling can return STALE, already-fixed errors from before a
  reload as if they were current -- cross-check against a fresh screenshot/
  network request before trusting a lingering console error.
- **Hide Wrist's own grid-alignment requirement (direct request: "the cut
  wrist base should still correspond to the Field Layout points") and
  HANDO's own Whole-Hand-Rotation palm-center pivot CANNOT both be
  satisfied exactly at the same time, for an arbitrary rotation -- they
  are 2 different points on the mesh.** `updateClonePoseTransform()`
  resolves this in grid-alignment's favor (the explicit "main thing"
  request), meaning Whole-Hand Rotation pivots around wherever Hide
  Wrist's own cut point currently is, NOT the palm center HANDO itself
  pivots around -- a real, disclosed behavior difference from HANDO, not
  just at Hide Wrist > 0% but even at 0% (where it pivots around the
  original forearm-base point instead). Measured directly: a first
  implementation attempt DID preserve HANDO's own palm-pivot math (ported
  near-verbatim, generalized with an extra baseline term for Hide Wrist)
  and looked correct in EVERY isolated test (Hide Wrist alone: exact,
  distance ~1e-14; Whole-Hand Rotation alone: exact) -- but a COMBINED
  test (Hide Wrist 30% + Whole-Hand Rotation Y 90°) showed a real,
  non-negligible 0.27-world-unit drift off the grid point, invisible until
  specifically tested together. Root cause: preserving 2 different fixed
  points under one rotation is only possible if the rotation axis happens
  to pass through both, which a general X/Y/Z slider combination won't.
  Fixed by dropping the palm-pivot preservation and instead solving
  directly for whatever `clone.position` keeps the CURRENT cut point (not
  the palm) at the wrapper origin for the CURRENT `cloneBaseQuat`, for any
  rotation -- verified across isolated Hide Wrist, isolated Whole-Hand
  Rotation, and multiple combined tests (all 3 rotation axes together
  with Hide Wrist, multiple hands, multiple grid positions): every case
  landed within float-precision of exactly 0.
- **A direction+distance pair is NOT enough to reconstruct an absolute
  raw-frame position -- a real bug hit while building the fix above.**
  The very first Hide Wrist position-compensation attempt stored only
  `armDownDirRaw` (a unit direction) and `armToWristDistRaw` (a scalar)
  from the wrist/forearm bones, then tried to use
  `direction * distance * hideT` as if it were the cut point's own
  absolute raw-frame position -- it isn't; that expression is a
  DISPLACEMENT, and treating it as a position implicitly (and wrongly)
  assumes the OTHER endpoint sits exactly at the raw content-space
  origin. It happened to look correct in ISOLATED Hide Wrist testing (the
  sign error's effect was masked by another factor specific to that
  simpler case) but broke as soon as Whole-Hand Rotation was combined
  with it (see the entry above). Fixed by storing the actual ABSOLUTE raw
  positions of both bones (`wristPosRaw`, `forearmPosRaw`) and computing
  the cut point as `forearmPosRaw.lerp(wristPosRaw, hideT)` directly --
  exact by construction, no reconstruction-from-a-displacement involved.
  General lesson for this codebase: a "direction + distance" pair can
  represent a displacement between 2 points, but reaching for it to
  stand in for an absolute position anywhere in this project's own raw/
  bind-pose measurement frame needs an explicit, verified anchor -- don't
  assume one endpoint sits at the origin without checking (verified here
  via a live measurement with the whole ancestor chain zeroed out).
- **A "world" rotation axis (`FINGER_CURL_AXIS` etc, ported from HANDO)
  is NOT actually anatomically fixed once a hand sits under an EXTRA
  per-instance rotation layer HANDO never had.** HANDO pre-rotates these
  axes by `modelRoot.quaternion` (its own single whole-hand orientation)
  before `rotateOnTrueWorldAxis()` converts them via the bone's FULL
  world quaternion -- correct for HANDO, because modelRoot IS the
  hand's entire orientation authority. This project's own hands have an
  EXTRA layer on top of the equivalent orientation (`cloneBaseQuat`):
  `wrapper.quaternion`, the per-hand, per-frame cursor-tracking look-at.
  Confirmed directly, live: with identical slider values, 2 hands facing
  different directions at that moment produced 2 DIFFERENT local bone
  quaternions for the same finger -- the ported axis math was leaking
  each hand's own current cursor-facing direction into what should be a
  purely anatomical rotation. Fixed by extending `rotateOnTrueWorldAxis()`
  with an optional `excludeQuat` (that hand's own `wrapper.quaternion`),
  factored out of the bone's world quaternion before the conversion; the
  quaternion algebra shows this exactly cancels the pre-rotation by
  `cloneBaseQuat`, leaving the axis fixed relative to the mesh's own raw
  bind-pose frame regardless of EITHER rotation. Re-verified after the
  fix: the same 2-hands test produces byte-identical local bone
  quaternions. Applies only to the fixed WORLD_X/Y/Z-based axes (Curl,
  Splay, Splay2) -- Tip Twist's own axis is computed live from 2 bones'
  actual current world positions and must NOT get this treatment (it's
  already a true world-space direction, not a canonical constant).
- **"Not cropping correctly while tracking the cursor" / "every arm should
  be cropped equally at all times" was NOT a bug -- it was pure perspective
  foreshortening on a physically-uniform 3D crop, confirmed by direct
  measurement from the user's own screen recording.** Sampled 11 hands
  scattered around the field at the exact moment the recording showed the
  biggest visual disparity: the TRUE 3D length of the remaining forearm
  segment (`armToWristDist`, live bone-to-bone distance) was IDENTICAL
  across every one, `14.630384832518` world units, differing only in the
  15th decimal place -- and grid-alignment held exactly (`0` to `~1e-15`)
  for all of them too, including the specific hands that looked LEAST
  cropped in the recording. What varied was the SCREEN-PROJECTED length of
  that same fixed 3D segment -- 17px for one hand, 56px for another, over
  3x -- purely because each hand points in a different 3D direction
  relative to the camera at any given moment (every hand independently
  tracks the cursor). A hand pointing more edge-on to the camera shows the
  segment near its true length; one pointing more toward/away from the
  camera shows it heavily foreshortened -- the same reason a pencil looks
  shorter viewed end-on than from the side. This is inherent to doing a
  real 3D crop on independently-rotating objects under perspective, not a
  code defect -- confirmed the SAME way the underlying feature itself was
  verified throughout this round: direct measurement (armToWristDist,
  screen-projected length via `.project(camera)`), not visual inspection
  alone. Superseded by the Reactive Arm Length feature (#12 above), which
  reframes this from "crop" to "arm length" and makes the visible amount a
  deliberate function of cursor distance instead of trying to hide the
  foreshortening effect.
- **A cached-state TDZ crash, the same class this project has hit before
  (see the `initDevPanel()`-detach-onChange gotcha) -- caught immediately
  on the first live reload, not shipped.** `armLengthRangeParsed`/
  `armLengthCurveParsed`/`armLengthWidgetResyncs` were first declared next
  to the functions that use them (`parseArmLengthConfig()`,
  `computeArmLengthT()`, `buildArmLengthWidgets()`, all in the Pose
  section, well after `initDevPanel()`) -- but `parseArmLengthConfig()`
  and `buildArmLengthWidgets()` are both CALLED immediately after
  `initDevPanel()`, i.e. BEFORE the module's own top-to-bottom evaluation
  ever reaches those declarations, hitting their temporal dead zone
  (`ReferenceError: Cannot access 'armLengthWidgetResyncs' before
  initialization`, confirmed directly in the console on first load). Fixed
  by moving all 3 declarations up to this file's own early shared-state
  block (alongside `boneRestQuat`/`wristPosRaw`/`forearmPosRaw`), matching
  this file's own established convention that shared mutable state lives
  at the top regardless of where the logic reading it happens to live --
  not next to whichever function happens to use it first.
- **Hide Wrist's grid-alignment guarantee (see #12 above) needed the SAME
  underlying `applyHandArmLength()` math whether the crop value comes from
  the flat Default Arm Length slider or the new per-hand-per-frame
  Reactive computation -- reused directly, not reimplemented, and
  re-verified after the Reactive round with the exact same measurement
  technique as the original (live bone positions vs. `wrapper` world
  position): closest-to-cursor and farthest-from-cursor hands, sampled
  live while Reactive was on and the cursor was moving, both measured
  within floating-point precision of exactly 0 distance from their own
  grid point despite having genuinely different, continuously-changing
  crop values.**
- **The dev panel's own Collapse button (`–`/`+` in the header) hid the
  settings but left the panel's own OUTER BOX at full height -- a real,
  reported bug in `devpanel.js` itself, not this project's own code.**
  `.dp-collapsed`'s CSS only hides `.dp-body`/`.dp-resize`; it can't also
  shrink the panel, for 2 separate reasons that both had to be fixed:
  `panel.style.height` is set as an explicit inline px value (for resize-
  drag persistence) which always wins over a stylesheet rule, AND
  `.dp-panel`'s own `min-height: 140px` (its floor for normal resizing)
  applies unconditionally too -- overriding `height` alone still left the
  box floored at 140px. Fixed in `devpanel.js`'s own collapse handler by
  explicitly swapping BOTH `height` and `minHeight` to the header's own
  natural height on collapse, restoring both real values on expand. Since
  this lives in the shared, reused-verbatim engine, it likely belongs in
  `.claude/TEMPLATE_DEV_PANEL.html` too (per CLAUDE.md §12's own
  maintenance note) -- not back-ported there as part of this round, since
  that's a deliberate, separate step per that same note, not an automatic
  one.
- **Reactive Arm Length's own dual-handle range widget and curve editor
  both had a real, confirmed bug: dragging while the Pose group was
  COLLAPSED corrupted the stored value to `{"min":null,"max":null}`.**
  `getBoundingClientRect()` on a hidden (`display:none` ancestor) element
  returns an all-zero rect, so the drag math divided by a 0-width track,
  producing `NaN`, which `JSON.stringify` silently turns into `null`.
  Fixed with a simple guard in both widgets' own drag handlers (bail out
  of the frame's update entirely if the rect has 0 width/height) rather
  than trying to prevent the drag from starting while collapsed (a widget
  can't easily know when devPanel.js's OWN unrelated collapse toggle is
  about to fire mid-drag).
- **A real, confirmed unit-mismatch bug was the actual root cause of "I
  set the default and max at .5 or less but i still see full arms."**
  `hideWrist` ("Default Arm Length") is a plain 0-100 slider, matching
  every other percent-like control in this project -- typing "0.5"
  produces a genuinely valid but barely-there 0.5% crop, not the intended
  50%, and looks indistinguishable from "no crop" by eye. Root-caused
  directly: typed "0.5" into the live control and confirmed
  `cfg.hideWrist` became exactly `0.5`, not `50`. Not a code bug in the
  sense that 0.5 IS a valid point on that slider's own real scale -- but
  the Length Scaling Curve widget's own axis captions read "(0-1)" at the
  time, which is very likely what primed the "type a fraction" instinct
  for the OTHER (0-100-scale) controls in this same feature. Fixed by
  relabeling the curve's own captions to read in percent too, so every
  control in this feature speaks the same units -- not by changing any
  control's actual underlying scale.
- **Reactive Arm Length's distance normalization used to divide by a
  FIXED constant (`sceneState.fieldRadius * ARM_LENGTH_DISTANCE_NORM`) --
  a real, reported bug ("why is it that sometimes when i place the cursor
  far away, the hands furthest from the cursor start disappearing, even
  though i have a min length of more than 0").** Root cause, confirmed by
  direct measurement: once the cursor moves far enough from the WHOLE
  field that every hand's distance exceeds that fixed reference, every
  hand's normalized distance clamps to the same value (1.0), collapsing
  ALL relative variation across the field -- measured directly, with the
  cursor placed far outside the field, both the nearest and farthest hand
  in the field landed on the identical crop value. Fixed by normalizing
  against the CURRENT field's own live min/max distance-to-cursor instead
  (a cheap pre-pass over all hands in `updateRenderOrder()`, before the
  main per-hand loop) -- the curve's full domain is now always spread
  across whatever hands are actually in the field, regardless of how far
  the cursor wanders. Re-verified with the cursor placed extremely far
  away (distance ~4000-4400 units, field radius ~185): the nearest hand
  measured crop 0.9 (max bound) and the farthest measured 0.3 (min bound)
  -- the full gradient held, where the old fixed-reference version would
  have collapsed both to the same value.
- **Length Scaling Curve is now a real smooth spline (Catmull-Rom,
  C1-continuous, passes exactly through every control point), not
  piecewise-linear** -- direct correction, with a Photoshop/Lightroom
  tone-curve editor given as the visual reference ("it should not be
  angled lines, it should be a smooth curve where ever i drag it").
  Parameterized by X-position within each segment using the immediate
  neighbor on each side (a common simplification vs. true arc-length
  parameterization, visually indistinguishable for this kind of editor).
  The SVG widget draws the SAME `evaluateArmLengthCurve()` function the
  real per-hand math calls, fine-sampled at 48 points into a `<path>` --
  not a separately-approximated visual, so the drawn curve is always
  exactly what's actually applied.
- **A defensive gap in the new Mouse Tracking Log's click handler --
  caught live while testing, not shipped.** `e.target.closest('.dp-panel')`
  throws if `e.target` isn't a real Element (confirmed via a synthetic
  test event dispatched directly on `document`, which has no `.closest()`
  method) -- a genuine user click always targets a real element in
  practice, but the failure mode costs nothing to remove entirely via
  optional chaining (`e.target?.closest?.(...)`) rather than relying on
  that always being true.
- **A concurrent session (working from the HANDO project, per direct
  request: "I may want the possibility of saving poses in the Hando app,
  then somehow importing those poses into this app") added a "Saved
  Poses" import list-picker and a standalone "Pose Preview" mini-viewer
  to this project's own `main.js`/`devpanel.js` WHILE this round's own 6
  fixes were being implemented in the same working tree.** Confirmed via
  direct diff inspection (not assumed) before touching anything further:
  their changes reuse `applyCurlToSkeleton()`/`applyWristPoseToSkeleton()`
  with a new, backward-compatible `values = cfg` parameter (defaulting to
  this project's own existing behavior at every existing call site) to
  pose a SEPARATE, standalone preview hand from a saved item's own values
  without touching the live field. Given how deeply their changes and
  this round's own changes ended up interleaved (even within shared
  function signatures), a clean per-session hunk split -- the approach
  successfully used for `devpanel.js`'s own isolated collapse-button fix,
  see the ETA History/CHANGELOG entry for this round -- was judged NOT
  safely possible for `main.js` without real risk of breaking their
  feature; both sessions' changes were committed together, with honest,
  explicit attribution in the commit message and here, rather than
  either silently claimed as one session's own work or held back
  entirely (which would have also withheld this round's own 6 fixes).
- **Mouse Tracking Log's own settings-change logging (`setupSettingsChangeLog()`)
  is ONE delegated listener on `.dp-panel`, not per-control wiring.**
  Direct follow-up request: "if i click a settings in the dev panel, it
  logs what i clicked and what setingg i had set." Listens for 'input' AND
  'change' bubbling up from WHATEVER native control the user touched
  (range slider, checkbox, color picker, the Arm Length widgets' own
  hidden text inputs, etc.), reads that row's `data-key` and looks up
  `cfg[key]` -- always already current by the time this fires, since the
  event target's own listener (devPanel.js's own `commit()`) runs before
  an ancestor's during bubbling. `lastLoggedByKey` skips only an EXACT
  duplicate (the same control firing 'input' then 'change' for the
  identical value); a genuinely different value during a slow slider drag
  still logs every distinct tick on purpose -- a debug log should show
  the real sequence, not just the final committed value. Works for every
  existing control (including this round's own new ones) and any FUTURE
  control added later, without either needing its own onChange touched.
- **FIXED — dev panel opened on the wrong tab (Landscape) on an actual
  desktop-sized window.** User-reported directly. Root cause in
  `src/devpanel/devPanel.js` itself (this file is "reused verbatim from
  HANDO" per this project's own file map -- see PROJECT_PROGRESS.md's
  "What's next" for the cross-project implication, since HANDO's own
  copy of this file likely has the identical bug): `realDeviceClass()`
  (desktop/mobile/landscape classification from `window.innerWidth`/
  `innerHeight`) is read exactly once, synchronously, at the very top of
  `initDevPanel()`, to set the panel's initially-active tab
  (`editingDevice`). This project already has a documented gotcha that
  those 2 values can read wrong at that exact early moment (main.js's
  own renderer has a frame-by-frame self-heal specifically because of
  it) -- but the panel's own tab selection had no equivalent correction:
  the existing `resize` listener only re-syncs per-device VALUES for a
  genuine later resize, never which tab is shown active, and never fires
  at all if the viewport was simply wrong once at load with no resize
  event following it. Reproduced the mechanism live using this
  environment's own separate, already-documented `window.innerWidth`-
  reads-0-via-the-JS-exec-path tool quirk (not a page bug) as a stand-in
  for a real-world bad initial read. Fixed with a short self-heal window
  (up to 30 `requestAnimationFrame` ticks, ~0.5s, right after the panel
  is built): re-checks `realDeviceClass()` each frame and corrects
  `editingDevice` once if it differs, but stops immediately the moment
  the user manually clicks a tab (a new `userSwitchedTab` flag) and stops
  after the frame budget regardless -- a load-time correction only, not
  a standing "tab always follows resize" behavior (which would wrongly
  snap back to Desktop mid-session while someone's deliberately viewing
  Mobile settings and happens to also resize their browser). Verified
  live: a fresh load on a genuine 1024x768 viewport correctly shows
  Desktop active; manually switching to Landscape afterward sticks
  (re-checked 600ms later, past the heal window). `devpanel.js`'s own
  cache-busting import bumped `?v=4` -> `?v=5`.
- **"Palm Faces Cursor" (Cursor Tracking group, new checkbox) -- whole-
  object rotation only, deliberately NOT a pose/skeleton feature.**
  Direct request, with an explicit correction mid-task when an
  investigation step looked pose-adjacent ("you shouldnt be doing any
  posing work" / "when i say rotate the hand... I just meant rotate the
  entire model"). Default (fingers point at cursor) already works by
  rotating each hand's WRAPPER so its `alignQuat`-aligned local -Z (the
  bind-pose wrist->middle-fingertip direction) faces the cursor via
  `THREE.Matrix4.lookAt()`; this feature needed the PALM-NORMAL direction
  to be the one aimed at the cursor instead, without touching that
  existing per-frame lookAt code's own shape or any bone/curl value.

  Measured the palm-plane normal the same way `pointDir`/`alignQuat`
  already are (bind-pose bone world positions, identity-transform frame,
  at model-load time): `(pinkyBase - wrist) x (indexBase - wrist)`,
  bones `rHand`/`rIndex1`/`rPinky1`, normalized. Sign (which of the 2
  possible cross-product orders points OUT of the palm vs. into the back
  of the hand) was CALIBRATED LIVE rather than assumed: transformed each
  candidate direction through `alignQuat` and a real hand's own current
  `wrapper.quaternion` (a hand positioned below the cursor, under the
  existing default mode), and checked which candidate's world-space
  direction pointed AWAY from the camera -- matching this project's own
  already-confirmed reference behavior ("hands under the cursor show the
  back of their hand to the camera... this is correct," direct user
  report from an earlier round). `pinky x index` (in that order) was the
  one that measured facing away from camera for that below-cursor hand,
  confirming it's the outward palm normal.

  The actual live-tracking change is a single composed quaternion, not a
  parallel code path: `palmFaceCorrectionQuat = Quaternion.setFromUnitVectors(
  palmNormalRaw.applyQuaternion(alignQuat), (0,0,-1))` -- a FIXED, one-
  time-computed correction (same block as `alignQuat` itself, at model
  load). Given `Q_lookAt.apply((0,0,-1)) == dirToCursor` (lookAt's own
  guarantee) and `R = Quaternion.setFromUnitVectors(v, (0,0,-1))` implies
  `R.apply(v) == (0,0,-1)` by construction, composing `desired =
  Q_lookAt.multiply(R)` (three.js quaternion-multiply order: apply `R`
  first, then `Q_lookAt`) gives `desired.apply(v) == Q_lookAt.apply(
  R.apply(v)) == Q_lookAt.apply((0,0,-1)) == dirToCursor` for `v` =
  `palmNormalAligned` -- i.e. the SAME lookAt quaternion the default mode
  already computes, with one extra `.multiply()` when the checkbox is on,
  redirects the palm (not the fingertip axis) to the cursor. No new
  per-frame cost of consequence, no skeleton/pose code touched at all.

  Verified live, 2 ways: (1) mathematically -- recomputed the exact same
  correction independently (bind-pose bone extraction via
  `skeleton.boneInverses`, since a posed/live skeleton's own
  `getWorldPosition()` no longer reflects bind pose) and confirmed
  `worldPalmNormal.dot(dirToCursor) = 0.999999995` (correct to float
  precision) for a hand below a centered cursor; (2) visually -- forced
  every hand's `wrapper.quaternion` directly (no damping) with the
  feature on and cursor centered, then compared cropped/upscaled regions
  above/below/left of the cursor: above vs. below showed a clear
  vertical-flip relationship, left showed the hand's own long axis
  rotated to roughly horizontal -- matching the qualitative "rotated
  ~180 above, ~90 to the side" pattern described in the request. Default
  OFF (`def: false`) -- nothing changes until opted in.

  **CORRECTED 2026-09-14, same day:** the live calibration above was
  WRONG in practice -- direct user report, "the palm face cursor is
  doing the exact opposite. rotate them 180 degrees." `pinky x index`
  was actually the BACK-of-hand normal, not the outward palm normal;
  fixed by swapping the operand order to `index x pinky` (the exact
  negation, per cross-product anticommutativity `a x b = -(b x a)`).
  **Also found a real limitation in the "verified 2 ways" claim above:
  the "(1) mathematically" dot-product check is TAUTOLOGICAL** -- it
  re-measures the SAME formula used to build the correction, so it will
  self-confirm to ~1.0 regardless of which cross-product order is fed
  in (confirmed directly: re-ran that identical check with BOTH the old
  and new operand order and got 1.0 both times). It can verify the
  composition LOGIC (that the correction quaternion is being applied
  correctly) but can never catch a wrong SIGN choice -- only a real
  visual/anatomical check can do that, and this project's flat-shaded
  toon mesh doesn't render a strongly distinct palm-vs-back texture,
  making even the "(2) visually" check from the original round weaker
  evidence than its own confident phrasing suggested (it could
  distinguish rotation ANGLE/axis correctly, per the vertical-flip/
  horizontal-rotation pattern, but not which FACE of the hand was
  showing). Any future feature built the same way (calibrate a normal's
  sign via a self-referential dot-product check) should get an
  INDEPENDENT ground-truth check instead -- e.g. a known anatomical
  landmark (thumb position relative to the palm plane) or an actual
  side-by-side visual comparison against a reference pose, not a check
  that re-derives its own answer.

  **Same-day follow-on: "Palm Face Rotation (Deg)" slider (-180 to 180,
  default 0), direct request ("Provide me a slider to control that
  rotation actually")** -- rather than trust a future calibration to be
  right by construction (the exact assumption that just failed), the
  correction is now computed LIVE every frame instead of once at model
  load. `palmFaceCorrectionQuat` (the fixed module-level quaternion) was
  replaced with `palmNormalAligned` (the raw calibrated direction, still
  measured once) plus `computePalmFaceCorrectionQuat()`, called once per
  frame in `animate()`'s tracking block (not once per hand -- the
  correction is identical for every hand, same as `alignQuat`). The
  slider rotates `palmNormalAligned` around the hand's own pointing axis
  (local -Z in the aligned frame, i.e. the same axis `pointDir` sits on)
  before building the correction -- chosen specifically because it's
  perpendicular-ish to the palm normal for a roughly-flat hand, so the
  slider sweeps the aimed direction continuously through the FULL range
  (palm at 0, edge-on at +-90, exact opposite/back-of-hand at +-180)
  rather than an axis that would leave the correction unchanged.

  This time, verified with a genuinely INDEPENDENT check (not the
  tautological kind flagged above): built the OLD (pre-fix) formula's
  correction quaternion completely separately from the new
  slider-based one, then compared it against the new formula's own
  output at offset=180 -- confirmed the two are the same rotation
  (quaternion dot ~0.999966) -- proving the roll-axis choice is
  mathematically sound and that the slider's two extremes genuinely
  correspond to "new/correct" (0) and "old/wrong" (+-180), not just
  self-consistent nonsense.

  **CORRECTED 2026-09-14, same day: the roll AXIS itself was wrong.**
  Direct follow-up ("The palm face thing isnt rotating correctly...
  the rotation axis should be perpendicular to the wrist cropping
  plane"). Local -Z (i.e. `pointDir`, wrist-to-fingertip) was a guess
  made without an explicit axis spec at the time -- the real spec is
  the WRIST CROP PLANE's own normal (forearm-to-wrist), the exact same
  direction `updateWristClipPlaneForHand()` already computes per-hand
  for the Arm Length crop, NOT the pointDir/alignQuat axis. Added
  `wristCropNormalAligned` (measured once at load, `rForearmBend` ->
  `rHand`, same raw bind-pose frame as `pointDir`), and
  `computePalmFaceCorrectionQuat()` now rolls around this instead.
  Confirmed this is a REAL fix, not a no-op: measured both directions
  live and found they differ by 28.35 degrees in this rig's actual
  bind pose (forearm-to-wrist isn't perfectly collinear with
  wrist-to-fingertip -- a genuine rig quirk). Also confirmed the new
  axis sits ~90.5 degrees from the palm normal (essentially
  perpendicular), which is what makes the slider sweep through a
  proper full range rather than a lopsided one. Offset=0 is unchanged
  by construction (a zero-angle rotation is the identity regardless of
  axis), so the prior round's already-fixed default orientation is
  untouched by this change.
- **Ported the built-in "Dev Panel" chrome-styling group's expansion from
  `TEMPLATE_DEV_PANEL.html`, scoped to "just the chrome group"** per
  direct request when asked how much of the template's own growth (also:
  a dynamic per-control Mobile/Landscape visibility system, whole-panel
  Named Setting States, a Standard Text Settings battery -- all left for
  a later round, not overlooked) to pull in. Added 24 controls to
  `devpanel.js`'s own `builtInGroup` (16 -> 40 total): per-category
  (Title/Tab/Group/Settings/Button) Bold toggles + Letter Spacing + Line
  Spacing sliders, Capitalize Title, Button Font Size, Button Height,
  Button Text Border (extra vertical button padding), Setting Number
  Font Size, and 4 new color pickers (Group/Button/Setting-Number/Tab
  Text Color) -- all wired to new `--dp-*` CSS custom properties in
  `style.css`, each with a fallback matching this project's own CURRENT
  hardcoded look (e.g. `font-weight: var(--dp-title-weight, 600)`) so
  the port is a genuine no-op until a control is actually touched.
  Renamed the existing `toggleCaps()` helper to `toggleClass()` (still
  used for the original 4 caps toggles unchanged) since it's now also
  used for the new Bold toggles -- a same-shape boolean-class-toggle
  helper, not caps-specific despite the old name.

  **CORRECTED 2026-09-14, same day:** the "Scroll Strength" exclusion
  above was WRONG. Direct user correction: "Scroll Strength is the
  srolling intensity witin the dev panel. impelment that." Re-read the
  template's actual wiring more carefully this time -- `.dev-panel-
  scroll-content` (the element its wheel handler targets) is, in fact,
  the template's OWN name for its panel body's scrollable area (its own
  CSS literally styles `.dev-panel-scroll-content` as the panel's
  scrollbar/overflow container), not some other project's game content
  as first assumed from a shallower read. Implemented for real this
  round -- see the later GOTCHAS entry below for the full account.

  **Found and fixed a real, independently significant bug while
  verifying this port live:** the very first live check (setting a new
  color, a new Bold toggle, and Capitalize Title) showed the CSS custom
  property being set correctly on the panel element every time, but the
  computed style on the actual descendant elements never changed --
  confirmed via `document.styleSheets` that the browser was rendering
  from a STALE CACHED copy of `style.css`, despite a fresh `fetch(...,
  {cache:'no-store'})` against the same URL correctly showing the new
  rules server-side. Root cause: `index.html`'s own
  `<link rel="stylesheet" href="src/style.css">` has NEVER had a cache-
  busting query string, unlike every other served file in this project
  (`main.js?v=N`, `devpanel.js?v=N`) -- meaning ANY style.css edit in
  this project's history could have silently failed to take effect in a
  browser that had already cached an older copy, not just this round's
  own change. Fixed by adding `?v=2` to that link tag; bump it on every
  future style.css edit, the same convention already used for the 2 JS
  files. Verified live (fresh tab, no prior cache) after the fix: new
  color/bold/capitalize-title controls all correctly changed their
  target elements' computed styles; default state (nothing touched)
  produced the SAME computed styles as before this round's own change
  (title/group weight 600, button weight 400, matching the pre-existing
  hardcoded look) -- confirming the port really is a no-op by default.
- **Same-day follow-on round: Scroll Strength (real this time), unlimited
  group nesting, full subgroup reorganization, and template-matching
  actual values/colors -- 4 direct requests together.**

  **Scroll Strength**: corrected from the wrong exclusion above --
  confirmed live this IS the panel's own body-scroll wheel intensity.
  Implemented on `body` (`.dp-body`) right after its own creation:
  `body.addEventListener('wheel', (e) => { const strength =
  cfg.dp_scrollStrength; if (strength === undefined || strength === 1)
  return; e.preventDefault(); body.scrollTop += e.deltaY * strength },
  { passive: false })` -- reads `cfg` live rather than a captured
  closure value, so the control's own onChange needs no DOM work at
  all. `strength === 1` is treated as "let native scroll happen," not a
  no-op `* 1` multiply, since native deltaY scaling already varies by
  browser/OS/input device. Verified live: dispatched a synthetic wheel
  event (`deltaY: 100`) at the default strength (0.2) and at a changed
  strength (3), confirming `body.scrollTop` moved by exactly `deltaY *
  strength` in both cases (20px and 300px respectively).

  **Unlimited group nesting**: direct report, "I currently cant nest
  groups within each other." Read `setupReorder()`'s own `getTargets`
  callback for the GROUP-level call in `buildDevPanel()` before touching
  anything -- found the 1-level cap lived ONLY there (a
  `:scope > .dp-group > .dp-group-body` selector that only ever matched
  TOP-LEVEL group bodies, plus an explicit "if this group already has
  its own nested children, only offer top-level" exclusion). Separately
  confirmed `captureGroup()`/`applyOrder()` (the save/restore
  round-trip) were ALREADY genuinely recursive (each calls itself on
  every direct child group) despite comments nearby claiming "one level
  deep only" -- those comments described the OBSERVED effect of the
  drag-reorder's own cap, not an actual limitation in either function,
  so neither needed a code change, only a corrected comment. Fixed by
  changing the target-body query to `groupsEl.querySelectorAll('.dp-group
  > .dp-group-body')` (no `:scope >`, matches every group's body at any
  depth) filtered by `!item.contains(b.closest('.dp-group'))` (excludes
  the dragged group's own body and every descendant's body, preventing a
  circular drop). The row-level `setupReorder` call already had no depth
  restriction at all (rows could already be dropped into a nested group)
  -- only the group-into-group case needed this fix. Left the LIST-
  PICKER's own separate 1-level group-nesting cap (`dp-lp-group`, the
  Saved Poses feature) untouched -- confirmed via direct comment reading
  that it's a genuinely different system, out of scope for a request
  specifically about the main settings panel, though corrected that
  system's own comment which had (now-inaccurately) claimed the 2
  systems shared "the same cap." Verified live: a body already nested 2
  levels deep (`Dev Panel > TEXT > Dev Panel Title`) is correctly
  offered as a valid drop target by the fixed query, which would produce
  a 3rd nesting level.

  **Subgroup reorganization + actual values/colors**: 2 more direct
  requests, "Port the template's Dev Panel Group settings names,
  ordering, groups, sub groups, group nesting, and ordering" and "i also
  wanted Dev panel colors to be ported too" (a walk-back of the FIRST
  chrome-group port's own deliberate "match this project's current
  look" choice -- now a genuine, disclosed visual change instead).
  Transcribed the template's own `applyDefaultDevPanelSubgroupOrder()`
  EXACTLY: `MECHANICS` (Scroll Strength), `PANEL UI` (background/accent/
  slider color, opacity), and `TEXT` (font family only) as 3 top-level
  subgroups inside "Dev Panel," with `TEXT` itself nesting 5 further
  subgroups (`Dev Panel Title`, `Group Title`, `Setting Title`, `TABS`,
  `BUTTONS`), each holding that category's own Bold/Capitalize/Font-
  Size/Letter-Spacing/Line-Spacing/Color battery, same names/order/
  membership as the template -- including porting `Button Text Border`
  under `Dev Panel Title` even though that placement reads as a likely
  copy-paste artifact in the SOURCE template, since the request was to
  match the template as given, not to silently correct it. New
  `organizeDevPanelSubgroups(groupsEl)` function (mirrors the template's
  own `makeGroup()` helper) builds this structure once, right after
  `buildDevPanel()` creates the initial flat rows, relying entirely on
  the unlimited-nesting fix above (TEXT's own 5 children are a 2nd
  nesting level) -- a genuine saved order (if any) is applied afterward
  by the existing `resetSettings()` call and correctly takes precedence,
  since `applyOrder()` repositions rows by key regardless of where this
  function put them first.

  Also added the one PRE-EXISTING template control the first chrome-
  group port had simply missed (not part of the 2026-09-14 additions
  that port was scoped to): `dp_groupLabelBgColor` ("Group Label
  Background Color"), overriding `.dp-group-header`'s own default
  accent-tinted background with a direct color once set (`background:
  var(--dp-group-label-bg-color, color-mix(...))`).

  Updated EVERY existing control's own `def` to the template's real
  current value (accent `#7d8cff` -> `#005f8f`, font `-apple-system...`
  -> `Verdana, Geneva, sans-serif`, `capsButtonText`/`capsTabText`/
  `capsGroupNames` `false` -> `true`, `boldTab`/`boldButton` `false` ->
  `true`, etc.) -- a deliberate, disclosed visual change this round, not
  a no-op (unlike the first chrome-group port, which intentionally kept
  every default at this project's OWN prior look).

  **Also fixed the 4 letter-spacing controls (Tab/Group/Settings/Button)
  that were wrongly marked `perDevice: true` in the first chrome-group
  port** -- direct report, "we currently have some yellow text in our
  Dev Panel group. fix that. Just match the template" (a `.dp-row.dp-
  per-device label { color: #ffd27d }` rule already existed to visually
  flag per-device-only controls; these 4 shouldn't have triggered it).
  Cross-checked the template's own `DEV_PANEL_STYLE_SHARED_KEYS` array
  directly rather than guessing: it lists `buttonTextLetterSpacing`/
  `tabTextLetterSpacing`/`groupTextLetterSpacing`/
  `settingsTextLetterSpacing` as SHARED (not per-tab) -- only Title's
  own letter-spacing is per-tab there, an asymmetry ported faithfully
  rather than "corrected" to be consistent with the other 4.

  Verified live (after clearing a STALE `localStorage` blob left over
  from much earlier in this same session's own testing, which was
  silently overriding both the new default values and the new subgroup
  structure via a genuine `applyOrder()`/`resetSettings()` restore --
  confirmed via `cfg` readback that this was old saved state, not a bug,
  before spending time debugging the wrong thing): the full nested
  structure matches the transcribed template exactly (only "Enable
  Label Rename Mode" stays flat, matching the template's own "standalone
  panel-level control" placement); real values (`accentColor: '#005f8f'`,
  `fontFamily: 'Verdana, Geneva, sans-serif'`, etc.) all resolve
  correctly; the 4 corrected letter-spacing controls no longer appear in
  `document.querySelectorAll('.dp-row.dp-per-device')`, while Title's
  own still correctly does.
- **Mouse Tracking Log: click classification now resolves at pointerUP,
  not pointerdown.** Neither multi-click (same-button clicks within
  `MOUSE_LOG_MULTICLICK_MS`) nor drag-release (held past
  `MOUSE_LOG_HELD_DRAG_MS` or moved past `MOUSE_LOG_MOVE_THRESHOLD_PX`) is
  knowable until release, so `pointerdown` only records a snapshot
  (time/position/button/target) and `pointerup` does all the
  classification, reusing `describeMouseLogTrigger(e)` (factored out of
  the original single-listener version) for both the drag-release and
  right-click paths. `e.target` is not guaranteed to have `.closest()`
  (e.g. a synthetic event dispatched directly on `document`) -- confirmed
  live as a real crash during testing; guarded with optional chaining.
- **A viewport-emulation tool changing `window.innerWidth`/`innerHeight`
  does not necessarily dispatch a real `resize` DOM event.** Confirmed
  live: the Browser pane's own `resize_window` tool correctly changed
  `window.innerWidth`/`innerHeight` (verified via direct JS readback) but
  did NOT fire the Mouse Tracking Log's `resize` listener; dispatching a
  genuine `new Event('resize')` immediately after did fire it correctly.
  This is a characteristic of that specific viewport-emulation path, not a
  bug in the listener itself -- don't conclude a resize listener is broken
  from this tool's own emulated-viewport behavior alone; dispatch a real
  `resize` event (or resize an actual window) to test it directly.
- **Named Setting States ("Saved Dev Settings") is deliberately NOT part
  of the per-tab reorderable group system.** It's built directly (plain
  `createGroupElement()`, inserted as `groupsEl`'s first child via
  `groupsEl.insertBefore(savedStatesGroup, groupsEl.firstChild)` -- a
  genuine member of the per-tab reorderable group/drag-reorder system,
  same as Dev Panel/Debug/every project group, per direct follow-up
  request ("Save Dev Settings should also be movable. It should be above
  the Dev Panel group."). **CORRECTED 2026-09-14, same day**: this
  entry originally described building it as a hand-built standalone group
  OUTSIDE `groupsEl` specifically to avoid `createGroupElement()`'s own
  drag-handle, reasoning that a saved-state snapshot covers the whole
  panel across all 3 tabs at once so the group "can never really be
  reordered" -- that reasoning no longer holds now that the group's
  POSITION (not its per-tab-ness) is what's being made adjustable. Its
  own select+button row still has no `data-key`, so `captureGroup()`'s
  row-capture (`:scope > .dp-group-body > .dp-row`) still never touches
  it -- only the group's own position/collapsed-state is captured/
  restored by the normal Save/Reset order mechanism, never the select's
  current selection or the buttons. `captureFullPanelState()`/
  `applyFullPanelState()` (factored out of the pre-existing
  `copySettings()`, which already built this exact snapshot shape) are
  shared by Copy Settings and every Save/Use/Delete/Set-Default button on
  this group -- a change to the snapshot shape only ever needs to happen
  in one place.
- **Clicko is the authoritative source for the Dev Panel group's exact
  perDevice-vs-shared split, not CLAUDE.md's own prose summary of it.**
  Confirmed via a real 2026-09-14 correction cycle: CLAUDE.md's §12i text
  claims "only colors/Bold/Capitalize/Font select are shared, everything
  else (font-size/letter-spacing/line-spacing) is per-tab" -- but Clicko's
  own `DEV_PANEL_STYLE_SHARED_KEYS` array explicitly keeps
  buttonTextLetterSpacing/tabTextLetterSpacing/groupTextLetterSpacing/
  settingsTextLetterSpacing SHARED (not per-tab), with a dated comment in
  Clicko's own source explaining this was itself a deliberate 2026-09-14
  fix there. When the two disagree, read Clicko's actual code, not the
  prose -- confirmed this project's own perDevice flags already matched
  Clicko correctly everywhere except one real bug: `dp_scrollStrength`
  was missing `perDevice: true` (Clicko's own `DEVPANEL_STYLE_CONTROLS`
  registers a genuine per-tab Scroll Strength slider for all 3 tabs).
- **`defMobile`/`defLandscape`** (optional per-control fields on a
  `builtInGroup.controls` entry) let a perDevice control seed Mobile/
  Landscape's own store slot with a DIFFERENT starting value than
  Desktop's `def`, instead of every device silently cloning Desktop's
  tuning -- needed because Clicko genuinely tunes 6 of its 15 per-tab
  Dev Panel fields differently per device (e.g. Title Font Size
  17/11/22), not just independently-editable from one shared starting
  point. Read in the control-seeding loop
  (`builtInGroup.controls.forEach` right after `devGroups = [builtInGroup,
  ...]`) -- a control with neither field behaves exactly as before this
  existed.
- **"Default" pose button (Pose group) reuses `syncValue()` +
  `onWholeHandRotationChange()` entirely -- no new field-reset logic.**
  `syncValue(key, v)` (devpanel.js's own public API, already imported for
  the Camera group's live 2-way sync) updates `cfg`/`store`/the slider
  display but deliberately does NOT call `onChange` -- it's built for
  "panel follows an already-changed reality" (e.g. camera dragged by
  OrbitControls), the opposite direction from "button click should
  change reality." `resetPoseToDefault()` calls it once per
  `POSE_PRESET_KEYS` entry (purely to sync the display), then a single
  `onWholeHandRotationChange()` call does the actual re-posing --
  already re-derives `cloneBaseQuat` AND re-applies every finger + the
  wrist internally, so no new "reset and reapply" logic was needed at
  all, just correct reuse of 2 existing pieces.
- **Palm Face Rotation slider's roll, and the palm-facing correction it
  used to be exclusively embedded inside, are now 2 separable pieces.**
  `computeRollQuat()` (just the rotation around the wrist-crop-plane
  axis) was factored out of `computePalmFaceCorrectionQuat()` (which
  still calls it internally, composing the roll into where the
  palm-normal aims) so the tracking loop can use `computeRollQuat()`
  directly when Palm Faces Cursor is off -- previously the slider's
  angle only ever reached the hand via the palm-facing path, so it had
  literally zero effect with that checkbox unchecked. Verifying this
  live via the wrapper's own `quaternion` before/after initially showed
  NO change -- root-caused to the Browser pane's own already-documented
  `requestAnimationFrame`-suspends-while-hidden limitation (confirmed via
  `tabs_context` reporting the pane hidden), not a code defect. Verified
  the actual composition math directly instead (replicating the exact
  render-loop calculation externally, same technique as the axis-
  difference check 2 rounds earlier): the quaternion dot product between
  offset=0 and offset=90 came out to exactly 0.7071 = cos(45 degrees),
  the precise signature of a genuine 90-degree rotation difference.
- **`organizeGroupSubgroups(groupsEl, groupTitle, subgroupSpecs)`** --
  a NEW generic export in `devpanel.js`, added to port HANDO's own Pose
  group subgroup structure without forking the shared engine. The
  existing `organizeDevPanelSubgroups()` (used for the built-in "Dev
  Panel" chrome group) is hardcoded to that one group's name and
  structure and stays untouched -- this is a separate, parameterized
  function reusing the exact same "flat rows into named subgroups, once,
  right after `buildDevPanel()`, before a real saved order takes over"
  shape. Wired in via a new optional `initDevPanel(groups, {
  organizeSubgroups })` hook, called once inside `initDevPanel()` right
  after `organizeDevPanelSubgroups(groupsEl)` -- a project passes its own
  `(groupsEl) => organizeGroupSubgroups(groupsEl, 'GroupTitle', SPEC)`
  callback rather than this file knowing any project-specific group or
  control names. Any future project group needing this same "port a
  reference project's own drag-organized subgroup structure" treatment
  can reuse this directly, no engine change needed.
- **HANDO's own Pose group control array has NO subgroup/nesting
  information at all -- the real structure only ever existed in its own
  committed `data/processed/dev-panel-settings.json` (the `order`
  array's "Pose" entry).** Confirmed directly: HANDO's `main.js` Pose
  `controls:` array is flat, and its subgroups were built entirely via
  live drag-and-drop, captured only in that saved-settings JSON --
  reading the CODE would have missed the entire structure. Ported from
  HANDO's own subgroup order/grouping exactly (Whole-Hand Rotation +
  Thumb, Wrist, Index, Middle, Ring, Pinky), but with descriptive names
  instead of HANDO's own literal ones -- HANDO's 6 subgroups were still
  named the unrenamed drag-drop defaults ("New Group", "New Group (3)"
  through "(7)"), never actually renamed there. Omitted HANDO's 5
  `baseOnlyCurl*` sliders (don't exist as controls in this project) and
  `hideWrist` from the ported Wrist subgroup (this project's own Hide
  Wrist belongs to a separate, larger reactive Arm Length system,
  already deliberately excluded from Pose-preset capture -- see this
  group's own `POSE_PRESET_KEYS`/`savedPoses` comment).
- **Same-day follow-on: both `organizeGroupSubgroups()` and
  `organizeDevPanelSubgroups()` are now idempotent, and both get called
  a 2nd time after `resetSettings()` runs.** Direct report, "you didnt
  move the settings into the groups which was half the point" -- the
  Pose port above WAS correct in code, but a saved order predating it
  (this project's own already-documented "stale localStorage" gotcha,
  previously only ever worked around by manually clearing storage in a
  TEST tab, never fixed for a real user's browser) got applied by
  `resetSettings()`'s own `applyOrder()` right after the fresh
  structure was built, moving every row back out to a flat layout --
  the exact same latent bug the Dev Panel group's own subgroup port had
  been carrying since earlier this session, now actually fixed instead
  of worked around. Both `makeSub()` (Dev Panel) and
  `organizeGroupSubgroups()` (generic) now query for an existing
  same-titled subgroup first, reusing it instead of always creating a
  fresh one via `createGroupElement()` -- this is what makes calling
  either function twice safe (no duplicate empty subgroup shells).
  `initDevPanel()` calls both again immediately after `resetSettings()`,
  so whatever a stale order just flattened gets correctly re-nested. A
  genuinely current saved order (reflecting real post-port user
  reorganization) is unaffected -- this only ever moves a spec'd key
  INTO its subgroup, never fights a real saved position elsewhere.
  Verified by deliberately staging a stale, fully-flat pre-port Pose
  order into `localStorage` and confirming all 6 subgroups still
  re-nested correctly on reload, with zero duplicate subgroup titles.
- **"Palm Faces Cursor" REDEFINED, same day, from a full 3D palm-normal-
  aim to a 2D radial roll -- the 3-round-calibrated `palmNormalAligned`/
  `computePalmFaceCorrectionQuat()` mechanism is gone, deleted outright,
  not just superseded.** Direct correction after the user tested the old
  mechanism against their own real settings and found it didn't do what
  they wanted at all -- they described a completely different, much
  simpler mental model: each hand should rotate like a compass needle,
  in the field's own 2D XY plane, by the angle from that hand's own
  position to the cursor. `computeRadialRollDeg(handPos, cursorPos) =
  atan2(dx, -dy)` (dx/dy in world space, hand minus cursor) reuses the
  SAME wrist-crop-plane roll axis (`wristCropNormalAligned`) the Palm
  Face Rotation slider already established 2 rounds ago -- only the
  ANGLE source changed (dynamic per-hand-per-frame instead of a single
  slider value), not the axis or the composition mechanism. The slider
  still adds onto whatever base angle is in effect (0 when Palm Faces
  Cursor is off, the dynamic radial angle when it's on).

  The formula was derived directly from the user's own 5 stated
  reference points (hand below cursor -> 0 degrees, matching the
  already-correct default "facing up" behavior; above -> 180; right ->
  90; left -> -90; upper-right diagonal -> 135) BEFORE writing any code
  -- confirmed by hand that `atan2(dx,-dy)` reproduces every one exactly,
  then verified again programmatically against all 8 compass points
  (including the 3 implied diagonals: 45/-45/-135) once implemented.

  Verification methodology, disclosed same as the immediately preceding
  round: a genuine live-render check is unreliable here (Browser-pane
  rAF suspends while hidden), so verified the actual composition
  mathematically instead, via 3 independent quaternion dot-product
  checks against the EXTERNALLY reconstructed render-loop formula: hand
  directly below cursor exactly reproduces the pre-existing default
  lookAt (dot=1.0 -- the "nothing changes for this case" guarantee the
  user explicitly asked for); hand to the right differs from default by
  exactly cos(45 degrees)=0.7071 (the signature of a genuine 90-degree
  difference); adding a 30-degree slider offset on top differs from the
  base case by exactly cos(15 degrees)=0.9659 (a genuine 30-degree
  difference). All 3 numbers are the PRECISE trigonometric signatures
  expected, not approximations.

  **Concurrent-session handling**: `devpanel.js` had a real in-progress
  uncommitted change from a different session (their own Pose-subgroup
  `organizeGroupSubgroups` work, landing as `cb11b3c`/`7fc8967` moments
  before this round started) sitting in the same shared working tree.
  Confirmed via `git diff src/main.js` before staging that this round's
  own uncommitted diff contained ONLY this round's own changes (their
  already-committed content was untouched baseline, not part of the
  diff) -- staged and committed `index.html`/`main.js` only, left
  `devpanel.js` completely alone.
- **A raw Euler read of a bone's `quaternion` does NOT isolate an
  applied rotation from this rig's own bind pose -- measure a
  baseline-delta instead.** Confirmed live while verifying Responsive
  Wrist Splay: `new THREE.Euler().setFromQuaternion(bone.quaternion)`
  read a constant -22.6-degree offset on EVERY hand regardless of the
  actual applied `extraSplayDeg`, because `rHand`'s own `boneRestQuat`
  (what `applyWristPoseToSkeleton()` starts from before `rotateX`/
  `rotateZ`) already has a non-identity Z-component in this asset's bind
  pose -- a raw Euler read conflates that rest orientation with whatever
  was actually applied on top of it. Fix: capture the bone's quaternion
  with the feature under test turned OFF as the true zero-reference,
  then compute `delta = baseline.clone().invert().multiply(final)` and
  read the signed angle via `2 * atan2(delta.z, delta.w)` (valid for a
  known-single-axis rotation) -- this isolates exactly what was applied,
  independent of whatever the rest pose itself already contains. Applies
  to any future live-verification of a rotation applied to a bone that
  isn't already known to be sitting at bind-pose identity.
- **`window.__debug.updateRenderOrder` (and other exposed functions) can
  be called directly from a test script to bypass the Browser pane's
  own `requestAnimationFrame`-suspends-while-hidden limitation
  entirely**, rather than working around it with external math
  reconstruction (the technique used in the 2 preceding rounds). Calling
  the REAL function synchronously runs the actual per-hand loop
  (including whatever new logic was just added to it) with zero
  reimplementation risk -- confirmed live for Responsive Wrist Splay's
  own verification. Prefer this over reconstructing the formula
  externally whenever the function in question is already exposed on
  `window.__debug` (`updateRenderOrder` is; most of this project's other
  internal functions aren't, per its own minimal-debug-surface
  convention -- exposing more of them is a reasonable trade whenever a
  future feature's own live verification would benefit the same way).
- **"Click Hold-Pose" / "Right-Click Hold-Pose"** (2 independent top-level
  dev-panel groups, `makeClickHoldPoseGroup('chp', ...)` /
  `makeClickHoldPoseGroup('rchp', ...)`) -- on mousedown/right-mousedown,
  every hand transitions to a chosen saved pose; on release, back to
  default. Each trigger is its own small per-hand-per-trigger state
  machine (`hand._chp[p]`, phases `idle -> forward -> retransition ->
  idle`, `p` is `'chp'` or `'rchp'`), driven from `updateRenderOrder()`'s
  own per-hand loop via `updateClickHoldPoseForHand()`. Reuses
  `applyCurlToSkeleton()`/`applyWristPoseToSkeleton()`'s existing
  `values` override param (already proven by Pose Preview) via
  `applyPoseValuesToHand()`, and `evaluateArmLengthCurve()` directly
  (fully generic despite the name) for both the curve-shaped start-time
  stagger and the pose lerp's own progress curve.

  Key design points: a hand's forward-phase start delay is computed ONCE
  at `pointerdown` from a live cursor-distance SNAPSHOT (`startClickHoldPose()`),
  not re-evaluated every frame, so a moving cursor mid-hold doesn't
  reshuffle stagger order. On `pointerup` (`endClickHoldPose()`), each
  hand's own LAST-APPLIED interpolated values (`chp.lastAppliedValues`,
  updated every frame while in 'forward' phase) become that hand's own
  retransition start -- correctly handles releasing mid-transition rather
  than assuming every hand had already reached the target. Setting a
  trigger's own Min/Max Start Time range to the same value collapses the
  stagger to "every hand moves together," satisfying that part of the
  request without a separate toggle. `lerpPoseValues()` deliberately
  excludes `modelRotX/Y/Z` (Whole-Hand Rotation) from the keys it
  interpolates -- those 3 drive a single SHARED `cloneBaseQuat` used by
  every hand's own Arm Length position-compensation math, so making them
  per-hand would need a materially larger change than this feature asked
  for (see CHANGELOG.txt's own entry for the full disclosed reasoning).

  The mousedown/mouseup listeners are added as parallel, non-blocking
  `window`-level listeners (never call `preventDefault()`/
  `stopPropagation()`), the same convention the Mouse Tracking Log
  already established -- OrbitControls' own existing left/right-click-
  drag camera pan is completely unaffected; both behaviors happen
  simultaneously by design, not a conflict to resolve.

  2 real bugs, both only visible once the page actually loaded in a
  browser (a `node --check` syntax pass can't catch either):
  - **A top-level `const` is not hoisted the way a `function` declaration
    is -- a setup call positioned textually BEFORE that const's own
    declaration throws a TDZ ReferenceError at module-evaluation time,**
    even though the const is declared "further down in the same file."
    `CLICK_HOLD_KEYS.forEach((p) => { parseClickHoldConfig(p);
    buildClickHoldPoseWidgets(p) })` was originally placed near the other
    widgets' own setup calls early in the file (`parseArmLengthConfig()`
    etc.), but `CLICK_HOLD_KEYS` (and `clickHoldPoseTriggers`, which
    `parseClickHoldConfig()` reads) are declared much later, alongside
    the rest of the state machine. Fixed by moving the ONE setup-call
    line down to immediately after `clickHoldPoseTriggers`'s own
    declaration block (right after `buildClickHoldPoseWidgets` is
    defined) -- not by hoisting the consts earlier, which would have
    meant relocating the entire state-machine section instead of one
    line. `parseArmLengthConfig`/`buildWristSplayWidgets`-style setup
    calls never hit this because those functions don't read a
    later-declared `const`; this is specific to any future control whose
    parse/build step depends on module-level state declared further down
    the file.
  - **A `select`-type dev-panel control's `<option>` list is only
    rebuilt on an explicit `refreshSelectOptions(key)` call -- it does
    NOT automatically stay in sync with a `list-picker` control it reads
    via `options()`.** The 2 Target Pose dropdowns
    (`options: () => (cfg.savedPoses || []).map((sp) => sp.name)`) went
    stale after Saving/Deleting/Renaming a pose in the existing "Saved
    Poses" list-picker, until fixed by wiring that control's own
    `onChange` to call `refreshSelectOptions('chpTargetPose')` /
    `refreshSelectOptions('rchpTargetPose')` (confirmed via
    `devPanel.js`'s own doc comment on `refreshSelectOptions`, which
    names exactly this scenario -- "Call after the underlying option list
    changes (e.g. a 'list-picker' control's own onChange...)" -- as its
    intended use). Applies to any future `select` control whose options
    are sourced from another control's own dynamically-changing value.

  New generic (parameterized) widget builders, `buildGenericRangeBarWidget(row,
  opts)` / `buildGenericCurveWidget(row, opts)`, added alongside (not
  replacing) the existing project-specific Arm Length/Wrist Splay widget
  builders -- this round needed 8 more widget instances (4 controls x 2
  triggers), past the point where hand-copying was judged worth it (the
  single-instance Wrist Splay round above deliberately kept its own
  dedicated, non-generic widgets; this is the point that tradeoff
  flipped). The curve widget includes right-click-to-delete-point from
  its first version (the other half of this session's own opening
  request, also retrofitted onto the original Arm Length curve widget).

  Live-verified via `window.__debug.updateRenderOrder()` plus real
  `PointerEvent` dispatch on `window` (exercising the actual listeners,
  not a hand-reconstructed simulation of them): correct phase
  transitions per hand; a genuine 81-degree quaternion delta on the
  `rIndex1` bone between 2 distinct injected target poses (confirms the
  target value drives the bone, not a fixed animation); 6 sampled hands
  mid-hold showing 6 distinct `forwardDelay` values (confirms the
  distance-based stagger computes a real per-hand number). See
  CHANGELOG.txt's own entry for the full verification account.
- **UNRESOLVED: `refreshSelectOptions('rchpTargetPose')` throws inside
  devPanel.js's own `commit()`/`fillSelectOptions()`, for a cause isolated
  but not root-caused.** Found while fixing the Target Pose dropdowns'
  own stale-on-load bug (they need an explicit one-time
  `refreshSelectOptions()` call after page load, since devPanel.js's
  `select` controls only rebuild their `<option>` list on that explicit
  call -- see that same fix's own comment at the call site in `main.js`).
  Repeated single-key isolation testing in a fresh browser tab (ruling
  out a degraded/crashed tab as the cause -- see the next GOTCHAS entry)
  showed: `refreshSelectOptions('chpTargetPose')` alone always succeeds;
  `refreshSelectOptions('rchpTargetPose')` alone always throws
  `TypeError: Cannot read properties of undefined (reading
  'rchpTargetPose')`. The 2 controls are registered via the exact same
  `makeClickHoldPoseGroup(p, title)` factory, byte-for-byte identical
  shape, differing only in the `p`/`title` arguments -- a full static
  read of `commit()`, `fillSelectOptions()`, `findCtrl()`, and
  `displayValue()` in devPanel.js found no code path that should treat
  the 2 keys differently. Mitigated, not fixed: added a
  `safeRefreshSelectOptions(key)` wrapper (try/catch + `console.error`,
  defined near the top of `main.js`) and routed every call site for
  these 2 keys through it, so this can never again crash the whole page
  load regardless of root cause. Left as an open item in
  PROJECT_PROGRESS.txt's "What's next" for a real debugging pass (a
  `debugger` breakpoint, not `console.log` markers -- see the next
  GOTCHAS entry for why those failed here) next time this file is
  touched, since the same devPanel.js code path could affect some other,
  not-yet-discovered `select` control the same way.
- **A browser tab that has crashed and reloaded many times in one
  debugging session cannot be trusted for a "did my fix work" check --
  open a fresh tab instead.** Found while root-causing the bug directly
  above: a `console.log()`-based debug marker placed right before the
  suspect code never appeared in that session's console output, even
  though the exact same code (confirmed via the same marker) ran fine in
  a freshly-opened tab moments later. Root cause: the crashing tab had
  accumulated enough real WebGL errors
  (`GL_INVALID_FRAMEBUFFER_OPERATION`, from the documented
  `window.innerWidth`-reads-0 gotcha compounding across many failed
  reloads) that Chrome's own console eventually stopped reporting
  further messages for that context entirely ("WebGL: too many errors,
  no more errors will be reported to the console for this context") --
  burying the marker, not a sign the code never ran. Confirmed the fix
  by opening a brand-new tab instead of continuing to reload the
  already-degraded one.
- **Pose's "Default" button lives outside DEV_GROUPS entirely now** --
  direct request redefined it from "reset every pose slider to its code
  default" to "set the currently-selected Saved Pose as the app's
  default (startup + Click-Hold-Pose retransition target)," and to
  physically sit inside the Saved Poses list-picker's own button row
  (next to Save/Overwrite/Use/Rename/Delete). devPanel.js's generic
  `type: 'list-picker'` builder has no config hook for an extra button,
  and this project's own file-map convention is not to fork that shared
  engine -- so `buildPoseDefaultButton()` (main.js) does plain DOM
  injection into `.dp-row[data-key="savedPoses"] .dp-list-picker-actions`
  after the panel is built, the SAME "find-and-augment an existing
  devPanel.js-built row" pattern the Arm Length/Wrist Splay/Click-Hold-
  Pose custom widgets already use for their own hidden-text-input rows.
  `getSelectedSavedPoseItem()` reads devPanel.js's own internal selection
  state externally via a DOM convention it ALREADY used for its own
  purposes (`row.__item = item` set in `renderListPickerItemRow()`,
  `.dp-list-picker-row-selected` toggled on click) -- no devPanel.js
  change needed for this part, since that state was already exposed on
  the DOM, just never previously read by a host app.
- **`poseDefaultValues` (mutable, `let`) replaces `POSE_KEY_DEFAULTS`
  (frozen, `const`) at BOTH of Click-Hold-Pose's own retransition-target
  call sites** (`updateClickHoldPoseForHand()`'s retransition branch, and
  `endClickHoldPose()`'s `chp.retransitionStart` fallback) -- direct
  request: the pose Click-Hold-Pose retransitions back to should be
  "the default pose" (settable via the button above), not the hardcoded
  code default forever. Seeded once, right after `POSE_KEY_DEFAULTS`
  itself is computed, from `cfg`'s OWN restored Pose values (whatever
  `initDevPanel()` just loaded from localStorage, or code `def` if
  nothing was ever saved) -- this already correctly represents "the pose
  on startup" the instant that line runs, by the SAME persistence
  mechanism every other panel setting already uses, so no bespoke
  startup-specific logic was needed. `setSelectedPoseAsDefault()`
  updates it live on click, so an in-progress retransition (or the next
  one) targets the new default without needing a reload first.
  `POSE_KEY_DEFAULTS` itself is UNCHANGED and still used exactly as
  before, for its own narrower, genuinely different job: the per-key
  fallback in `lerpPoseValues()`/`previewPosePreset()` for a saved/
  partial pose object missing a key (e.g. an older saved pose predating
  a newer slider) -- deliberately NOT conflated with "what pose is the
  default," a different question this round's own comments are explicit
  about keeping separate.
- **New devPanel.js export: `saveCurrentSettings()`.** Triggers the
  panel's own real persisted save (the same localStorage/remote write
  its Save/Sync button performs) programmatically, for a host-app action
  where waiting on a separate manual Save click would be the wrong UX
  (here: "Default" should persist immediately, per the direct request's
  own "so its the pose on startup" wording). Implemented via a new
  module-level `let saveSettingsRef = null`, set the moment
  `initDevPanel()`'s own internal `saveSettings()` closure is defined,
  mirroring the existing `syncValue()`/`refreshSelectOptions()` host-hook
  pattern rather than reaching for a hackier route (e.g. simulating a
  click on the actual Save button DOM element, which would silently
  break if that button's label/position ever changed). This is the one
  genuine devPanel.js code change this round required -- everything else
  (the button itself, its click handler, the selected-item read) lives
  entirely in main.js.
- **NOT independently live-verified this round** (Pose Default button
  relocation/redefinition, the 700ms speed-slider cap) -- per direct
  instruction ("dont verify just fix ad push"), reasoned through
  statically against devPanel.js's own already-verified conventions
  rather than clicked in a real browser. See PROJECT_PROGRESS.md's own
  "What's next" for the specific follow-up checks this leaves open.
- **FIXED: the thumb was the only finger visibly mis-posed whenever
  wrist bend/splay and finger values changed together in one call --
  wrist MUST be applied before fingers, not after.** Direct user report
  ("the thumb pose doesnt seem correct... has to do with the rotation
  that we had used"), root-caused by direct comparison against HANDO
  (this project's own reference implementation, identical rig asset) --
  HANDO had already found and fixed this exact bug, just never ported
  here. `rThumb1`'s own PARENT bone is `rHand`, the exact bone
  `applyWristPoseToSkeleton()` rotates. `rotateOnTrueWorldAxis()` (used
  by every finger's own Curl/Splay/Splay2) converts its world axis into
  the bone's CURRENT local space via the bone's FULL world quaternion,
  which depends on the ENTIRE parent chain's CURRENT `matrixWorld` --
  posing the thumb before the wrist reaches its own new target rotation
  reads whatever wrist rotation was left over from BEFORE that call, not
  the one about to be set. The other 4 fingers aren't parented directly
  to `rHand`, so they were never affected -- exactly why only the thumb
  looked wrong. Fixed by swapping the order (wrist first, fingers
  second) at all 3 combined-call sites: `applyAllFingerPoses()`
  (rebuild, Whole-Hand Rotation changes), `previewPosePreset()` (Pose
  Preview / "Use" on a saved pose), and `applyPoseValuesToHand()`
  (Click-Hold-Pose). A finger-only slider drag (e.g. dragging Thumb Curl
  alone) was NEVER affected by this -- `applyCurl(fingerName)` on its
  own doesn't touch the wrist, and `bone.updateMatrixWorld(true)` always
  recomputes from the CURRENT (already-correct-by-then) parent chain
  regardless of call order in that case; the bug only manifests when
  wrist and finger values change TOGETHER in one combined call, starting
  from a skeleton whose `rHand` hadn't yet been updated to match.

  **Verified with a real quantitative measurement, not just a visual
  check:** applied `wristBend=45`/`wristSplay=-20`/`thumbCurl=90` to a
  freshly-rebuilt (bind-pose) skeleton -- the exact bug-triggering
  scenario -- and captured `rThumb1`'s quaternion right after. Built an
  independent reference by re-applying wrist bend/splay and thumb curl
  via their own SEPARATE, single-purpose slider calls (each only ever
  touches its own bone; no ordering ambiguity possible there). With the
  OLD order, the rebuild's result differed from the reference by 33.0
  degrees. Temporarily restored the old order specifically to CONFIRM
  this number (ruling out "the test isn't sensitive enough" as an
  explanation for a 0-degree result), then re-applied the fix and reran
  the identical test: exactly 0.0 degrees. See CHANGELOG.txt's own entry
  for the full before/after account.
- **RESOLVED (was "UNRESOLVED, STILL OPEN" in this same section, an
  earlier round): "thumb pose of all poses except startup still looks
  wrong."** The wrist-before-fingers fix (above) was always correct --
  every 0.0-degree test result was real. The REMAINING gap was a
  SEPARATE issue, only reachable with poses that carry nonzero Whole-
  Hand Rotation: the user's own key clarification ("i am using imported
  poses from Hando") revealed that every synthetic test pose used in
  this session's own prior investigation had modelRotX/Y/Z left at 0,
  which never exercised it. Root cause: `lerpPoseValues()` (used by both
  Click-Hold-Pose and Click-Pose) deliberately EXCLUDED modelRotX/Y/Z
  from interpolation -- a disclosed scope decision from when Click-Hold-
  Pose was first built, since those 3 keys drove a single SHARED
  `cloneBaseQuat` used by every hand's own Arm Length position-
  compensation math, and making them per-hand was flagged at the time as
  a materially larger change than that feature's own original request.
  HANDO-imported poses commonly bake nonzero Whole-Hand Rotation into
  the gesture -- during a transition to such a pose, fingers/wrist
  correctly animated to the target, but the finger CURL AXES stayed pre-
  rotated by the OLD, unchanged shared basis throughout, anatomically
  wrong the instant a pose implies a different whole-hand orientation
  than the field's current shared sliders say. Confirmed directly:
  mid-transition, `cfg.modelRotY` measured unchanged (still 0) while the
  target pose's own modelRotY was 60.

  **Fix (the user explicitly chose the full per-hand interpolation over
  2 smaller alternatives when asked):** each hand now has its own
  `currentBaseQuat` (added to the hand object in `rebuildField()`),
  mirroring the single shared `cloneBaseQuat` every frame for any hand
  NOT mid a pose-transition (verified: 0.0-degree error vs. an
  independently-computed reference for ordinary, non-transition Whole-
  Hand Rotation usage -- no regression). `lerpPoseValues()` now
  genuinely interpolates modelRotX/Y/Z (the exclusion is gone). A new
  `computeBaseQuatFromValues(values)` helper (module-level, near
  `lerpPoseValues`) replicates `updateCloneBaseQuat()`'s own exact
  formula (`alignQuat` premultiplied onto an Euler built from
  `values.modelRotX/Y/Z`, in degrees) but reads a passed-in values
  object instead of live `cfg` -- called from `applyPoseValuesToHand()`
  every time it runs, writing the result into `hand.currentBaseQuat`
  before posing that hand's own fingers with it (instead of the shared
  `cloneBaseQuat`). `applyHandArmLength()` was changed to read
  `hand.currentBaseQuat` instead of the shared value directly --
  disclosed tradeoff: since `applyHandArmLength` runs BEFORE the pose-
  override step in `updateRenderOrder()`'s own per-hand loop, it reads a
  value that's ~1 frame behind for a hand that's actively transitioning
  (the ARM-LENGTH POSITION only, never the fingers, which read the fresh
  value the same frame) -- imperceptible at 60fps, and avoids
  restructuring the loop's own established bone-posing order just to
  eliminate a sub-16ms position lag.

  Live-verified across the full lifecycle (not just one moment): a
  60-degree target modelRotY rotates the hand's own actual rendered
  orientation by exactly 60.0 degrees during the transition; the shared
  field-wide `cfg.modelRotY` stays untouched throughout (no leakage to
  other hands); a hand never touched by any trigger stays exactly synced
  to the shared basis; after a full retransition cycle, a hand's own
  basis correctly reverts to the shared/default orientation; ordinary
  slider-driven Whole-Hand Rotation was re-verified afterward at exactly
  0.0 degrees of error. See CHANGELOG.txt's own entry for the full
  account, including the exact clarifying question that broke the case
  open.
- **New: "Click Pose" / "Double-Click Pose"** -- a fire-and-forget
  variant of Click Hold-Pose (direct follow-up request). Where Click
  Hold-Pose requires holding the mouse down (forward while held, retransition
  on release), this variant fires its ENTIRE sequence from a single
  click/double-click: transition to target -> PAUSE at the target for a
  configurable duration (`${p}PauseDurationMs`, a single flat slider, not
  a curve+range pair) -> retransition back to default -- then runs to
  completion per-hand with no further mouse involvement. Built via
  `makeClickPoseGroup(p, title)` (near `makeClickHoldPoseGroup`, same
  control shape plus the one new Pause Duration slider) and a parallel,
  independent state machine: `CLICK_POSE_KEYS`, `clickPoseTriggers`,
  `parseClickPoseConfig`, `getOrInitHandCP` (separate per-hand namespace,
  `hand._cp`, NOT `hand._chp` -- the 2 feature families track fully
  separate state and can be enabled independently without colliding),
  `updateClickPoseForHand` (the 3-phase forward/paused/retransition
  sequence, one more phase than Click Hold-Pose's own forward/
  retransition pair), `triggerClickPose` (fires every hand's own full
  sequence synchronously at click time, computing each hand's own
  forward delay immediately from a single combined distance snapshot --
  there's no later "release" event to wait for the way Click Hold-Pose
  has, so nothing is deferred to next-frame the way its own lazy
  per-hand forward-delay computation is).

  **Direct, literal implementation of "Retransition start time of a
  single [hand] does not consider whether every hand has completed their
  transition. It is independent to that hand specifically":** each
  hand's own pause-end -> retransition-delay computation
  (`computeStartDelayMs(live, minLiveDist, liveDistRange, ...)` inside
  `updateClickPoseForHand`'s own 'paused' branch) uses THAT frame's live
  distance for THAT hand specifically, evaluated at the exact moment
  THAT hand's own pause timer elapses -- never a shared snapshot taken
  once for every hand (contrast with `endClickHoldPose()`, which takes
  ONE combined min/max distance snapshot for all hands at a single
  release instant, since Click Hold-Pose genuinely does have one shared
  release event to anchor to; Click Pose has no equivalent moment per
  hand, so per-hand-live is the correct analogue, not a shortcut).

  **Click vs. double-click disambiguation** reuses the Mouse Tracking
  Log's own existing `MOUSE_LOG_MULTICLICK_MS` (350ms) debounce constant
  rather than inventing a new threshold -- a `pointerup` listener on the
  LEFT button only (Click Hold-Pose's own right-click instance is
  unaffected) increments a counter and resets a timer on every click;
  when the timer finally fires with no further click having arrived, the
  accumulated count decides `triggerClickPose('click')` vs. `('dblclick')`.
  Both triggers deliberately share the LEFT button (not a left/right
  split like Click Hold-Pose's own 2 groups) since click-count, not
  button, is the axis this feature varies on.

  Deliberately NOT engineered for mutual exclusivity with Click Hold-Pose
  even though both can trigger from the same left-click gesture in
  principle (a plain click also fires as a `pointerup` Click Hold-Pose
  itself reacts to) -- each of the 4 groups (chp/rchp/click/dblclick) has
  its own independent Enabled checkbox, default off, and the practical
  expectation is the user enables only the ONE mechanism they want active
  at a time; if 2 happen to be enabled simultaneously and both claim the
  same hand's pose in the same frame, whichever runs later in
  `updateRenderOrder()`'s own per-hand loop wins that frame's write --
  the same disclosed simplification Click Hold-Pose's own chp/rchp
  conflict case already uses.

- **New: OrbitControls' own Pan disables during an active Click-Hold-Pose
  trigger.** Direct request: dragging the mouse during a hold was also
  panning the camera (this project's OrbitControls config maps BOTH
  left and right buttons to Pan -- `controls.mouseButtons = { LEFT:
  THREE.MOUSE.PAN, MIDDLE: THREE.MOUSE.DOLLY, RIGHT: THREE.MOUSE.PAN }`,
  the exact same buttons Click Hold-Pose's own pointerdown/pointerup
  listeners already use). `controls.enablePan` is set `false` at the top
  of `startClickHoldPose()`, restored `true` in `endClickHoldPose()` only
  once NEITHER `chp` nor `rchp` is still active (so releasing one button
  while the other is still held doesn't prematurely re-enable panning).
  Confirmed by reading `animate()`'s own code that cursor-tracking
  rotation (`hand.wrapper.quaternion.slerp(...)`, lines ~2950-2967) is
  a fully separate, unconditional per-frame step that never went through
  OrbitControls at all -- it already continues following the cursor
  through any pose-transition state, including mid-transition, with zero
  code change needed for that half of the request.
- **Ported HANDO's own "Base-Only Curl" (5 sliders, `baseOnlyCurl{Finger}`,
  `FINGER_BASE_ONLY_CURL_KEY` map) -- this project's Pose-group port had
  omitted it entirely, disclosed at the time but never circled back to.**
  Direct user suggestion found it: "maybe check hando for pose settings.
  maybe there is an extra setting you dont have." Applied in
  `applyCurlToSkeleton()` as a 2nd, purely additive `rotateOnTrueWorldAxis`
  call on joint 0 ONLY (`if (i === 0)`), same curl axis as the regular
  Curl rotation, positioned right after it -- composes naturally since
  `rotateOnAxis` always adds onto whatever local rotation the bone
  already has (same mechanism Splay/Splay2 already use to compose onto
  Curl for their own joints). `baseOnlyCurlT` reads `values[...] || 0`
  (not a bare `values[...] / 100`) so an older saved/imported pose
  missing this key entirely is treated as 0, not `NaN`. Added all 5 new
  keys to `POSE_PRESET_KEYS` (so capture/lerp/restore all pick them up
  for free via the existing generic per-key iteration) and to their own
  per-finger `POSE_SUBGROUP_SPECS` entries. Live-verified in isolation:
  45 degrees of measured rotation on `rThumb1` (the base joint) between
  `baseOnlyCurlThumb` 0 and 100, exactly 0 degrees on `rThumb2` (the mid
  joint) once every other thumb slider was genuinely zeroed (a nonzero
  reading on a first attempt was traced to leftover `thumbSplay` state
  from earlier tests in the same persisted session, not a real bug --
  thumb Splay's own pivot joint is index 1, and its axis conversion
  correctly depends on the now-differently-rotated parent joint 0, per
  `rotateOnTrueWorldAxis`'s own by-design behavior). Also re-verified,
  end-to-end: a Click-Hold-Pose transition to a pose with nonzero
  `baseOnlyCurlThumb` now measures 0.0 degrees of error against the
  "Default" button applying the identical pose.
- **Click-Hold-Pose / Click-Pose interference, 2 rounds (the 1st fix
  introduced a real regression, caught and corrected the same round).**
  Round 1, direct report: "for click hold, when i release, it seems to
  trigger the correct release, but then it calls it again. I suspect it
  is click hold interferring with click triggers" -- correct. Releasing
  a Click-Hold-Pose hold fires the same native `pointerup` Click Pose's
  own listener reacts to on the left button, with no way to distinguish
  "this pointerup ends a hold" from "this pointerup is a genuine
  standalone click" -- so every hold-release ALSO fired Click Pose's own
  sequence. Fixed with `lastPointerupWasHoldRelease` (module-level `let`,
  set by the Click-Hold-Pose pointerup listener -- registered EARLIER in
  the file, so it runs first for the same event -- and read by the Click
  Pose listener before counting a click).

  Round 2, direct report on the SAME session: "i tested the new build and
  now i cant trigger click nor double click. i thinkn the click hold
  trigger probably overrides it" -- also correct: `clickHoldPoseTriggers.chp.active`
  goes true the INSTANT a press starts (Click-Hold-Pose has no minimum
  hold duration of its own), so with BOTH features enabled, even a
  genuinely instant click left `.active` true right up until release,
  making round 1's check suppress EVERY click, not just real holds.
  Corrected by requiring the hold to have lasted at least
  `MOUSE_LOG_HELD_DRAG_MS` (reusing the Mouse Tracking Log's own existing
  "quick tap vs. real hold" threshold, not a new one) before treating a
  release as suppressible. Live-verified both cases together: a ~50ms
  click still enters Click Pose's own 'forward' phase; a ~700ms hold
  still suppresses the extra trigger.
- **RESOLVED (was "UNRESOLVED, HIGH PRIORITY" in this same section) --
  the "thumb pose looks wrong" report's true root cause: not a code bug
  at all, in any of the 4 real, independently-verified fixes below it
  (wrist-before-fingers ordering; per-hand Whole-Hand Rotation during
  transitions; Base-Only Curl) -- all 4 stay correct and shipped.** The
  actual cause was **Responsive Wrist Splay**, a live, cursor-distance-
  driven feature the user had enabled: it adds an extra wrist rotation
  recomputed every frame, independent of whatever pose is targeted, and
  since only the thumb is parented to the wrist bone (`rHand`), it's the
  only finger any such extra rotation can ever visibly move. User-
  confirmed via 2 decisive tests: (1) setting a Click Pose's target to
  the SAME pose as Default still produced an instant thumb shift on
  click -- impossible if the cause were in pose DATA, since interpolating
  identical values is a no-op; (2) "every other finger is in the correct
  pose, its just the thumb" -- the one structural asymmetry. Confirmed:
  "yeah thats it. good catch."
  Direct follow-up fix (2026-09-14): Responsive Wrist Splay's own
  `extraSplayDeg` is now captured ONCE at the instant a Click-Hold-Pose
  or Click/Double-Click-Pose transition triggers (`chp.frozenSplayDeg` /
  `cp.frozenSplayDeg`) and held fixed through that hand's entire sequence
  (forward/pause/retransition), instead of being recomputed live every
  frame throughout the transition. An idle hand (not in any active
  transition) keeps tracking Responsive Wrist Splay fully live, exactly
  as before -- both features now coexist correctly. Live-verified in
  browser (see CHANGELOG.txt's matching entry for the full account).
- **CORRECTION to the entry directly above (still 2026-09-14, same day):
  the freeze fix was real and still correct, but was NOT actually the
  full explanation.** Direct follow-up report: "doesnt work. thumb still
  messed up" -- then, asked whether it reproduced with the cursor held
  still: "yeah im not talking about moving during the transition. im
  just talk about regular static click, duble click or clickhold." With
  a genuinely static cursor, `computeResponsiveWristSplayDeg()` returns
  the identical value whether recomputed live or captured once, so the
  freeze fix could not have been the (whole) explanation for a static-
  cursor symptom -- it remains correct for its own actual case (cursor
  moving mid-transition), just narrower than first believed.
  **The real, final root cause, found by reading `updateRenderOrder()`'s
  own idle branch line by line:** it re-applies the wrist bone's live
  Responsive Wrist Splay rotation EVERY frame for an idle hand, but
  never re-applies finger curl -- curl is normally only recomputed on a
  slider change or "Default" click (`applyAllFingerPoses()`), not every
  frame. `rotateOnTrueWorldAxis()` converts a curl's intended WORLD-space
  axis into a LOCAL bone rotation using `bone.getWorldQuaternion()` --
  the CURRENT parent-chain world orientation at the exact instant curl
  is computed (the same mechanism the original wrist-before-fingers fix,
  many rounds earlier, already established). `rThumb1` is the only
  finger bone parented directly to the wrist bone (`rHand`), so it's the
  only one whose local rotation goes stale the moment the wrist keeps
  rotating afterward -- independent of cursor movement (a nonzero live
  splay value sits there whether the cursor moves or not), and matching
  "every other finger is correct, it's just the thumb" exactly. "Default"
  looks correct for exactly the one frame it's applied (extraSplayDeg=0
  in that specific call), then the wrist keeps moving via live splay
  every frame after that while the thumb doesn't follow.
  Fix: the idle branch now also re-applies the THUMB's curl (only the
  thumb -- the other 4 fingers are provably unaffected, confirmed by the
  original wrist-ordering fix) immediately after the wrist update,
  mirroring the same "wrist then fingers, together, every frame" pattern
  `applyPoseValuesToHand()` already uses during an active transition.
  **Verified with a genuinely independent double-check, static cursor
  throughout:** 2 separate computations of the thumb's WORLD orientation
  at a hand with a 90-degree live splay swing -- (A) the real route
  (Default, then 5 real idle frames via `updateRenderOrder()`) and (B) an
  independent reference (wrist set directly to its final value, thumb
  curl applied fresh against it, called directly, not through the frame
  loop) -- measured **0.0 degrees** of difference. Confirmed the test
  was actually discriminating by reproducing the OLD (pre-fix) behavior
  the same way and comparing against the same reference B: **12.5
  degrees of real, measurable error.** See CHANGELOG.txt's matching entry
  for the full account, including a note on this round's own live-browser
  verification being intermittently blocked by local static-server
  connection resets (an environment issue, confirmed via `curl` too,
  unrelated to any code change) -- worked around with a fresh server
  instance on an alternate port.
- **`updateRenderOrder()`'s idle branch now gates its own per-hand
  wrist+finger repose behind `needsIdleRepose = cfg.wristSplayResponsiveEnabled
  || hand._wasOverriddenLastFrame`**, instead of running unconditionally
  every frame. Direct live measurement (255 hands, `performance.now()`
  around isolated `updateRenderOrder()` calls, 50ms gaps to avoid a GPU-
  queue-backlog measurement artifact this same investigation had to rule
  out first) found the repose itself -- not rendering -- was the real
  cursor-lag bottleneck: ~30-44ms/call with Responsive Wrist Splay on,
  ~0.2-0.7ms/call with it off. Safe to skip when Splay is off because
  nothing else re-triggers a change frame-to-frame while idle (a manual
  slider/Default-pose edit already goes through its own onChange-driven
  repose). `hand._wasOverriddenLastFrame` (set at the end of each hand's
  own block in the per-hand loop) forces exactly one repose the frame
  after a Click-Hold-Pose/Click-Pose/Double-Click-Hold-Tween transition
  ends, so a hand returning to idle still re-syncs whatever curl the
  transition left it holding before the gate can start skipping again.
- **New: "Right Click"** -- a 3rd instance of the Click-Pose family
  (`CLICK_POSE_KEYS = ['click', 'dblclick', 'rc']`), on the right mouse
  button via its own quick-tap `pointerup` listener (guarded against
  double-firing with a genuine Right-Click Hold-Pose release by
  `lastPointerupWasRchpHoldRelease`, split out from the combined
  `lastPointerupWasHoldRelease` flag specifically for this). Adds one
  capability none of the other 3 Click-family triggers have: a `rcMode`
  select ('Single Pose' / 'Tween') choosing whether the shared forward/
  paused/retransition phase machine (`updateClickPoseForHand()`,
  unchanged for `click`/`dblclick`) interpolates toward a single named
  Target Pose (the pre-existing behavior) or plays through a resolved
  Tween Sequence via `lerpTweenSequence()` (`cp.tweenPoses`, set once in
  `triggerClickPose()` as `[liveSnapshot, ...resolveTweenSequencePoses(...)]`
  -- starts from the hand's own current live pose, not
  `poseDefaultValues`, keeping it consistent with Single Pose mode's own
  "transition from wherever it currently is," unlike Double Click Hold
  Tween's always-from-default convention). Only `rcTargetPose`/
  `rcTweenSelector` are mode-specific; every other control (Enabled, the
  4 timing/stagger fields, Pause Duration) applies to both modes
  unchanged, so switching modes mid-session doesn't reset any tuning.
  Mode-dependent row visibility (`updateRightClickModeVisibility()`) uses
  `row.style.display`, not the `hidden` attribute -- `.dp-row { display:
  flex }` in style.css is an author-stylesheet rule that wins the cascade
  over the UA stylesheet's `[hidden] { display: none }` at equal
  specificity regardless of source order, so toggling `.hidden` alone
  would have silently done nothing (confirmed live before shipping the
  inline-style version). `rcMode`/`rcTargetPose`/`rcTweenSelector` also
  hit the same pre-`cfg`-exists TDZ timing gap already on record for
  `chpTargetPose` etc. (see the "New: 'Click Pose'" entry above) --
  needed their own 3 `safeRefreshSelectOptions()` calls added to that
  same one-time post-`cfg` list, not automatically covered by the
  existing 4. Landed via a concurrent session's commit alongside an
  unrelated Double Click Hold Loop checkbox change (both unauthored by
  that commit's own author, found mid-task in the same uncommitted
  working tree, non-overlapping, committed together since they couldn't
  be cleanly split) -- this entry is that work's own author, verifying
  it live after the fact via `window.__debug.triggerClickPose('rc')`
  plus manually-stepped `updateRenderOrder()` calls (this session's
  browser tool has its own known rAF-timing unreliability, so real
  frames were driven directly rather than waited on). See CHANGELOG.txt's
  5th 2026-09-15 entry for the complete account.
- **devPanel.js's `list-picker` control gained shift-click multi-select,
  feeding a new auto-group behavior on `+Group`** (direct request,
  2026-09-15) -- a real engine change (not a main.js patch), since
  `buildListPickerRow()`/`renderListPickerItemRow()` are shared verbatim
  by every list-picker in this file (Saved Poses, Saved Tween Sequences,
  Saved Cameras). `entry.selectedItem` (the pre-existing single-item
  state `getSelectedSavedPoseItem()` above already reads externally via
  `.dp-list-picker-row-selected`) is UNCHANGED in meaning -- still one
  item, still what Use/Rename/Overwrite/Delete act on. A new, separate
  `entry.multiSelected` `Set` (plus `entry.selectAnchor`, the last plain-
  clicked row) tracks a shift-click range, computed from CURRENT RENDERED
  `.dp-list-picker-row` DOM order rather than `entry.items` directly, so
  the range correctly spans group boundaries the way a user visually
  sees the flattened list. Only `+Group`'s own handler reads
  `multiSelected`: non-empty means "create the new group already
  populated with these items" (re-parenting any that were in a different
  group), empty falls back to the original "create an empty group"
  behavior unchanged. A 2nd new CSS class (`dp-list-picker-row-multi-
  selected`, style.css) gives the range an accent-colored outline,
  deliberately distinct from `-selected`'s own solid fill, so it stays
  visually clear which single row Delete/Rename/Use/Overwrite would act
  on vs. which rows `+Group` would sweep up. Verified live via dispatched
  `MouseEvent('click', {shiftKey})` (a faithful simulation -- the
  listener only ever reads `e.shiftKey`, so this exercises the identical
  code path a real shift-click would): a 4-item range moved correctly
  into a new group on `+Group`, the other items untouched; the original
  no-selection empty-group path re-confirmed unchanged; plain-click
  single-selection regression-checked clean. See CHANGELOG.txt's latest
  2026-09-15 entry for the complete account.
